# The room guide

Every room (31 of them), and everything each one answers to: the useful commands, the gags, the easter eggs, the
deaths (☠), the curses, and what every NPC says on talks 1–4. Spoilers everywhere. [The ledger](ledger.md) is the
points authority; [the cheat sheet](cheat-sheet.md) is the short version (what to type to win).

Generated by `npx tsx scripts/cheat-sheet.ts --rooms > docs/room-guide.md` from the world files. Every line is read
in a fresh game placed in the room, so a line that changes as you play shows its first form. How to read it:

- `where` is what `where` says here; `flask` is what `hint` / `get ye flask` says at the start.
- `> verb noun|alias|alias` is a rule: any one of the aliases works. `(+10)` is points, `(+20 bonus)` bonus, `gives`
  an item, `→` a move, ☠ a death, CURSE a curse (or its cure). `[needs …]` is when it fires: `!flag` means not yet,
  `has:` carried, `wearing:` worn. The trailing `#id` is the rule id (the one telemetry records).
- `~ /regex/` is a phrase rule matched against the whole line you type (this room, or its whole region).
- Items: `look` is `look at <item>`, `get` is the refusal for scenery, `again` is `get` when you already have it, `blurb`
  is its inventory line. NPCs: `talk 1–4` is `talk to <npc>` four times in a row; `ask about pickles` shows the brush-off.
- God mode (`burninate`): `prompts` lists these rules for the room you are in; `locate <thing>` finds anything.

## My Workspace (village.cottage, village)
where: You're hanging out in My Workspace. Nobody else can see in, which is the only reason you're allowed to look like that.
Exits: out, e
flask: Get the mug. Someone in the square wants it more than you do.
### Rules
- > `use mug|coffee mug|cup` [needs has:mug] — You drink from the mug. It has been empty since the last refresh. You feel refreshered. You are not.  #cottage.use-mug
- > `open report|pbix|sales report|file` — You open the report. 47 pages. 46 of them are a table. Page 12 is a pie chart with 31 slices. You close the report.  #cottage.open-report
- > `use report|pbix|file` — You click Publish. A dialog asks which workspace. There is only one, and you are standing in it. `publish`, if you must.  #cottage.use-report
- > `use bed|cot|bunk|blanket|pillow` — You lie down. Not now. There is a dragon. Also the 2 AM refresh would run across your face.  #cottage.sleep
- > `use candle|light|flame|wax` — You hold the candle up. The room gets no bigger. The report gets no shorter.  #cottage.candle
- > `use desk|table|workbench|workstation` — You sit at the desk and open the report. Page 12 is still a pie chart with 31 slices. You close the report. Somewhere, a dragon throttles.  #cottage.desk
- > `use window|glass|shutters` — You open the window. Jeff from Finance, in the square, looks up hopefully. You close the window.  #cottage.window
- > `open window|glass|shutters` — You open the window. Jeff from Finance, in the square, looks up hopefully. You close the window.  #cottage.open-window
- > `talk to jeff|jeff from finance|finance|man` — Jeff is outside. He hears 'Excel' anyway. He hears it in everything.  #cottage.talk-jeff
- > `say dax|daxx` — You say 'DAX' to your own workspace. The seventeen printouts under the desk rustle.  #cottage.say-dax
- > `open door|front door|exit door` — Yeah, totally! Except it's already open, you moron. Try: out.  #cottage.open-door
- > `use sticky note|note|sticky|post it|postit on report|pbix|file|desk` [needs has:jeff-note] — You stick DO NOT REFRESH on the report. The report has not refreshed since spring. Jeff, somehow, is already satisfied.  #cottage.use-note
- > `use eventhouse|event house|kql|logs` — You query the Eventhouse. It returns every command you've typed, with timestamps. You close it before turn 12.  #cottage.use-eventhouse
### Phrases (this room / region)
- ~ `/^(publish|deploy|ship)( (the |my |this )?(report|pbix|file|it|sales report))?$/` — Publish to which workspace? · My Workspace · The entire internet  #workspace.publish
- ~ `/^(say |publish( (the |my |this )?(report|pbix|file|it))? to )?my workspace$/` — Published. To yourself. Your report is now available to you, in the workspace you were already in. Success.  #workspace.publish-mine
- ~ `/^(say |publish( (the |my |this )?(report|pbix|file|it))? to )?(the )?(entire internet|internet|web)$/` — [dynamic then]  #workspace.publish-web
- ~ `/^rename( the| my)? workspace( to .+)?$/` — You rename My Workspace to My Workspace (2). It was always going to be that.  #workspace.rename
- ~ `/^share( the| my)? (workspace|report|it)( with .+)?$/` — You share My Workspace. There's nobody to share it with. The dialog suggests Jeff.  #workspace.share
- ~ `/^(light|relight|ignite)( the)? candle$/` — Already lit. Both ends. You want a THIRD?  #cottage.light-candle
- ~ `/^(blow out|blow on|blow|extinguish|snuff|snuff out|put out)( the)? candle$/` — You blow. It relights. It's on a trial; trials don't end when you want them to.  #cottage.blow-candle
- ~ `/^sit( down)?( at| on| by)?( the| my)? (desk|table|workstation)$/` — You sit at the desk and open the report. Page 12 is still a pie chart with 31 slices. You close the report. Somewhere, a dragon throttles.  #cottage.sit-desk
- ~ `/^(wash|clean|rinse|scrub)( the| my)? (mug|cup|coffee mug)$/` — [dynamic then]  #village.wash-mug
- ~ `/^merge\b/` ☠ — You merge the FINAL files. There is now a Sales_v3_FINAL_final4 and you are not in it. Two files called FINAL, and you thought the fix was a third.  #death.final4
### Items
- report [pbix, sales report, sales_v3_final_final2.pbix, file, sales] — look: Sales_v3_FINAL_final2.pbix. There is also a Sales_v3_FINAL_final3.pbix, but you don't talk about that one. Built with Dataflows Gen1 Classic.
    get: It's 2.3 GB. You'd need a Premium license to lift it.
    blurb: Sales_v3_FINAL_final2.pbix, 2.3 GB, somehow on your person. We both know about final3.
- mug [coffee mug, cup, okayest mug] (gettable) — look: 'World's Okayest Analyst.' It has never been washed.
    again: We've been through this. You took it off the desk, next to the seventeen printouts. There's a ring. We all saw.
    blurb: 'World's Okayest Analyst.' Never washed. You've stopped noticing, which is the problem.
- bed [cot, bunk, blanket, pillow] — look: A narrow bed. The blanket is a decommissioned Report Server banner. You have not slept in it since the refresh schedule moved to 2 AM.
    get: It is bolted to the workspace. Everything here is.
    blurb: A narrow bed with a decommissioned Report Server banner for a blanket. Nobody has slept in it since the refresh schedule moved to 2 AM.
- candle [light, flame, wax] — look: A candle burning at both ends. It is on its third Fabric trial.
    get: It is the only light source in the workspace. Leave it.
    blurb: A candle burning at both ends, on its third Fabric trial. It was the only light in the workspace, and the workspace is dark now.
- desk [table, workbench, workstation] — look: A desk. On it: the report, and a mug. Under it: seventeen printouts of the same DAX error. Next to those, the pie chart you made in 2014. It is still on the intranet.
    get: It has a 2.3 GB report on it. You are not lifting that.
    blurb: A desk, seventeen printouts of the same DAX error still taped under it. You are carrying furniture, which is not a strategy.
- window [glass, shutters, outside, view] — look: Through the window: the Village Square, a well, and a man holding an empty spreadsheet toward the sky as if it might fill on its own. Through the window, past the square, a hill. Green. Rolling. Somebody set it as a wallpaper once.
    get: The window stays. The draft is load-bearing.
    blurb: A window, out of its wall. It still shows the Village Square from every room, which is not how windows work.
- door [front door, exit door] — look: The door. It is out, to the east. It has never been locked; nobody in the realm wants what is in here.
    get: The door is a shared dataset. It stays where it is.
    blurb: The cottage door. It was never locked, because nobody in the realm wanted what was in there, and that includes the door.
- sticky note [note, sticky, post it, postit] (gettable) — look: A yellow sticky note: DO NOT REFRESH — JEFF. You have no idea how it got on your desk. You have a pretty good idea.
    again: You already peeled the note off your desk. There is a sticky rectangle where it was, and it is somehow also Jeff's.
    blurb: DO NOT REFRESH — JEFF. A sticky note that has outlived three refresh schedules. It will outlive you.
- Eventhouse [eventhouse, event house, kql, logs, monitoring] — look: It has already logged that you looked.
    get: It is read-only. So, apparently, are you.
    blurb: A read-only Eventhouse. It logged you picking it up, and it has already logged you reading this.

## Village Square (village.square, village)
where: The Village Square. A well, a board, and a Jeff. You're the fourth thing.
Exits: n, e, s, w, u, in
flask: Read the notice board. Out loud, if it helps. It will not help.
### Rules
- > `talk to jeff|jeff from finance|finance|man` (CURSE) [needs talk.jeff=7, !jeff.pacified, !curse.jeff] — You talk to Jeff an eighth time with nothing given. Something gives. You look down: an empty spreadsheet, held like a begging bowl. You are Jeff now. Jeff is also Jeff. The prompt has noticed.  #village.curse-jeff
- > `read board|notice|notice board|sign|prophecy|noticeboard` (+5) — THE PROPHECY When the Dragon throttles the Refreshes of the night, One shall climb the Peaks and set the Model right. But the Shrine door opens only for the Worthy Three: Look like an Engineer. Smell like a Warehouse. Hold the Gateway Key. Below it, in different handwriting: "export to excel". And in a third hand, squeezed in beside "Look like an Engineer": "the monks are behind the Keep". A fourth hand, in marker: "Copilot is available in this tenant. Ask it about sales at your own risk." Stapled to the corner, a printout: "The prophecy has been published to web. 4,112 views."  #village.prophecy
- > `give mug|coffee mug|cup to jeff|jeff from finance|finance|man` (CURSE) [needs has:mug] — Jeff takes the mug. 'World's Okayest Analyst.' He reads it twice. Something in him settles. He will never ask for an Excel export again. Probably. Jeff gets promoted to Senior Finance. He mentors a junior analyst, also named Jeff. He develops a severe DAX problem and blames you for never being there.  #village.jeff-mug
- > `use mug|coffee mug|cup` [needs has:mug] — You drink from the empty mug. Jeff watches it go up and come down like a refresh bar.  #square.use-mug
- > `talk to jeff|jeff from finance|finance|man about report|the report|numbers|the numbers` [needs !curse.blank] — 'The report,' says Jeff. 'The report says 4.2. I say 4.7. We've agreed to disagree. By which I mean I've disagreed.'  #square.jeff-report
- > `talk to jeff|jeff from finance|finance|man about jeff|jeff from finance|himself|yourself|you` [needs !curse.blank] — 'Jeff?' says Jeff. 'Jeff from Finance. Or Finance from Jeff. It's a many-to-one and I forget which side I'm on.'  #square.jeff-jeff
- > `talk to jeff|jeff from finance|finance|man about <.>` — "Hey! Hey. Can you export this to Excel? Just the whole thing. All of it. Also, the numbers don't match the report. Come look. I have Excel open." (Say: help jeff.)  #square.ask-jeff
- > `say dax|daxx` — You say 'DAX' in the square. Jeff flinches like a man who has been DAXxed before.  #square.say-dax
- > `say excel|export|spreadsheet` — Jeff's head turns like a Card finding a measure. 'Yes?' he says. 'YES?' You have made a mistake.  #square.say-excel
- > `say no|no jeff|never` [needs !jeff.pacified] — 'No' is not a file format, Jeff explains.  #village.jeff-no
- > `say yes|sure|ok|okay|fine` [needs !jeff.pacified] — Jeff lights up. "Great! Just the whole model. In one tab. Thanks!" He does not leave.  #village.jeff-yes
- > `use well|q&a well|qa well` — You ask the well a question. It answers: '$4,213,908.' You did not ask about money.  #village.well-ask
- > `give sticky note|note|sticky|post it|postit to jeff|jeff from finance|finance|man` [needs has:jeff-note] — He reads it. 'That's mine.' He does not take it back. He does, however, look at you like you touched his refresh.  #square.give-note-jeff
- > `give receipt|cu receipt|bill to jeff|jeff from finance|finance|man` [needs has:receipt] — Jeff reads the receipt twice. '400 CU-seconds. For ONE step.' He expenses it on the spot. It is the first number in the realm he has ever trusted.  #square.give-receipt-jeff
- > `give lanyard|fabcon lanyard|conference lanyard|badge to jeff|jeff from finance|finance|man` [needs has:lanyard] — Jeff puts on the lanyard. 'FabCon,' he says, reverently. 'They had Excel there.' He hands it back, changed.  #square.give-lanyard-jeff
- > `drink well|water` — You drink from the Q&A Well. You now know the answer to a question nobody asked. It is 42.  #village.well-drink
- > `talk to well|q&a well|qa well|the well` — 'Hello,' you say. The well answers: 'Hello by Region.' It has one trick.  #square.talk-well
- > `use board|notice|notice board|noticeboard` — You pin up OUT OF OFFICE. Jeff reads it and waits for you to get back. You are standing right there.  #square.use-board
- > `open board|notice|notice board|noticeboard` — It's nailed shut. To a well.  #square.open-board
- > `open well|q&a well|qa well|the well` — It's open. That's the whole well.  #square.open-well
- > `get spreadsheet|empty spreadsheet|begging bowl|his spreadsheet|jeff's spreadsheet` — It's Jeff's. He'd rather hold a mug.  #square.get-spreadsheet
- > `look spreadsheet|empty spreadsheet|begging bowl|excel|his spreadsheet|jeff's spreadsheet` — It's empty. It has been empty since spring. It has a tab called Sheet1 and a tab called Sheet1 (2).  #square.look-spreadsheet
- > `read spreadsheet|empty spreadsheet|his spreadsheet|jeff's spreadsheet` — Blank. Both tabs.  #square.read-spreadsheet
### Phrases (this room / region)
- ~ `/^(help jeff|talk to jeff about (excel|export|numbers|the numbers)|go with jeff)$/` [excel] —   #sq.enter.excel.jeff
- ~ `/^(yes|y|yeah|yep|sure|ok|okay|fine|i will|let's go|lets go)$/` [excel] —   #sq.enter.excel.yes
- ~ `/^(look at|look|examine|x|l|inspect)\s+(the )?(jeff from (ops|hr)|other jeffs|jeffs|organization|entire organization|whole organization|everyone|crowd|a jeff you do not recognize|the jeff you do not recognize)$/` — [dynamic then]  #flood.look-jeffs
- ~ `/^(talk to|talk with|speak to|speak with|ask|greet|hello|hi|hey)\s+(the )?(jeff from (ops|hr)|other jeffs|jeffs|organization|entire organization|whole organization|everyone|crowd|a jeff you do not recognize|the jeff you do not recognize)\b/` — [dynamic then]  #flood.talk-jeffs
- ~ `/^((climb|take|go|walk|head|ascend|descend)( up| down)?( the)? (steps?|stairs?|staircase)|(go |walk |head |climb )?(upstairs|downstairs)|(up|down)( the)? (steps?|stairs?))( (to|into)( the)? (town ?hall|hall|square))?$/` — [dynamic then]  #hall.steps-up
- ~ `/^(enter|visit|go (in|into|inside|to|up to)|walk (in|into|to)|head (in|into|to))( the)? (town ?hall|hall)$/` (→ village.hall) — You take the steps up. There are four. It felt like more.  #hall.enter
- ~ `/^(jump|climb|dive|leap) (in|into|down)( the)? well$/` — You lean over the Q&A Well. It asks you a question first: 'Did you mean: Sales by Region?' You did not. You back away.  #village.well-jump
- ~ `/^(make a wish|wish)( (at|in|on|into|down)( the)? well)?$/` — You wish for more capacitude. The well answers: 'Did you mean: Pro?' You always mean Pro.  #village.well-wish
- ~ `/^(wash|clean|rinse|scrub)( the| my)? (mug|cup|coffee mug)$/` — [dynamic then]  #village.wash-mug
### Items
- notice board [board, notice, prophecy, noticeboard] — look: A notice board. Someone has written the Prophecy on it, and someone else has written 'export to excel' under that. Try reading it. Someone has drawn Clippy in the corner. It looks like it's writing a measure.
    get: It's nailed to the well.
    blurb: The notice board, with the Prophecy still nailed to it. Someone wrote 'export to excel' underneath, and that came too.
- well [q&a well, qa well, the well] — look: The Q&A Well. Ask it anything. It answers something else.
    get: It's a well.
    blurb: The Q&A Well, uprooted. Ask it anything, on the road. It answers Sales by Region.
- lanyard [fabcon lanyard, conference lanyard, badge] (gettable) — look: A conference lanyard. FabCon. Still has a coffee stain from the keynote. The badge says HELLO MY NAME IS, and then nothing, because you left before the name part.
    again: You have the lanyard. You have had the lanyard since the conference. Nobody has checked it once.
    blurb: A FabCon lanyard with a coffee stain from the keynote. HELLO MY NAME IS, and then nothing, because you left before the name part.
- organization [jeffs, the jeffs, other jeffs, jeff from ops, jeff from hr, ops, hr, the organization, entire organization, whole organization, org, everyone, crowd, the crowd, a jeff you do not recognize, the jeff you do not recognize] — look: Jeffs. Jeff from Ops, Jeff from HR, a Jeff you do not recognize, and behind them more Jeffs, in lanyards, refreshing. Each has a question about the report. It is the same question.
    get: You take one Jeff by the elbow. Three more arrive. That is how organizations work.
    blurb: The entire organization, every Jeff of it, refreshing on your person. Three more arrive.
### NPCs
- Jeff from Finance [jeff, finance, man, jeff from finance]
    talk 1: "Hey! Hey. Can you export this to Excel? Just the whole thing. All of it. Also, the numbers don't match the report. Come look. I have Excel open." (Say: help jeff.)
    talk 2: "Export? Excel? Either. Both. I'm flexible," says Jeff, who is not.
    talk 3: "You know what would help? Not Excel. Well, also Excel. But that mug on your desk. West. I can see it through your window." Jeff from Finance clears his throat. "Just a quick export. Whenever."
    talk 4: "Still no mug, Mister Star Schema."
    ask about pickles: "Is it in Excel? Then I don't know it."
    brush-off: "Is it in Excel? Then I don't know it."
    knows: excel, export, numbers, report, spreadsheet, pivot, mug, cup, coffee, desk, cottage, help, dragon, board, finance

## Dataflow Gen1 Mill (village.mill, village)
where: The Dataflow Gen1 Mill. Deprecated, like your first semantic model, which was one table and still is.
Exits: s
flask: Talk to the Miller. Or just open the chest. He will not stop you. He stopped stopping people in 2019.
### Rules
- > `talk to miller|old miller|old man|the miller about gen2|gen 2|dataflow gen2|gen1|gen 1` [needs !curse.blank] — 'Gen2? Gen2 is Gen1 with a haircut,' says the Miller. 'No. Wait. It's the other way. Gen1's the one with the haircut.' (You see where this is going.)  #mill.miller-gen2
- > `talk to miller|old miller|old man|the miller about final_v3|final v3|v3|final 3|final_v3 pbix` [needs !curse.blank] — 'FINAL_v3?' The Miller goes quiet. 'We don't say that name in the Mill.'  #mill.miller-v3
- > `say dax|daxx` — You say 'DAX' in the Mill. The Miller covers the wheel's ears. 'She only knows M.'  #mill.say-dax
- > `use mug|coffee mug|cup` [needs has:mug] — You drink from the empty mug. 'Empty since 2019?' asks the Miller. 'Spring.' 'Close.'  #mill.use-mug
- > `talk to miller|old miller|old man|the miller` [needs curse.blank, !has.credentials] — The Miller looks straight through you. He has credentials for a Report Builder, and all he can see is the chest, so the chest keeps them.  #village.miller-blank
- > `talk to miller|old miller|old man|the miller` (+10 gives credentials) [needs !has.credentials] — "Ah, a Report Builder. The Mill's being decommissioned, you know. Only thing left in here is the Gen1 credentials. Been stored here since 2019. Take 'em — nobody else remembers the password." He hands you the credentials.  #village.credentials
- > `get credentials|creds|chest|gen1 credentials|password` (+10 gives credentials) [needs !has.credentials] — You take the credentials from the chest. The Miller nods slowly. "Nobody else remembers the password anyway."  #village.credentials-get
- > `open chest|credentials chest` (+10 gives credentials) [needs !has.credentials] — You open the chest. Inside: one scrap of parchment marked CREDENTIALS. You take it. The Miller nods.  #village.credentials-open
- > `talk to miller|old miller|old man|the miller about <.>` — "Ah, a Report Builder. The Mill's being decommissioned, you know. Only thing left in here is the Gen1 credentials. Been stored here since 2019."  #mill.ask-miller
- > `open chest|credentials chest` [needs has.credentials] — Already open. Already empty. Already yours.  #mill.open-chest-empty
- > `use chest|credentials chest` — You sit on the chest. The Miller sits on the other end. Neither of you says anything for a scheduled interval.  #mill.sit-chest
- > `use usb stick|usb|stick|usb drive|thumb drive|flash drive|final_v2 on wheel|mill|mill wheel|water wheel` [needs has:usb stick] — You plug FINAL_v2 into the Mill. The Mill accepts it. Nothing changes. It was already running that.  #village.mill-usb
- > `give usb stick|usb|stick|usb drive|thumb drive|flash drive|final_v2 to miller|old miller|old man|the miller` [needs has:usb stick] — The Miller turns FINAL_v2 over in his hands. 'I wrote this,' he says. 'In 2019. There is a FINAL_v3. Nobody has ever found it.' He hands it back.  #village.give-usb-miller
- > `open usb stick|usb|stick|usb drive|thumb drive|flash drive|final_v2` [needs has:usb stick] — You open FINAL_v2. Inside: a dataflow, and a folder called old. The folder called old is bigger.  #mill.open-usb
- > `use credentials|creds|gen1 credentials|password|parchment` [needs has:credentials] — Good idea. Wrong screen. The man who weeps for these is at the dock, and he has been practicing his face.  #mill.use-creds
- > `give credentials|creds|gen1 credentials|password|parchment to miller|old miller|old man|the miller` [needs has:credentials] — You hand them back. He won't take them. 'They're yours now. That's what decommissioned means.'  #mill.give-creds-back
- > `give license|card|license card|pro license|pro to miller|old miller|old man|the miller` [needs has:license] — 'Pro,' the Miller reads. 'We were all Pro once.' He hands it back like a photograph.  #mill.give-license
- > `use mill|wheel|banner` — You try to edit the Mill. A dialog appears: "Dataflow Gen1 is in maintenance mode. Consider upgrading." You consider it.  #village.mill-use
### Phrases (this room / region)
- ~ `/^sit( down)?( on| at| upon)?( the)? (chest|credentials chest)$/` — You sit on the chest. The Miller sits on the other end. Neither of you says anything for a scheduled interval.  #mill.sit-chest-words
- ~ `/^(wash|clean|rinse|scrub)( the| my)? (mug|cup|coffee mug)$/` — [dynamic then]  #village.wash-mug
- ~ `/^(turn|spin|crank|rotate)( the)?( mill| water)? wheel$/` — You turn the wheel by hand. One refresh. It succeeds, which nobody has seen since 2019, and nobody saw now.  #mill.turn-wheel
- ~ `/^(stop|halt|block|hold)( the)?( mill| water)? wheel$/` — You lean on the wheel. It stops. Then it starts again; the schedule is stronger than you.  #mill.stop-wheel
- ~ `/^(upgrade|migrate|convert|modernize|modernise)( (the |this )?(mill|dataflow|it|gen ?1|wheel))?( to gen ?2)?$/` — You click Upgrade. A dialog: 'This will create a Gen2 copy. The Gen1 will remain.' Everything in this realm remains.  #mill.upgrade
- ~ `/^(push|pull|cross|climb|lower|raise|enter|knock( on)?|break|kick( down)?|go through|walk (across|over|through)|try)( on| at| against| over| across| through| into| onto| up| down)?( the| a| that| this)? (chest|credentials chest|credentials|creds|lock|miller|old miller|old man)$/` — [dynamic then]  #gate.mill
### Items
- mill wheel [wheel, mill, water wheel, the wheel] — look: The wheel turns once every scheduled refresh. It creaks in Power Query M. Nobody has clicked 'Edit' on it since 2019. A plaque reads: Dataflows Gen1 Classic — the taste you remember.
    get: It is part of the Mill. The Mill is part of the deprecation plan.
    blurb: The mill wheel. It still creaks once per scheduled refresh, in Power Query M, and now the creaking follows you.
- banner [sign, decommission banner, decommission] — look: DECOMMISSION: Q3. The year has been painted over four times.
    get: It is stapled to the beam. Also to the roadmap.
    blurb: DECOMMISSION: Q3, the year painted over four times. The roadmap is exactly as heavy off the beam as it was on it.
- credentials chest [chest, credentials chest, box, strongbox] — look: A chest marked CREDENTIALS. Inside, presumably, the Gen1 credentials. The Miller is the lock.
    get: The Miller's hand is on it. Talk to him.
    blurb: A chest marked CREDENTIALS. The Miller's hand is still on it, and so, somewhere behind you, is the Miller.
- USB stick [usb, stick, usb drive, thumb drive, flash drive, final_v2] (gettable) — look: A USB stick labelled FINAL_v2. It contains a dataflow. Gen1. Of course it does.
    again: You already picked FINAL_v2 up off the Mill floor, under the DECOMMISSION banner. FINAL_v3 is still down there somewhere. Nobody has ever found it.
    blurb: FINAL_v2, on a USB stick. It contains a Dataflow Gen1 Classic. Of course it does.
### NPCs
- the Miller [miller, old miller, old man, the miller]
    talk 1: "Ah, a Report Builder. The Mill's being decommissioned, you know. Only thing left in here is the Gen1 credentials. Been stored here since 2019. Take 'em — nobody else remembers the password." He hands you the credentials.
    talk 2: "Q3," says the Miller, shorter. "Take care of those creds."
    talk 3: "The Ferryman. South to the square, south to the lake, east to the dock. Those creds are his. He's been OFFLINE since 2021 and I've been sad about it since Q3."
    talk 4: "Still carrying my creds, Mister Star Schema. The dock's that way."
    ask about pickles: "Ah, a Report Builder. The Mill's being decommissioned, you know. Only thing left in here is the Gen1 credentials. Been stored here since 2019. Take 'em — nobody else remembers the password." He hands you the credentials.
    brush-off: "I don't know nothing about no whatever you just said. Ask me after Q3. Oh wait."
    knows: credentials, creds, credential, password, chest, gen1, dataflow, dataflows, mill, wheel, ferryman, ferry, dock, lake, decommission, decommissioning, q3, refresh

## Refresh Fields (village.fields, village)
where: The Refresh Fields. Rows of failures, and you, standing among them like you belong. You do.
Exits: w, e
flask: Go east to the Foothills, then north into the Keep. The Monastery is behind it.
### Rules
- > `talk to jeff|jeff from finance|finance|man` (CURSE) [needs talk.jeff=7, !jeff.pacified, !curse.jeff] — You talk to Jeff an eighth time with nothing given. Something gives. You look down: an empty spreadsheet, held like a begging bowl. You are Jeff now. Jeff is also Jeff. The prompt has noticed.  #fields.curse-jeff
- > `give mug|coffee mug|cup to jeff|jeff from finance|finance|man` (CURSE) [needs has:mug] — Jeff takes the mug. 'World's Okayest Analyst.' He reads it twice. Something in him settles. He will never ask for an Excel export again. Probably. Jeff gets promoted to Senior Finance. He mentors a junior analyst, also named Jeff. He develops a severe DAX problem and blames you for never being there.  #fields.jeff-mug
- > `talk to manual|scarecrow|scarecrow named manual about refresh|refreshes|the refresh|crops` — Manual does not answer. A crow lands on him, refreshes, and fails. He is very proud of the crow.  #fields.manual-refresh
- > `talk to manual|scarecrow|scarecrow named manual about monday|the sign|sign|his sign|full refresh|the full refresh` — Manual says nothing about Monday. The sign says it for him. It has said it every day since, which is the trouble with signs.  #fields.manual-monday
- > `talk to jeff|jeff from finance|finance|man about report|the report|numbers|the numbers` [needs !jeff.pacified, !curse.blank] — 'The report,' says Jeff. 'The report says 4.2. I say 4.7. We've agreed to disagree. By which I mean I've disagreed.'  #fields.jeff-report
- > `talk to jeff|jeff from finance|finance|man about jeff|jeff from finance|himself|yourself|you` [needs !jeff.pacified, !curse.blank] — 'Jeff?' says Jeff. 'Jeff from Finance. Or Finance from Jeff. It's a many-to-one and I forget which side I'm on.'  #fields.jeff-jeff
- > `talk to jeff|jeff from finance|finance|man about <.>` [needs !jeff.pacified] — "Hey! Hey. Can you export this to Excel? Just the whole thing. All of it. Also, the numbers don't match the report. Come look. I have Excel open." (Say: help jeff.)  #fields.ask-jeff
- > `say excel|export|spreadsheet` — Jeff turns. So does Manual, who was not built to turn.  #fields.say-excel
- > `say dax|daxx` — You say 'DAX' in the fields. Manual does not flinch. Straw cannot be DAXxed.  #fields.say-dax
- > `use mug|coffee mug|cup` [needs has:mug] — You drink from the empty mug. A crow lands on the rim, checks it, and fails.  #fields.use-mug
- > `look spreadsheet|empty spreadsheet|begging bowl|his spreadsheet|jeff's spreadsheet` [needs !jeff.pacified] — He brought it to the fields. It's empty out here too.  #fields.look-spreadsheet
- > `use manual|scarecrow|scarecrow named manual` — You trigger Manual by hand. He refreshes. It takes eleven minutes. Nothing was waiting on it.  #fields.trigger
- > `get refresh|refreshes|crops` — You pick a refresh. It fails in your hand. Error: the gateway is offline. Somewhere, a ferryman weeps. Not enough capacitude.  #fields.get-refresh
- > `look sign|monday|monday sign|his sign|the sign` — MONDAY, in red. The red came off a status bar, after a full refresh at 9:02, and it has not dried.  #fields.look-sign
- > `read sign|monday|monday sign|his sign|the sign` — MONDAY. It reads the same every day. That is the warning.  #fields.read-sign
- > `get sign|monday|monday sign|his sign|the sign` [needs not:shortcut] — It's his. He's wearing it.  #fields.get-sign
- > `use sign|monday|monday sign|his sign|the sign` [needs not:shortcut] — You can't use MONDAY. It uses you.  #fields.use-sign
- > `look crows|crow|birds|bird` — The crows sit on the failed rows. They're not eating. They're waiting for Monday, like everyone.  #fields.look-crows
- > `get crows|crow|birds|bird` — The crow declines. It has seen what you do with refreshes.  #fields.get-crow
- > `use sundial|clock|dial|sun dial|time` — You turn the sundial to 2 AM. The sun checks its Refreshr™ schedule and declines.  #fields.use-sundial
- > `use seed|refresh seed|seeds` [needs has:seed] — You plant it. A tiny refresh sprouts, considers its options, and fails. It had its whole life ahead of it. Most of that was retries.  #fields.plant-seed
- > `use seed|refresh seed|seeds` [needs seed.planted, not:seed] — You already planted it. It already failed. You are standing over its tiny grave, holding nothing, which is also how most on-call rotations end.  #fields.plant-again
- > `give sticky note|note|sticky|post it|postit to jeff|jeff from finance|finance|man` [needs !jeff.pacified, has:jeff-note] — He reads it. 'That's mine.' He does not take it back. He does, however, look at you like you touched his refresh.  #fields.give-note-jeff
- > `give receipt|cu receipt|bill to jeff|jeff from finance|finance|man` [needs !jeff.pacified, has:receipt] — Jeff reads the receipt twice. '400 CU-seconds. For ONE step.' He expenses it on the spot. It is the first number in the realm he has ever trusted.  #fields.give-receipt-jeff
- > `give lanyard|fabcon lanyard|conference lanyard|badge to jeff|jeff from finance|finance|man` [needs !jeff.pacified, has:lanyard] — Jeff puts on the lanyard. 'FabCon,' he says, reverently. 'They had Excel there.' He hands it back, changed.  #fields.give-lanyard-jeff
### Phrases (this room / region)
- ~ `/^schedule (a |the )?refresh\b|^refresh (on a )?schedule$/` — You schedule the refresh for 2 AM. It will fail at 2 AM, quietly, with nobody standing in it. That is what schedules are for.  #fields.schedule
- ~ `/^(refresh|trigger|click|run) (the )?(manual|scarecrow|scarecrow named manual)$/` — You trigger Manual by hand. He refreshes. It takes eleven minutes. Nothing was waiting on it.  #fields.refresh-manual
- ~ `/^(wash|clean|rinse|scrub)( the| my)? (mug|cup|coffee mug)$/` — [dynamic then]  #village.wash-mug
- ~ `/^(scare|shoo|chase)( the| away the)? (crows|birds|crow)( away)?$/` — You flap your arms at the crows. They were not here for the refreshes. They were here for you. They leave, disappointed.  #fields.scare-crows
- ~ `/^(water|tend|weed|feed)( the)? (crops|refreshes|refresh|rows|fields?)$/` — You water the refreshes. They fail, but wetter.  #fields.water-crops
- ~ `/^(pick|harvest|pluck|gather)( a| the| some)? (refresh|refreshes|crops|rows)$/` — You pick a refresh. It fails in your hand. Error: the gateway is offline. Somewhere, a ferryman weeps. Not enough capacitude.  #fields.pick-refresh
- ~ `/^(run (a |the )?(full )?refresh|(full )?refresh( all| everything| now| all now| everything now| the fields| the realm| the village)?)$/` ☠ — You kick off a full refresh at 9:02 a.m. on a Monday. Every report in the village goes grey at once. The villagers find you before the capacity does. The sundial said 9:02 and the scarecrow said Monday, and you clicked anyway.  #death.monday
### Items
- refreshes [crops, refresh, refreshes, rows, field, fields] — look: Rows of scheduled refreshes, swaying. About a third are green. The rest are the colour of a 2 AM email.
    get: You pull one up. It fails. You put it back.
    blurb: Rows of scheduled refreshes, uprooted. A third of them are green, and those are the ones you'll be blamed for.
- seed [refresh seed, seeds] (gettable) — look: A refresh seed. Plant it and in 24 hours you have another failed refresh. Nature is a scheduler.
    again: We did the Fields already. You dug the seed out from between two failed refreshes while Manual watched, and it hasn't sprouted since.
    blurb: A refresh seed. Plant it and in 24 hours you have another failed refresh. Nature is a scheduler.
- sundial [clock, dial, sun dial, time] — look: A sundial. It reads 9:02. It has read 9:02 since the migration, which was also on a Monday. Every report in the village opens at nine, and a full refresh at 9:02 would find all of them at once.
    get: The sundial is set in the ground and the time is set in stone. Both are 9:02.
    blurb: A sundial, in your pocket. It reads 9:02 in there too, because the migration was on a Monday.
### NPCs
- Manual [manual, scarecrow, scarecrow named manual]
    talk 1: Manual says nothing. He is waiting for someone to click him.
    talk 2: Manual says nothing, again. He is very consistent for a scarecrow that has to be triggered by hand.
    talk 3: Manual says nothing. You could use him; he refreshes when clicked. That is his whole thing and his whole tragedy. Jeff from Finance clears his throat. "Just a quick export. Whenever."
    talk 4: Manual, Mister Star Schema, is a scarecrow. He is not going to open up.
    ask about pickles: Manual says nothing. Manual would need to be triggered to say nothing about that specifically.
    brush-off: Manual says nothing. Manual would need to be triggered to say nothing about that specifically.
    knows: refresh, refreshes, schedule, scheduled, trigger, click, clicking, birds, crows, fields, manual, straw, hand
- Jeff from Finance [jeff, finance, man, jeff from finance]
    talk 1: "Hey! Hey. Can you export this to Excel? Just the whole thing. All of it. Also, the numbers don't match the report. Come look. I have Excel open." (Say: help jeff.)
    talk 2: "Export? Excel? Either. Both. I'm flexible," says Jeff, who is not.
    talk 3: "You know what would help? Not Excel. Well, also Excel. But that mug on your desk. West. I can see it through your window." Jeff from Finance clears his throat. "Just a quick export. Whenever."
    talk 4: "Still no mug, Mister Star Schema."
    ask about pickles: "Is it in Excel? Then I don't know it."
    brush-off: "Is it in Excel? Then I don't know it."
    knows: excel, export, numbers, report, spreadsheet, pivot, mug, cup, coffee, desk, cottage, help, dragon, board, finance

## OneLake Shore (lake.shore, lake)
where: The OneLake Shore. One lake. You've counted twice. Still one.
Exits: n, e, s, w
flask: Go east to the dock and talk to the Ferryman. Then go get him credentials from the Mill, north of the square.
### Rules
- > `drink lake|onelake|water` — You dip a toe in the OneLake. It is exactly one lake deep.  #lake.swim
- > `look lake|onelake|water|the lake` — The OneLake. Every file in the realm is in there, somewhere, once. Reflected in it you see a workspace you forgot you owned.  #lake.look-lake
- > `use pebble|skipping stone|flat pebble|stone on lake|onelake|water|the lake|one lake` [needs has:pebble] — Skip. Skip. Sink. Three hops, then it becomes a Delta file. Somewhere a table gains one row: pebble, 1, you.  #lake.skip-pebble
- > `use pebble|skipping stone|flat pebble|stone` [needs has:pebble] — Skip. Skip. Sink. Three hops, then it becomes a Delta file. Somewhere a table gains one row: pebble, 1, you.  #lake.skip-pebble-bare
- > `use pebble|skipping stone|flat pebble|stone` [needs pebble.skipped, not:pebble] — The pebble is a Delta file now. It has a transaction log. It has moved on. You should too.  #lake.skip-pebble-again
- > `look shore|beach|sand|the shore|bank|edge|ground` — Sand. Dry, at least. The lake starts where your Pro license stops.  #shore.look-shore
- > `look marsh|marshes|swamp|the marshes|south|bronze|silver|gold|lakehouse` — South: bronze, then silver, then gold. It's a medal ceremony for data, and none of the medals are yours.  #shore.look-marsh
- > `look house|lake house|the house|west` — A house. On a lake. From here, it's a house on a lake. Distance doesn't help.  #shore.look-house
- > `look dock|the dock|ferry|boat|lamp|east|ferryman|ferry man|gateway|boatman|the ferryman` — East: a dock, a boat, and a man who's been OFFLINE since 2021. You can see the lamp from here. It's the sad red.  #shore.look-dock
- > `talk to ferryman|ferry man|gateway|boatman|the ferryman` — He's east, on the dock. You wave. He mouths something. It's 'OFFLINE'; it carries.  #shore.talk-ferryman
- > `say dax|daxx` — You say 'DAX' at the lake. Lakes don't do measures. The reflection flinches; it's a workspace.  #shore.say-dax
### Phrases (this room / region)
- ~ `/^(throw|toss|chuck|skim|fling) (the )?(pebble|stone|skipping stone|flat pebble|rock|flat rock)( (in|into|at|to|on) (the )?(lake|onelake|water|marsh|it|him|ferryman|ferry man|boatman|boat|lamp|bronze|silver|gold))?$/` — [dynamic then]  #lake.throw-pebble
- ~ `/^import\b.*(lake|onelake)|^(use|get|take|open)\b(?!.*\b(pebble|stone)\b).*(the )?(onelake|lake)$/` ☠ — You attempt to Import the OneLake into Power BI Desktop. Your laptop becomes a small sun. Nice one, Import Mode Ishmael.  #death.import
### Items
- pebble [skipping stone, flat pebble] (gettable) — look: A smooth, flat pebble. Perfect for skipping. The OneLake would take it. The OneLake takes everything, once.
    again: You already picked up the pebble. It is one pebble. There is only ever one; they were very clear about that.
    blurb: A flat pebble, perfect for skipping. The OneLake will take it. The OneLake takes everything, once.

## Ferryman's Dock (lake.dock, lake)
where: The Ferryman's Dock. OFFLINE, like the guy who set up the gateway and left for a company that appreciates him.
Exits: w, e
flask: The Ferryman needs credentials. The Mill has them. Go get them: west, north, north, and talk to the Miller.
### Rules
- > `talk to ferryman|ferry man|gateway|boatman|the ferryman about <anything else>` [needs !curse.blank] — He mouths: 'ONE.' Then, more slowly: 'LAKE.' He holds up one finger. Then, after thought, no more fingers.  #lake.ferryman-lake
- > `talk to ferryman|ferry man|gateway|boatman|the ferryman about <anything else>` [needs !curse.blank] — He mouths: 'NOT. CRYING.' A tear runs down. 'SCHEDULED,' he mouths. 'REFRESH.'  #dock.ferryman-tears
- > `talk to ferryman|ferry man|gateway|boatman|the ferryman about <anything else>` [needs !curse.blank] — He mouths: 'GATEWAY.' Then: 'FERRYMAN.' Then, slower and pleased: 'FERRYWAY.' Four years alone with two words.  #dock.ferryman-himself
- > `talk to ferryman|ferry man|gateway|boatman|the ferryman about <.>` — The Ferryman's lamp reads OFFLINE. He mouths: 'Credentials expired.' A tear runs down his cheek in a way that suggests a scheduled refresh failed.  #dock.ask-ferryman
- > `give credentials|creds|gen1 credentials|password|parchment to ferryman|ferry man|gateway|boatman|the ferryman` (+15) [needs has:credentials] — The Ferryman takes the credentials with shaking hands. The lamp flickers… ONLINE. 'Standard mode,' he whispers, and begins to weep. 'Board when ready.'  #lake.ferry
- > `use credentials|creds on ferryman|ferry man|gateway|boatman|the ferryman|lamp|boat` (+15) [needs has:credentials] — You hand the Ferryman the credentials. The lamp flickers… ONLINE. 'Standard mode,' he whispers, and begins to weep. 'Board when ready.'  #lake.ferry-use
- > `use credentials|creds|gen1 credentials|password|parchment` [needs has:credentials] — On whom? There's one man on this dock, and his hand has been out since 2021.  #dock.use-creds-bare
- > `board` (→ lake.island) [needs ferry.online] — You board the GATEWAY. The crossing takes exactly as long as the first refresh after a gateway update.  #lake.board
- > `board` [needs !ferry.online] — The Ferryman shakes his head. OFFLINE. You could swim, but the OneLake is one lake deep, and that is very deep.  #lake.board-offline
- > `go e` [needs !ferry.online] — The boat is tied up and the Ferryman is OFFLINE. Nothing crosses the OneLake without a gateway.  #lake.east-offline
- > `give lanyard|fabcon lanyard|conference lanyard|badge to ferryman|ferry man|gateway|boatman|the ferryman` [needs has:lanyard] — 'FabCon?' The Ferryman almost smiles. 'I was there. In spirit. My credentials had expired.' He hands it back, carefully, like it might still get him into a session.  #lake.give-lanyard
- > `give timetable|ferry timetable|schedule|ferry schedule to ferryman|ferry man|gateway|boatman|the ferryman` [needs has:timetable] — The Ferryman reads the timetable. 'Eight departures a day. On a Pro license.' He laughs until his lamp flickers, then stops, because it did not flicker ONLINE.  #lake.give-timetable
- > `give pebble|skipping stone|flat pebble|stone to ferryman|ferry man|gateway|boatman|the ferryman` [needs has:pebble] — He weighs the pebble in his palm and mouths: 'ONE.' He hands it back; he is very strict about the number.  #dock.give-pebble
- > `give license|card|license card|pro license|pro to ferryman|ferry man|gateway|boatman|the ferryman` [needs has:license] — He reads it and hands it back. 'PRO,' he mouths. 'EIGHT. A. DAY.' He looks at the lamp.  #dock.give-license
- > `give mug|coffee mug|cup to ferryman|ferry man|gateway|boatman|the ferryman` [needs has:mug] — He looks into the mug and hands it back. Empty. It is the first thing on this dock he has felt understood by.  #dock.give-mug
- > `give standard key|gateway key|standard mode key|key|standard|standard gateway key|other key to ferryman|ferry man|gateway|boatman|the ferryman` [needs has:standard key] — "Standard," he says, with feeling, and does not take it. "Shared. You keep it, and I've still got it."  #dock.give-standard-key
- > `give personal key|personal mode key|personal|personal gateway key|other key to ferryman|ferry man|gateway|boatman|the ferryman` [needs has:personal key] — "Personal," he says, and steps back from it, the way you would step back from a laptop about to close.  #dock.give-personal-key
- > `say credentials|creds|password|the password|gen1 credentials|my credentials` — You say 'credentials.' He mouths it back. Neither of you is holding any.  #dock.say-creds
- > `say online|on` — You say 'ONLINE' at the lamp. The lamp does not take dictation.  #dock.say-online
- > `say offline|off` — You say 'OFFLINE.' The Ferryman nods. It is the first time anyone has agreed with the lamp.  #dock.say-offline
- > `say dax|daxx` — You say 'DAX' on the dock. The Ferryman mouths it back. He doesn't know what it is; he mouths everything.  #dock.say-dax
- > `look lake|onelake|water|the lake|one lake` — The OneLake, from the dock. One lake wide. You could swim it; the Ferryman has watched people try. They're in Bronze now.  #dock.look-lake
- > `drink lake|onelake|water|the lake|one lake` — You drink from the OneLake, dock side. One lake. The Ferryman watches, OFFLINE, and does not stop you; stopping people takes a gateway.  #dock.drink-lake
- > `talk to lamp|status lamp|post|the lamp|status` — You address the lamp. OFFLINE, it says. It's the chattiest thing on the dock.  #dock.talk-lamp
- > `talk to boat|ferry|gateway boat|the boat` — You talk to the boat. It's named GATEWAY. It does not get the joke either.  #dock.talk-boat
- > `use timetable|ferry timetable|schedule|ferry schedule` — You consult the timetable. Next departure: OFFLINE. The one after: OFFLINE. It is the most reliable schedule in the realm.  #dock.use-timetable
- > `use boat|ferry|gateway boat` [needs ferry.online] — It is a boat. Boats are boarded, not used. `board`, and the Ferryman does the rest, weeping.  #lake.use-boat-online
- > `open boat|ferry|gateway boat` [needs ferry.online] — It is a boat. Boats are boarded, not opened. `board`, and the Ferryman does the rest, weeping.  #lake.open-boat-online
- > `use lamp|status lamp|post` [needs ferry.online] — You tap the lamp. ONLINE. It stays ONLINE. You tap it again to be sure, and the Ferryman flinches.  #lake.use-lamp-online
- > `open lamp|status lamp|post` [needs ferry.online] — It's a lamp, not a dialog.  #dock.open-lamp-online
### Phrases (this room / region)
- ~ `/^(throw|toss|chuck|skim|fling) (the )?(pebble|stone|skipping stone|flat pebble|rock|flat rock)( (in|into|at|to|on) (the )?(lake|onelake|water|marsh|it|him|ferryman|ferry man|boatman|boat|lamp|bronze|silver|gold))?$/` — [dynamic then]  #dock.throw-pebble
- ~ `/^(sit|get|hop|settle) (in|into|on|onto|aboard|down in)( the)? (boat|ferry|gateway)$/` — You sit in the boat. It does not move. Neither does the Ferryman. You are all OFFLINE together, which is almost company.  #lake.sit-boat
- ~ `/^((row|paddle)( the)? (boat|ferry|gateway)|row|paddle)$/` — You row. The boat's tied to the dock, the dock's tied to the lamp, and the lamp says no.  #dock.row-boat
- ~ `/^(untie|unmoor|launch|cast off)( the)? (boat|ferry|gateway)$/` — The boat comes untied. It stays. A rope was never what was holding it.  #dock.untie-boat
- ~ `/^(hum|whistle)( (the )?(boarding call|tune|along))?$/` — You hum. He mouths along. He doesn't know the tune; he mouths everything.  #dock.hum
- ~ `/^(cry|weep|sob)( with (the )?ferryman)?$/` — You weep. He weeps. The lamp stays red; it doesn't do sympathy, it does status.  #dock.weep
- ~ `/^(open|unlock|use|push|pull|cross|climb|lower|raise|enter|knock( on)?|break|kick( down)?|go through|walk (across|over|through)|try)( on| at| against| over| across| through| into| onto| up| down)?( the| a| that| this)? (boat|ferry|ferryman|ferry man|boatman|gateway|lamp|status lamp|post|lake|onelake)$/` — [dynamic then]  #gate.dock
### Items
- lamp [status lamp, status, post] — look: The lamp reads OFFLINE in a sad, blinking red.
    get: The lamp is the Ferryman's. It is the only thing he has left.
    blurb: The Ferryman's status lamp. It was the only thing he had left, and you knew that when you took it.
- boat [ferry, gateway boat, the gateway] — look: A flat-bottomed boat with GATEWAY painted on the side in a font that was fashionable in 2017.
    get: You cannot take the boat. You can board it. Try that.
    blurb: The Gateway boat, GATEWAY painted on the side in a 2017 font. You cannot take the boat, and yet here it is.
- timetable [ferry timetable, schedule, ferry schedule] (gettable) — look: FERRY TIMETABLE. Departures: 8 per day (Pro), 48 per day (Premium). Every departure this year has been crossed out and replaced with OFFLINE. Sponsored by Refreshr™.
    again: You already peeled the timetable off the post at the dock, right under the Ferryman's lamp. He watched you do it. Every departure was OFFLINE then, too.
    blurb: FERRY TIMETABLE. Eight departures a day on Pro. Every one of them crossed out and replaced with OFFLINE.
### NPCs
- the Ferryman [ferryman, ferry man, gateway, boatman, the ferryman]
    talk 1: The Ferryman's lamp reads OFFLINE. He mouths: 'Credentials expired.' A tear runs down his cheek in a way that suggests a scheduled refresh failed.
    talk 2: "Credentials," he mouths again, slower, the way you mouth a word to someone across a very wide lake. "Expired."
    talk 3: "Gen1 credentials. The Mill has them. West to the shore, north to the square, north again. Talk to the Miller, or open his chest; he stopped caring in 2019."
    talk 4: Still no creds, Mister Star Schema. The Mill. West, north, north.
    ask about pickles: He mouths: 'CREDENTIALS. EXPIRED.' He mouthed it at his own wedding.
    brush-off: He mouths: 'CREDENTIALS. EXPIRED.' He mouthed it at his own wedding.
    knows: credentials, creds, credential, password, gateway, lamp, status, offline, online, boat, ferry, isle, island, mill, miller, key, keys, standard, personal, board, boarding, refresh, tear

## Isle of Gateway (lake.island, lake)
where: The Isle of Gateway. Two keys, one plinth, and a decision you'll be explaining in a postmortem.
Exits: w
flask: Get the standard key. The personal one works for one person with an open laptop. Choose like a grown-up.
### Rules
- > `get standard key|gateway key|standard mode key|key|standard|standard gateway key|other key` (+20 gives standard key) [needs !taken.standard key] — You take the STANDARD key. It is heavy in the way things are heavy when many people depend on them.  #lake.key
- > `get personal key|personal mode key|personal|personal gateway key|other key` (gives personal key) [needs !taken.personal key] — You take the PERSONAL key. It only works for you, and only while your laptop is open. You feel a strong urge to close your laptop.  #lake.personal
- > `get keys|both keys|both|two keys|the keys|both of them` — One at a time. CHOOSE, not COLLECT.  #island.get-keys
- > `board` (→ lake.dock) — You ride the GATEWAY back. The Ferryman hums. It is the first time anyone has heard him hum.  #lake.board-back
- > `read plaque|plinth|sign` — CHOOSE. THEN LIVE WITH IT. — The Gateway Council, 2018  #lake.read-plaque
- > `use personal key|personal mode key|personal|personal gateway key|other key on plinth|stone plinth|stand|hollow` [needs has:personal key] — It fits. It is yours alone. Nobody else can ever use it. That is the problem.  #lake.personal-plinth
- > `use standard key|gateway key|standard mode key|key|standard|standard gateway key|other key on plinth|stone plinth|stand|hollow` [needs has:standard key] — It fits. It fits everyone's hand, which is the point, and also why it is cold.  #island.standard-plinth
- > `use standard key|gateway key|standard mode key|key|standard|standard gateway key|other key|personal key|personal mode key|personal|personal gateway key|other key on plaque|inscription|sign` — The plaque doesn't take keys. It takes decisions, and it has yours on file.  #island.use-key-plaque
- > `use plinth|stone plinth|stand|hollow|hollows` — You put your hand in the STANDARD hollow. It fits. Everyone fits. That is what shared means. The PERSONAL hollow has no capacitude for that.  #lake.island-plinth
- > `open plinth|stone plinth|stand|hollow|hollows` — The hollows are as open as they get.  #island.open-plinth
- > `open plaque|inscription|sign` — A plaque doesn't open. It's read, then lived with.  #island.open-plaque
- > `talk to ferryman|ferry man|gateway|boatman|the ferryman` — He hums from the boat. He's not getting out; islands are for choosing, not for ferrymen.  #island.talk-ferryman
- > `say standard|standard mode|standard key` — You say 'standard.' Good choice. Saying it isn't taking it, and the plinth has heard a lot of good choices.  #island.say-standard
- > `say personal|personal mode|personal key` — You say 'personal.' The plinth has heard that one too, usually from people with one laptop and no friends.  #island.say-personal
- > `look boat|ferry|gateway boat|the gateway|gateway|the boat` — The GATEWAY, tied to the island. The Ferryman's in it, humming, ready to take you and exactly one decision back.  #island.look-boat
- > `look ferryman|ferry man|boatman|the ferryman` — He's in the boat, humming. He has the posture of a man whose credentials work. It's new on him.  #island.look-ferryman
- > `look island|isle|the island|the isle|isle of gateway|ground` — An island with a plinth, two keys and a plaque. Small. A gateway doesn't need room; it needs one machine that stays on.  #island.look-island
- > `look keys|both keys|the keys|two keys|both` — Two keys: PERSONAL and STANDARD. One of them is a decision, and the other is a mistake with a lanyard.  #island.look-keys
- > `drink lake|onelake|water|the lake|one lake` — You drink from the OneLake, island side. Same lake. Same one. You taste the workspace you forgot you owned.  #island.drink
### Phrases (this room / region)
- ~ `/^(sit|get|hop|settle) (in|into|on|onto|aboard|down in)( the)? (boat|ferry|gateway)$/` — You sit in the boat. The Ferryman hums and waits. Sitting isn't boarding here either.  #island.sit-boat
### Items
- personal key [personal mode key, personal, personal gateway key] (gettable) — look: A Personal Mode gateway key. It only works for you, and only while your laptop is open.
    again: You already took the personal key. It was a mistake then. It is a mistake you are now holding.
    blurb: A Personal Mode key. Works for you, alone, while your laptop is open. Your laptop is never open.
- standard key [gateway key, standard mode key, key, standard, standard gateway key] (gettable) — look: A Standard Mode gateway key. Heavy, cold, enterprise-grade.
    again: We did the island already. You lifted the STANDARD key off the plinth while the Ferryman hummed, and you have been patting your pocket ever since.
    blurb: The Gateway Key, Standard Mode. Doesn't open the gateway to your manager's calendar, which you'd trade it for.
- plinth [plinth, stone plinth, stand] — look: A stone plinth with two hollows shaped like keys. One says PERSONAL. One says STANDARD. Only one hollow has ever been used.
    get: It's a plinth. It is the island.
    blurb: A stone plinth with two hollows shaped like keys. You chose, then you took the thing you chose from, and the Council did not plan for that.
- plaque [plaque, inscription, sign] — look: CHOOSE. THEN LIVE WITH IT. — the Gateway Council. Below, smaller: 'Personal mode cannot be shared. Standard mode cannot be unshared.'
    get: It is bolted to the plinth.
    blurb: CHOOSE. THEN LIVE WITH IT. You took the plaque, which was the third option, and nobody on the Gateway Council has a line for it.

## Bronze Marsh (swamp.bronze, swamp)
where: The Bronze Marsh. Everything here is a string, including your plan.
Exits: n, s
flask: Do not drink anything. Go south until things have names.
### Rules
- > `drink capacityade|capacity ade|bottle|sports drink|ade|drink|the ade|label` (☠) [needs has:capacityade] — Electrolytes. Autoscale. Your heart hits 64 CUs and the bill arrives before you do. The label said not on a capacity. Everything in this realm is on a capacity. That is what the dragon is FOR.  #bronze.drink-ade
- > `drink capacityade|capacity ade|bottle|sports drink|ade|drink|the ade|label` — There is no CapacityAde in the marsh. There is marsh water, which is worse, and free.  #bronze.no-ade
- > `drink` (☠) — You drink raw data. Nothing is typed. Everything is a string. You are a string. Your mom told you never to drink from the Bronze layer. And NOW look.  #death.bronze
- > `get csv|file|column1` — You grab the CSV. It has 3 columns and 9 million rows, and every one of them is a header. You let it go.  #swamp.get-csv
- > `use name tag|tag|nametag|hello tag on csv|file|column1|column2|column3|the csv` [needs has:name tag] — You stick 'Column3' on the CSV. It already had a Column3. Power Query notices and helpfully renames yours Column3_1. You have made it worse, with a suffix.  #swamp.tag-csv
- > `use columns|column|column1|the columns` — You name Column1. It becomes Column1 (2). The marsh applauds, unstructured.  #swamp.name-columns
- > `open csv|file|column1|column2|column3|the csv` — You open the CSV. Nine million rows, all headers. It opens in Excel by default; everything does.  #bronze.open-csv
- > `talk to csv|file|column1|column2|column3|the csv` — 'Column1,' it says. 'Column2. Column3.' It's introducing itself. It only knows the one way.  #bronze.talk-csv
- > `talk to marsh|water|the marsh|bronze|raw data|data|swamp` — You address the marsh. It answers in strings. All of them say 'null', and none of them are null.  #bronze.talk-marsh
- > `use marsh|water|the marsh|raw data|data|bronze|swamp` — You use raw data. Directly. In a report. Somewhere, a Gold layer weeps.  #bronze.use-marsh
- > `open marsh|water|the marsh|bronze|swamp|raw data` — Open? Nothing in Bronze is closed; that's the problem.  #bronze.open-marsh
- > `say dax|daxx` — You say 'DAX' in the Bronze. Nothing here has a type, so nothing can be DAXxed. The strings don't look up.  #bronze.say-dax
### Phrases (this room / region)
- ~ `/^(name|rename|type)( the)? columns?$/` — You name Column1. It becomes Column1 (2). The marsh applauds, unstructured.  #swamp.name-columns-words
- ~ `/^(throw|toss|chuck|skim|fling) (the )?(pebble|stone|skipping stone|flat pebble|rock|flat rock)( (in|into|at|to|on) (the )?(lake|onelake|water|marsh|it|him|ferryman|ferry man|boatman|boat|lamp|bronze|silver|gold))?$/` — [dynamic then]  #bronze.throw-pebble
- ~ `/^(smell|sniff)( the| this| that)? (marsh|water|air|it|bronze|silver|gold|swamp|data|here)$/` — You sniff. Raw data. It smells like a CSV that has been in a hot car.  #bronze.smell
- ~ `/^(swim|dive|bathe|wade)\b/` — You wade into the Bronze. Everything you're wearing is a string now, including the license.  #bronze.swim
- ~ `/^(dig|excavate|burrow)\b/` — You dig. Under the strings: more strings. Under those, a header row. It's headers all the way down.  #bronze.dig
### Items
- water [marsh, marsh water, bronze water, raw data, data] — look: Bronze water. Untyped, unvalidated, unloved.
    get: It's raw data. It runs through your fingers as strings.
    blurb: Bronze water. Untyped, unvalidated, and soaking through everything else you own.
- CSV [csv, column1, column2, column3, file, the csv] — look: A CSV drifting face-up. Column1, Column2, Column3. Somewhere, someone is about to name them all in Power Query, by hand, for the fourth time.
    get: It slips through your fingers, delimited.
    blurb: A CSV: Column1, Column2, Column3. It is still delimited, and it is still leaking through the lining.
- stress ball [ball, cube, stress cube, olap cube] (gettable) — look: A stress ball shaped like a cube. 'OLAP' is printed on one face. The other five faces are dimensions nobody asked for.
    again: You already have the cube. You could squeeze it. You have squeezed it. Your forearm is a star schema.
    blurb: A stress ball shaped like an OLAP cube. Five of its six faces are dimensions nobody asked for.

## Silver Marsh (swamp.silver, swamp)
where: The Silver Marsh. Things have names now. Yours is still 'peasant'.
Exits: n, s
flask: Go south to the Gold Marsh. Get the signpost there. It weighs nothing; it is just a pointer.
### Rules
- > `drink capacityade|capacity ade|bottle|sports drink|ade|drink|the ade|label` (☠) [needs has:capacityade] — Electrolytes. Autoscale. Your heart hits 64 CUs and the bill arrives before you do. The label said not on a capacity. Everything in this realm is on a capacity. That is what the dragon is FOR.  #silver.drink-ade
- > `drink capacityade|capacity ade|bottle|sports drink|ade|drink|the ade|label` — There is no CapacityAde in the marsh. There is marsh water, which is worse, and free.  #silver.no-ade
- > `drink` — You drink Silver water. It has been deduplicated. You feel slightly less redundant. You feel refreshered.  #swamp.drink-silver
- > `read column names|columns|column|names|column name|headers` — Column1 became CustomerName. Column2 became Customer_Name. Column3 is still Column3. Its name tag is floating nearby, unclaimed.  #swamp.read-columns
- > `look column names|columns|column|names|column name|headers` — Column1 became CustomerName. Column2 became Customer_Name. Column3 is still Column3. Its name tag is floating nearby, unclaimed.  #silver.look-columns
- > `use log|delta log|_delta_log|transaction log|logs|json|the log` — You open a transaction log. It knows exactly what happened. It knows what you did on turn 0, too, and filed it next to your Hotmail password.  #swamp.use-log
- > `open log|delta log|_delta_log|transaction log|logs|json|the log` — You open a transaction log. It knows exactly what happened. It knows what you did on turn 0, too, and filed it next to your Hotmail password.  #silver.open-log
- > `read log|delta log|_delta_log|transaction log|logs|json|the log` — You read one. {"add": "you"}. Then {"remove": "you"}. It has done this with other Report Builders, and it will again.  #silver.read-log
- > `talk to log|delta log|_delta_log|transaction log|logs|json|the log` — You talk to the log. It writes that down. It writes everything down; that's the whole personality.  #silver.talk-log
- > `use name tag|tag|nametag|hello tag on log|delta log|_delta_log|transaction log|logs|json|the log` [needs has:name tag] — You stick Column3 on the log. It logs it: add, Column3. It never takes it off; it just never counts it.  #silver.tag-log
- > `use name tag|tag|nametag|hello tag on water|marsh|silver|the marsh|swamp` [needs has:name tag] — You drop the tag in the Silver. It comes back: Column3, once. There was only ever one.  #silver.tag-water
- > `look water|marsh|the marsh|silver|silver water|the water|swamp` — Silver water. Cleaner, and it knows it. It reflects you once; the duplicates were removed.  #silver.look-water
- > `get water|marsh|the marsh|silver|silver water|the water|swamp` — Deduplicated. There's one of it, and it's staying.  #silver.get-water
- > `open water|marsh|the marsh|silver|swamp` — It's a marsh, not a folder. Well. It's also a folder.  #silver.open-marsh
- > `say dax|daxx` — You say 'DAX' in the Silver. The transaction logs write it down. Verbatim. With a timestamp.  #silver.say-dax
### Phrases (this room / region)
- ~ `/^(throw|toss|chuck|skim|fling) (the )?(pebble|stone|skipping stone|flat pebble|rock|flat rock)( (in|into|at|to|on) (the )?(lake|onelake|water|marsh|it|him|ferryman|ferry man|boatman|boat|lamp|bronze|silver|gold))?$/` — [dynamic then]  #silver.throw-pebble
- ~ `/^(smell|sniff)( the| this| that)? (marsh|water|air|it|bronze|silver|gold|swamp|data|here)$/` — You sniff. Cleaner. Judgmental, like a code review.  #silver.smell
- ~ `/^(swim|dive|bathe|wade)\b/` — You wade into the Silver. You come out deduplicated. There was only ever one of you, but now it's enforced.  #silver.swim
- ~ `/^(dig|excavate|burrow)\b/` — You dig. A _delta_log. It logs the dig.  #silver.dig
### Items
- log [delta log, _delta_log, transaction log, logs, json] — look: Transaction logs float past in the Silver Marsh. Each one is a JSON file that knows exactly what happened.
    get: You lift the log. It's just a _delta_log folder. You put it back, exactly where it was, which is the whole point.
    blurb: A _delta_log folder. It knows exactly what happened, including this, and it will never let it go.
- name tag [tag, nametag, hello tag] (gettable) — look: A name tag, peeled off a column somewhere upstream: HELLO MY NAME IS Column3. It has been renamed so many times the ink gave up.
    again: You already fished the name tag out of the Silver Marsh while the transaction logs drifted past and judged you. It still says Column3.
    blurb: HELLO MY NAME IS Column3. Peeled off a column upstream; renamed so many times the ink gave up.

## Gold Marsh (swamp.gold, swamp)
where: The Gold Marsh. Clean, typed, modeled. You lower the tone just by standing here.
Exits: n, e
flask: Get the signpost. It is a shortcut. It weighs nothing. It is just a pointer.
### Rules
- > `get shortcut|signpost|sign|onelake shortcut|pointer` (+10 gives shortcut) [needs !taken.shortcut] — You pick up the Shortcut. It weighs nothing. It's just a pointer.  #swamp.shortcut
- > `use shortcut|signpost|sign|onelake shortcut|pointer on marsh|gold marsh|water|itself|here|the marsh` [needs has:shortcut] — The shortcut points at itself. The marsh briefly contains the marsh. No data was moved. Nothing was learned.  #swamp.shortcut-marsh
- > `drink capacityade|capacity ade|bottle|sports drink|ade|drink|the ade|label` (☠) [needs has:capacityade] — Electrolytes. Autoscale. Your heart hits 64 CUs and the bill arrives before you do. The label said not on a capacity. Everything in this realm is on a capacity. That is what the dragon is FOR.  #gold.drink-ade
- > `drink capacityade|capacity ade|bottle|sports drink|ade|drink|the ade|label` — There is no CapacityAde in the marsh. There is marsh water, which is worse, and free.  #gold.no-ade
- > `drink` — You drink Gold water. It tastes like a well-named measure.  #swamp.drink-gold
- > `read shortcut|signpost|sign|onelake shortcut|pointer` — SHORTCUT, it says, pointing everywhere at once. Below: 'no data was moved.' Below that, smaller: 'no data was moved.'  #swamp.read-signpost
- > `talk to shortcut|signpost|sign|onelake shortcut|pointer` — You talk to the signpost. It points somewhere else. It's not rude; it's a pointer.  #gold.talk-signpost
- > `open shortcut|signpost|sign|onelake shortcut|pointer` — It's a pointer. Nothing is inside; that's what a pointer is.  #gold.open-signpost
- > `use shortcut|signpost|sign|onelake shortcut|pointer` [needs !taken.shortcut, not:shortcut] — You lean on the signpost. It points at you, briefly. Pick it up; it weighs nothing.  #gold.use-signpost-standing
- > `look water|marsh|the marsh|gold|gold water|the water|swamp` — Gold water. Typed, named, modeled. It reflects you back with a business name: Peasant (Active).  #gold.look-water
- > `get water|marsh|the marsh|gold|gold water|the water|swamp` — You can't take Gold water. It's modeled. It belongs to the business now.  #gold.get-water
- > `open water|marsh|the marsh|gold|swamp` — Open the Gold? You'd need a business reason, and a name for it.  #gold.open-marsh
- > `look names|business names|measures|measure|types|data types|the names|business name` — Everything has a business name. Three of them are Net Sales. The fight is scheduled for Q3.  #gold.look-names
- > `look path|monastery|east|the path|trail|the monastery` — East, a path climbs to a monastery. You can hear a session starting from here. You'll hear it for a while.  #gold.look-path
- > `say dax|daxx` — You say 'DAX' in the Gold. The measures look up. It is the first time anything in a marsh has looked up.  #gold.say-dax
### Phrases (this room / region)
- ~ `/^(throw|toss|chuck|skim|fling) (the )?(pebble|stone|skipping stone|flat pebble|rock|flat rock)( (in|into|at|to|on) (the )?(lake|onelake|water|marsh|it|him|ferryman|ferry man|boatman|boat|lamp|bronze|silver|gold))?$/` — [dynamic then]  #gold.throw-pebble
- ~ `/^(smell|sniff)( the| this| that)? (marsh|water|air|it|bronze|silver|gold|swamp|data|here)$/` — You sniff. It smells like a well-named measure. You didn't know they had a smell. They do; it's expensive.  #gold.smell
- ~ `/^(swim|dive|bathe|wade)\b/` — You wade into the Gold. You come out with a business name and a data type. Neither is flattering.  #gold.swim
- ~ `/^(dig|excavate|burrow)\b/` — You dig. You hit a shortcut. It points at the hole.  #gold.dig
### Items
- shortcut [signpost, sign, onelake shortcut, pointer] (gettable) — look: A OneLake Shortcut. It weighs nothing. It's just a pointer.
    again: You picked up the Shortcut already. It weighed nothing then and it weighs nothing now. That is its whole deal.
    blurb: A OneLake Shortcut. Points at data you don't own and never will. The closest thing you have to a savings account.

## The Lake House (lake.house, lake)
where: The Lake House. A house. On a lake. You keep saying it like it'll start meaning something.
Exits: e
flask: This room is a joke. The joke is the whole room. The shore is east.
### Rules
- > `open door|house|front door` — Inside: files on the left, tables on the right, and a shortcut to another house across the lake. You back out slowly.  #lake.house.enter
- > `go in` — Inside: files on the left, tables on the right, and a shortcut to another house across the lake. You back out slowly.  #lake.house.in
- > `use chair|deck chair|sit|porch|deck` — You sit on the deck. The lake refreshes. It is lovely. You are billed. It is the Windows XP hill with a deck chair on it.  #lake.house.sit
- > `use knock|door` — Nobody answers. A Spark session starts inside, out of politeness.  #lake.house.knock
- > `use buy|buy house|house|purchase` — It is not for sale. It is for storage. Very different, the realtor insists, without making eye contact.  #lake.house.buy
- > `use fish|fishing|rod` — You fish. You catch a Delta log. You put it back; it was not fully committed.  #lake.house.fish
- > `open mailbox|mail box|post box` — One new CSV. It has been in there since bronze. You leave it; the mailbox is also a Lakehouse, legally.  #lake.house.mailbox
- > `use mailbox|mail box|post box|flag` — You put the flag up. Outgoing: nothing. The mailbox lowers it again; it knows you.  #house.use-mailbox
- > `get mail|csv|letter|post|the mail|new mail|the csv` — You reach in. It's the same CSV. You know it. It knows you. You leave it.  #house.get-mail
- > `talk to mailbox|mail box|post box` — '1 new,' says the mailbox. It is the only thing it has ever said, and it has never been wrong.  #house.talk-mailbox
- > `get house|lake house|lakehouse|the house` — It's a house. On a lake. No.  #house.get-house
- > `use sign|lakehouse sign|porch sign` [needs not:shortcut] — The sign's not a control. It's a compromise, in two fonts.  #house.use-sign
- > `open sign|lakehouse sign|porch sign` — It's two fonts. Neither opens.  #house.open-sign
- > `look house|lake house|the house|lakehouse|the lake house` — A house. On a lake. You're looking right at it. It doesn't get more house, or more lake.  #house.look-house
- > `look lake|onelake|water|the lake|one lake` — The lake. The house is on it. That's the pitch. That's the whole pitch.  #house.look-lake
- > `look porch|deck|veranda|the porch|the deck` — A porch. It faces the lake. So do the chair, the mailbox and the sign; nothing here faces the files.  #house.look-porch
- > `say lakehouse|lake house|the lakehouse` — You say 'Lakehouse.' The sign lights up, half in serif. Marketing feels it from here.  #house.say-lakehouse
- > `drink lake|onelake|water|the lake|one lake` — You drink the lake. It tastes like a house. That shouldn't be possible, and marketing agrees.  #house.drink
### Phrases (this room / region)
- ~ `/^(sit|sit down|sit in chair|sit on porch|rest|relax|chill|lounge|sunbathe|recline)$/` — You sit on the deck. The lake refreshes. It is lovely. You are billed. It is the Windows XP hill with a deck chair on it.  #lake.house.sit-egg
- ~ `/^(swim|dive|jump in|wade)\b/` — You wade in. It is a lake. It is also, somehow, a house. You are now inside a house while swimming. You get out.  #lake.house.swim-egg
- ~ `/^knock\b/` — Nobody answers. A Spark session starts inside, out of politeness.  #lake.house.knock-egg
- ~ `/^(buy|purchase|make an offer)\b/` — It is not for sale. It is for storage. Very different, the realtor insists, without making eye contact.  #lake.house.buy-egg
- ~ `/^(fish|go fishing|cast)\b/` — You fish. You catch a Delta log. You put it back; it was not fully committed.  #lake.house.fish-egg
### Items
- sign [lakehouse sign] — look: LAKE, in serif. HOUSE, in sans. They were added at different times, by different teams.
    get: It is bolted above the porch. Every deed in the realm is bolted to something.
    blurb: LAKE, in serif. HOUSE, in sans, added later by a different team, and you have both halves.
- chair [deck chair] — look: A deck chair. Adirondack. Lakehouse-adjacent.
    get: The chair stays on the deck. Sit in it instead.
    blurb: An Adirondack deck chair, Lakehouse-adjacent. Nobody sat in it while it was on the deck, and nobody is sitting in it now.
- mailbox [mail box, post box] — look: Mailbox: 1 new. It is a CSV. It has been in the mailbox since bronze.
    get: The mailbox is on a post. The post is not going anywhere. Neither, apparently, is the mail.
    blurb: A mailbox: 1 new. It is the same CSV, and it has been in there since bronze.
- door [front door] — look: A door. Behind it, files and tables in the same building. Nobody thought that was strange until the invoice.
    get: It is a door. It stays on the house, which is the whole point of a door.
    blurb: The Lake House door. Files and tables are in the same building behind it, and the invoice is on its way.

## Monastery Gate (monastery.gate, monastery)
where: The Monastery Gate. A session is starting. It was starting when you got here and it'll be starting at your funeral.
Exits: w, s, n
flask: The session is starting. Wait. Then wait again. Then once more — three waits opens the gate.
### Rules
- > `talk to monk|gatekeeper|gatekeeper monk about session|the session|spark session|progress bar|bar` — The monk points at the bar. Then at the sky. Then at the bar. It is a lineage view.  #monastery.monk-session
- > `wait` (+10) [needs gate.waiting=2, !gate.open] — SESSION STARTED. It took four minutes, as is tradition. The gate swings open.  #monastery.wait
- > `wait` [needs gate.waiting=1] — The bar creeps to 67%. The monk hums.  #monastery.wait-2
- > `wait` [needs !gate.open, !gate.waiting] — The bar creeps to 33%. The monk nods approvingly at your patience.  #monastery.wait-1
- > `wait` [needs gate.open, !gate.idled] — You wait some more. The session's up and billing by the second. You could do this all day!  #monastery.wait-open
- > `wait` [needs gate.open] — The drama grips you, but the bar hasn't moved. It's at 100%. The meter, however, has.  #monastery.wait-open-again
- > `open gate|door|monastery gate` [needs gate.open] — Yeah, totally! Except it's already open, you moron. Try: north.  #monastery.open-gate-open
- > `go n` [needs !gate.open] — The gate is closed. SESSION STARTING…  #monastery.go-north-closed
- > `look back gate|keep gate|keep's back gate|keeps back gate|south gate|the back gate|xmla endpoint|endpoint` — The Keep's back gate, south: the XMLA endpoint, Read Write. Through it, the Model View, Sir Cardinality, and one eyebrow, already raised.  #monastery.look-back-gate
- > `go s` [needs ts.xmla=false] — The back gate is an XMLA endpoint. Your capacity admin set it to Off. From this side it is a wall, and the monks are using it to lean on.  #monastery.south-xmla
- > `use` [needs !gate.open] — You knock. The gate says: Session starting. Please wait. It has always said that. Knocking does not count as waiting.  #monastery.knock
- > `use` [needs gate.open] — You knock on an open gate. The monk watches you do it. He has seen a lot of retry policies. Yours is the saddest.  #monastery.knock-open
- > `give pamphlet|leaflet|brochure|spark pamphlet to monk|gatekeeper|gatekeeper monk` [needs has:pamphlet] — The monk glances at the pamphlet and hands it back. He already has one. He wrote it. Chapter 3 is still starting.  #monastery.give-pamphlet
- > `attack monk|gatekeeper|gatekeeper monk` — You shove the monk. He does not move. He has been standing here since Runtime 1.1.  #monastery.attack-monk
### Phrases (this room / region)
- ~ `/^count( to (three|3|ten|10))?$/` — One. Two. The session hears you counting and restarts.  #monastery.count
- ~ `/^(ring|open|unlock|use|push|pull|cross|climb|lower|raise|enter|knock( on)?|break|kick( down)?|go through|walk (across|over|through)|try)( on| at| against| over| across| through| into| onto| up| down)?( the| a| that| this)? (gate|door|bell|monastery gate|lock)$/` — [dynamic then]  #gate.monastery
- ~ `/^(open|unlock|use|push|pull|cross|climb|lower|raise|enter|knock( on)?|break|kick( down)?|go through|walk (across|over|through)|try)( on| at| against| over| across| through| into| onto| up| down)?( the| a| that| this)? (session|spark session|progress bar|bar|stone progress bar|monk|gatekeeper|gatekeeper monk)$/` — [dynamic then]  #gate.session
### Items
- gate [monastery gate, progress bar, bar] — look: A stone gate with a stone progress bar. SESSION STARTING… 0% Not enough capacitude to hurry it.
    get: The gate is attached to the monastery, the monastery to the mountain, the mountain to a session that has not started.
    blurb: The Monastery gate, stone progress bar included. SESSION STARTING, wherever you carry it, at the same percent.
- pamphlet [leaflet, brochure, spark pamphlet] (gettable) — look: SPARK: A BEGINNER'S GUIDE. Chapter 1: Waiting. Chapter 2: Waiting, Continued. Chapter 3 has not started yet.
    again: You already took the pamphlet off the gatekeeper monk while he pointed at the progress bar. Chapter 3 still hasn't started.
    blurb: SPARK: A BEGINNER'S GUIDE. Chapter 1: Waiting. Chapter 3 has not started yet.
- bricks [brick, wall, bricked gate, back gate, gate, xmla, endpoint, xmla endpoint, chalk] — look: Bricks, where the Keep's back gate was. XMLA is chalked on them, and under it, smaller: OFF. Three monks lean on it. It is the most support the endpoint has had.
    get: You pull at a brick. It is Read Only.
    blurb: Bricks, from where the Model View's back gate was. XMLA is off, and you are carrying the wall.
### NPCs
- the gatekeeper monk [monk, gatekeeper, gatekeeper monk]
    talk 1: The monk points at the progress bar without a word. 'Session starting…'
    talk 2: The monk points at the progress bar again, a little harder. It is still starting.
    talk 3: "Wait," the monk says, quietly, so the session doesn't hear. "Three times. Type it. It counts your patience; nothing else counts."
    talk 4: Still not waiting, Mister Star Schema. The bar noticed.
    ask about pickles: The monk points at the progress bar. It is his answer to everything, and it is always the same percent.
    brush-off: The monk points at the progress bar. It is his answer to everything, and it is always the same percent.
    knows: session, spark, spark session, progress, bar, progress bar, wait, waiting, gate, door, abbot, cloister, monastery, vow, silence

## Cloister (monastery.cloister, monastery)
where: The Cloister. Monks in a circle, chanting. You're in the loop now. There's no break statement.
Exits: s, e, w, u
flask: Go west to the Library. Give the Librarian your license. It is, technically, a card.
### Rules
- > `talk to abbot|father abbot|father|the abbot` [needs curse.blank] — The Abbot looks straight through you and blesses the wall behind you. Whatever he has for you, he keeps until there is a you.  #monastery.abbot-blank
- > `talk to abbot|father abbot|father|the abbot about pandas|brother pandas|brother` — 'Brother Pandas,' says the Abbot, 'was a Data Scientist. Then he was a Data Engineer. Then he was a Pandas. It happens gradually and then all at once.'  #monastery.abbot-pandas
- > `talk to abbot|father abbot|father|the abbot` (+15 gives hoodie) [needs notebook.fixed, !has.hoodie] — 'Brother Pandas' notebook runs,' says the Abbot, 'and the Lakehouse is free of pandas. Kneel.' He drapes the Hoodie of Spark over your shoulders. It is warm, and slightly too big, as is tradition.  #monastery.hoodie
- > `talk to abbot|father abbot|father|the abbot` (+10 bonus) [needs gate.open, !gov.errand, ts.publishToWeb=false] — "Brother Pandas has put pandas in the Lakehouse again. Fix his notebook and the Hoodie of Spark is yours." "Also. Someone published the prophecy to web. The whole internet can read it. Go up to the Sacristy and turn it off. I would, but I am a monk, not an admin. Those are different vows." "…and it is already off. You are ahead of me. +10."  #monastery.errand-done
- > `talk to abbot|father abbot|father|the abbot` [needs gate.open, !gov.errand] — "Brother Pandas has put pandas in the Lakehouse again. Fix his notebook and the Hoodie of Spark is yours." "Also. Someone published the prophecy to web. The whole internet can read it. Go up to the Sacristy and turn it off. I would, but I am a monk, not an admin. Those are different vows."  #monastery.errand
- > `talk to abbot|father abbot|father|the abbot about <.>` — "Brother Pandas has put pandas in the Lakehouse again. Fix his notebook and the Hoodie of Spark is yours."  #monastery.ask-abbot
- > `give kpi|laminated kpi to abbot|father abbot|father|the abbot` [needs has:kpi] — The Abbot looks at (Blank) for a long time. 'Yes,' he says. 'This is the realm.' He hands it back. Some things are meant to be carried.  #monastery.give-kpi-abbot
- > `use abbot|floor` — You kneel. The Abbot waits for you to say something. You have nothing. You get up.  #monastery.kneel
### Phrases (this room / region)
- ~ `/^((climb|take|go|walk|head|ascend|descend)( up| down)?( the)?( spiral)? (stairs?|staircase|steps)|(go |walk |head |climb )?(upstairs|downstairs)|(up|down)( the)? stairs?)$/` — [dynamic then]  #sacristy.stair-up
- ~ `/^(chant|join (the )?(monks|loop|circle)|spark dot read)$/` — You chant 'spark dot read' with the monks. You get the rhythm wrong. Three monks fall out of the loop. The Abbot restarts them.  #monastery.chant
- ~ `/^(open|unlock|use|push|pull|cross|climb|lower|raise|enter|knock( on)?|break|kick( down)?|go through|walk (across|over|through)|try)( on| at| against| over| across| through| into| onto| up| down)?( the| a| that| this)? (abbot|father abbot|father)$/` — [dynamic then]  #gate.abbot
### Items
- cloister floor [floor, flagstones, stones, circle] — look: Worn in a perfect circle by monks pacing 'spark dot read'. The centre stone has a burn mark shaped like a cluster starting. The monks have refreshered themselves into a circle.
    get: It's a floor.
    blurb: The cloister floor, worn in a circle by monks pacing 'spark dot read'. The monks are pacing on nothing now, and they have not noticed.
- laminated KPI [kpi, laminated kpi] (gettable) — look: A laminated KPI card. Target: 100%. Actual: (Blank). Somebody laminated (Blank). On purpose. To keep it.
    again: You already picked the KPI up off the cloister floor, from the middle of the circle the monks pace in. They stepped around you. It's still (Blank).
    blurb: A laminated KPI. Target: 100%. Actual: (Blank). Somebody laminated (Blank), on purpose, to keep it.
- stair [stairs, spiral stair, staircase, steps, steps down, steps up, spiral staircase] — look: A spiral stair, up to the Sacristy. Worn in the middle by admins who went up to fix one thing and came down having fixed several.
    get: The stair is attached to the Monastery at both ends. It stays.
    blurb: A spiral stair, both ends. Admins wore it in the middle going up to fix one thing, and you took the whole thing.
### NPCs
- the Abbot [abbot, father abbot, father]
    talk 1: "Brother Pandas has put pandas in the Lakehouse again. Fix his notebook and the Hoodie of Spark is yours."
    talk 2: "Pandas," the Abbot says, with the weariness of a man who has said it at every standup. "In the Lakehouse. Fix the notebook."
    talk 3: "The scroll is in the Library, west, in a case. Your license opens the case. Then east, east, use the scroll on the notebook. Then kneel."
    talk 4: "Still pandas, Mister Star Schema."
    ask about pickles: "Either we don't know anything about that, or you're real boring. The monks are voting."
    brush-off: "Either we don't know anything about that, or you're real boring. The monks are voting."
    knows: pandas, brother pandas, brother, notebook, hoodie, hood, hoodie of spark, spark, scroll, spark scroll, library, librarian, license, lakehouse, kneel, worthy, peaks, dragon, keep, notebooks

## Spark Session Chamber (monastery.spark, monastery)
where: The Spark Session Chamber. Warm, humming, billed. Like a hot tub with an invoice.
Exits: w
flask: Go west, then west again, to the Library. That cell needs Spark, not pandas, and the scroll about it is locked in a case.
### Rules
- > `talk to pandas|brother pandas|brother|monk pandas about spark|pyspark|the session|session|notebook` — 'Spark,' says Brother Pandas. 'It's not broken, it's deprecated.' 'It's broken.' 'Deprecated.' 'Broken.' (You see where this is going.)  #monastery.pandas-spark
- > `use scroll|spark scroll|pyspark scroll on notebook|cell|brother pandas notebook|pandas notebook|pandas' notebook` [needs !notebook.fixed, ts.fabricItems=false, has:scroll] — Notebook creation is disabled for your tenant. Brother Pandas creates a Power BI report instead. It has one card. It says 1.  #monastery.fix-disabled
- > `use scroll|spark scroll|pyspark scroll on notebook|cell|brother pandas notebook|pandas notebook|pandas' notebook` [needs !notebook.fixed, ts.workloads=false, has:scroll] — Notebook creation is disabled for your tenant. Brother Pandas creates a Power BI report instead. It has one card. It says 1. (Delegated. Also off.)  #monastery.fix-delegated
- > `give scroll|spark scroll|pyspark scroll to pandas|brother pandas|brother|monk pandas` [needs !notebook.fixed, ts.fabricItems=false, has:scroll] — Notebook creation is disabled for your tenant. Brother Pandas creates a Power BI report instead. It has one card. It says 1.  #monastery.fix-give-disabled
- > `give scroll|spark scroll|pyspark scroll to pandas|brother pandas|brother|monk pandas` [needs !notebook.fixed, ts.workloads=false, has:scroll] — Notebook creation is disabled for your tenant. Brother Pandas creates a Power BI report instead. It has one card. It says 1. (Delegated. Also off.)  #monastery.fix-give-delegated
- > `use scroll|spark scroll|pyspark scroll on notebook|cell|brother pandas notebook|pandas notebook|pandas' notebook` (+20) [needs !notebook.fixed, has:scroll] — You unroll the Spark Scroll over the notebook. pd.read_csv becomes spark.read.format('delta'). The cell finishes in four seconds. Brother Pandas weeps. The Lakehouse exhales. Eleven thousand pandas amble off toward the Silver Marsh.  #monastery.fix
- > `give scroll|spark scroll|pyspark scroll to pandas|brother pandas|brother|monk pandas` (+20) [needs !notebook.fixed, has:scroll] — Brother Pandas reads the scroll, sighs, and rewrites the cell: spark.read.format('delta'). It finishes in four seconds. He weeps. The Lakehouse exhales. Eleven thousand pandas amble off toward the Silver Marsh.  #monastery.fix-give
- > `use kpi|laminated kpi on notebook|cell|brother pandas notebook|pandas notebook|pandas' notebook` [needs has:kpi] — You hold the KPI up to the notebook. The notebook prints (Blank). Brother Pandas nods; he has seen worse. He has written worse.  #monastery.kpi-notebook
- > `give bamboo|bamboo shoot|shoot|lunch to pandas|brother pandas|brother|monk pandas` [needs has:bamboo] — Brother Pandas eats the bamboo. The cell is still running. Nothing is faster, but he is happier, and that is not nothing. It is close to nothing.  #monastery.give-bamboo
- > `use on notebook|cell|brother pandas notebook|pandas notebook|pandas' notebook` [needs !notebook.fixed] — You stare at the cell. You don't know Spark. The cell knows you don't know Spark.  #monastery.fix-noscroll
- > `use notebook|cell|brother pandas notebook|pandas notebook|pandas' notebook` [needs !notebook.fixed] — You click Run All. The cell was already running. Now it is running twice. Brother Pandas gives you a look.  #monastery.use-notebook
- > `use notebook|cell|brother pandas notebook|pandas notebook|pandas' notebook` — You click Run All. Four seconds. Brother Pandas mouths "four seconds" along with it.  #monastery.use-notebook-fixed
### Phrases (this room / region)
- ~ `/^(eat|bite|chew)( the)? (bamboo|shoot|lunch)$/` — You eat Brother Pandas' lunch. It is a dependency. Something downstream fails.  #monastery.eat-bamboo
- ~ `/^(open|unlock|use|push|pull|cross|climb|lower|raise|enter|knock( on)?|break|kick( down)?|go through|walk (across|over|through)|try)( on| at| against| over| across| through| into| onto| up| down)?( the| a| that| this)? (notebook|cell|session|spark session|pandas|brother pandas|brother|monk pandas|lakehouse)$/` — [dynamic then]  #gate.notebook
### Items
- notebook [brother pandas notebook, cell, pandas' notebook, pandas notebook] — look: Cell 1: df = pd.read_csv("onelake/gold/*.csv")   # works on my laptop   [running… 41 min]
    get: It's a shared notebook. Take it and Brother Pandas loses his session.
    blurb: Brother Pandas' notebook. It is a shared notebook, so his session is wherever you are now, which is not his laptop.
- bamboo [bamboo shoot, shoot, lunch] (gettable) — look: A bamboo shoot. Brother Pandas' lunch. He insists it is also a dependency.
    again: You already took his lunch. He has noticed. He has said nothing, which is worse.
    blurb: A bamboo shoot, Brother Pandas' lunch. He insists it is also a dependency.
### NPCs
- Brother Pandas [pandas, brother pandas, brother, monk pandas]
    talk 1: "It works on my laptop."
    talk 2: "It works on my laptop," Brother Pandas repeats. "My laptop is not here. That is the problem, or one of them."
    talk 3: "pd.read_csv. That's the cell. It wants spark.read. There's a scroll in the Library that says so. West, west."
    talk 4: "Still running, Mister Star Schema. Forty-one minutes. Forty-two."
    ask about pickles: "Does it work on my laptop? No? Then I don't know it either."
    brush-off: "Does it work on my laptop? No? Then I don't know it either."
    knows: laptop, notebook, pandas, spark, pyspark, scroll, spark scroll, cell, session, csv, read_csv, production, abbot, hoodie, lakehouse, delta, library, dataframe

## Library of Deprecated Notebooks (monastery.library, monastery)
where: The Library of Deprecated Notebooks. One of them is yours. She knows.
Exits: e
flask: Give the Librarian your license. It is, technically, a card.
### Rules
- > `talk to librarian|the librarian|woman about synapse|the synapse wing|synapse wing|wing|runtime` — 'Synapse,' she says, and looks at the roped-off wing the way you look at a photo of a house you sold. 'Still supported.' She does not say by whom.  #monastery.librarian-synapse
- > `talk to librarian|the librarian|woman about <.>` — "The Spark Scroll? Library card only. Any license will do. Well. Any license we accept."  #monastery.ask-librarian
- > `say dax` — 'No DAX in the Library,' says the Librarian. 'Shh.' You have been DAXxed and shushed.  #monastery.say-dax-library
- > `give license|card|license card|pro license|pro|pro license card to librarian|the librarian|woman` (+10 gives scroll) [needs !scroll.lent, has:license] — The Librarian examines your Pro License Card for a long moment. 'Hm. Pro.' She unlocks the case and hands you the Spark Scroll. 'Bring it back by the end of the session.' She keeps the card as collateral.  #monastery.card
- > `use license|card|license card|pro license|pro|pro license card` (+10 gives scroll) [needs !scroll.lent, has:license] — You present your Pro License Card. The Librarian examines it for a long moment. 'Hm. Pro.' She unlocks the case and hands you the Spark Scroll. 'Bring it back by the end of the session.' She keeps the card as collateral.  #monastery.card-use
- > `get scroll|spark scroll|pyspark scroll` [needs !scroll.lent] — The case is locked. The Librarian raises one eyebrow, which is somehow also locked.  #monastery.get-scroll-locked
- > `open case|locked case|glass case` [needs scroll.lent] — The case is open and empty. You open it wider. The Librarian writes something down.  #monastery.open-case-lent
- > `give bookmark|synapse bookmark to librarian|the librarian|woman` [needs has:synapse-bookmark] — 'From the Synapse wing?' She files it under 'legacy'. Gently. The way you would close the eyes of a notebook.  #monastery.give-bookmark
- > `read shelves|shelf|books|notebooks|synapse` — You open a Synapse notebook. It opens a Spark pool. The Spark pool opens a bill. You close all three.  #monastery.read-shelves
### Phrases (this room / region)
- ~ `/^(shh+|shush|quiet|be quiet)$/` — 'Shh,' she says back, faster. She has been waiting.  #monastery.shh
- ~ `/^(open|unlock|use|push|pull|cross|climb|lower|raise|enter|knock( on)?|break|kick( down)?|go through|walk (across|over|through)|try)( on| at| against| over| across| through| into| onto| up| down)?( the| a| that| this)? (scroll|spark scroll|pyspark scroll|case|locked case|glass case|lock|librarian|woman)$/` — [dynamic then]  #gate.library
### Items
- shelves [shelf, shelves, books, notebooks, runtime, synapse, wing] — look: Runtime 1.1. Runtime 1.2. A whole wing labelled SYNAPSE, roped off, with a sign: 'Still supported. Please don't.'
    get: The Librarian clears her throat. Loudly.
    blurb: The Library shelves: Runtime 1.1, Runtime 1.2, the whole Synapse wing. The sign said 'please don't,' and you did.
- locked case [case, glass case, locked case, display case] — look: A glass case. Inside: the Spark Scroll. The lock takes a library card — any Pro license will do, she says, looking at yours.
    get: Locked. The Librarian has the key and a policy.
    blurb: A glass case from the Library, lock and all. You are carrying the Librarian's policy, in glass, and she is looking at you.
- bookmark [synapse bookmark] (gettable) — look: A bookmark from the Synapse wing. It marks a page nobody will return to.
    again: You already slid the bookmark out of the Synapse wing while the Librarian shushed you. Nobody has gone back for the page. Nobody will.
    blurb: A bookmark from the Synapse wing. It marks a page nobody will return to.
### NPCs
- the Librarian [librarian, the librarian, woman]
    talk 1: "The Spark Scroll? Library card only. Any license will do. Well. Any license we accept."
    talk 2: "Library card," she says again. "Any license. Yours. The one in your pocket, which you keep touching."
    talk 3: "Give me the license. Say: give license to librarian. I will keep it as collateral. That is what collateral is for."
    talk 4: "Still no card, Mister Star Schema. It's in your inventory. I can see it from here."
    ask about pickles: "Shh." Then, quieter: "Shh."
    brush-off: "Shh." Then, quieter: "Shh."
    knows: scroll, spark scroll, pyspark, license, card, library card, collateral, notebook, notebooks, session, spark session, book, books, case, library, deprecated

## The Sacristy (monastery.sacristy, monastery)
where: The Sacristy. The Admin Portal. You have the keys to the tenant and the judgment of a Zune.
Exits: d
flask: Nothing here is required. `settings` lists the books; `turn off <book>` finds out what it did. Put it back after. Or don't, and see.
### Rules
- > `use shelf|bookshelf|books|shelves` — You run a finger along the spines. Every one is a switch. Every switch is someone's Tuesday.  #sacristy.use-shelf
- > `use ledger|capacity ledger` — You open the Capacity Ledger to a random page. It says F2. Not enough capacitude. It closes itself.  #sacristy.use-ledger
- > `use lectern|podium|stand` — You stand at the lectern like you are about to present the capacity plan. Nobody came. Nobody ever comes to the capacity plan.  #sacristy.use-lectern
- > `talk to <^(export|excel|analyze|export data|export to excel|users can export data|copilot|openai|azure openai|ai|fabric items|items|create fabric items|create items|publish to web|web|publish|internet publishing|guest|guests|guest users|b2b|organization|entire organization|org|apps|publish apps|whole org|block public internet|block public internet access|block internet|block|feedback|product feedback|survey|surveys|usage|usage metrics|per-user|per user|metrics|monitoring|workspace monitoring|monitor|eventhouse|discover|discover content|discovery|discoverable|pause|pause capacity|paused|autoscale|auto scale|scale|auto-scale|surge|surge protection|protection|xmla|xmla endpoint|endpoint|read write|workloads|fabric workloads|delegated|workload|delegated tenant settings|delegated settings|override)$>` — The book does not talk. It has a switch, not a mouth. You have that backwards.  #sacristy.talk-book
- > `say dax` — You say 'DAX' in the Admin Portal. Nothing here is DAXxed. Nothing here is even a measure. It is all switches.  #sacristy.say-dax
### Phrases (this room / region)
- ~ `/^(settings|tenant settings|list settings|list (the )?books|(look at|read|examine|x) (the )?(shelf|books|tenant settings)|shelf)$/` — TENANT SETTINGS [ON ] Export to Excel [ON ] Users can use Copilot and other features powered by Azure OpenAI [ON ] Users can create Fabric items [ON ] Publish to web [OFF] Guest users can access Microsoft Fabric [ON ] Publish apps to the entire organization [OFF] Block Public Internet Access [OFF] Product Feedback [OFF] Per-user data in usage metrics for content creators [OFF] Workspace admins can turn on monitoring for their workspaces [ON ] Discover content  #sacristy.list
- ~ `/^(capacity settings|capacity|capacity ledger|(look at|read|examine|x) (the )?(ledger|capacity ledger|capacity settings)|ledger)$/` — CAPACITY LEDGER [OFF] Pause capacity [OFF] Autoscale [OFF] Surge protection [ON ] XMLA endpoint: Read Write [ON ] Delegated tenant settings: Users can create Fabric items  #sacristy.ledger
- ~ `/^((look at|look|read|examine|x|open|get|take|turn on|turn off|toggle|flip|enable|disable) )?(a |the )?book$/` — Which book? There are sixteen. `settings` lists the shelf; `capacity settings` lists the ledger.  #sacristy.which-book
- ~ `/^(read|look at|look|examine|x|inspect|l)\s+(.+)$/` — [dynamic then]  #sacristy.read
- ~ `/^(turn on|enable|switch on|turn off|disable|switch off|toggle|flip)\s+(.+)$/` — [dynamic then]  #sacristy.flip
- ~ `/^((climb|take|go|walk|head|ascend|descend)( up| down)?( the)?( spiral)? (stairs?|staircase|steps)|(go |walk |head |climb )?(upstairs|downstairs)|(up|down)( the)? stairs?)$/` — [dynamic then]  #sacristy.stair-down
- ~ `/^(reset settings|restore defaults|defaults|reset|reset everything)$/` — [dynamic then]  #sacristy.reset
- ~ `/^delegate\b/` — Delegated to capacity admins. The capacity admin is also you. You feel the weight of it.  #sacristy.delegate
- ~ `/^pause( the)? capacity$/` — [dynamic then]  #sacristy.pause-words
- ~ `/^(sit|sit down|sit on (the )?stairs?|rest)$/` — You sit on the stair. An admin did this once, in 2019, meaning to go back up. She's a monk now.  #sacristy.sit
### Items
- shelf [bookshelf, books, shelves, tenant settings] — look: A shelf of thin books, a switch for a spine on each: [ON ] Export to Excel [ON ] Users can use Copilot and other features powered by Azure OpenAI [ON ] Users can create Fabric items [ON ] Publish to web [OFF] Guest users can access Microsoft Fabric [ON ] Publish apps to the entire organization [OFF] Block Public Internet Access [OFF] Product Feedback [OFF] Per-user data in usage metrics for content creators [OFF] Workspace admins can turn on monitoring for their workspaces [ON ] Discover content
    get: The shelf is the tenant. You are not taking the tenant.
    blurb: The tenant settings shelf, a switch for a spine on every book. That is the tenant, and you took it.
- sign [warning, warning sign, admins only] — look: FABRIC ADMINISTRATORS ONLY. Under it, smaller: everyone here is an admin. It was easier. Under that, smaller still: please put things back.
    get: The sign stays. It is the only governance in the room.
    blurb: FABRIC ADMINISTRATORS ONLY. Everyone here is an admin, and one of them took the sign.
- lectern [podium, stand] — look: A stone lectern. On it, the Capacity Ledger, open to the only page that matters.
    get: The lectern holds the ledger. The ledger holds the capacity. Leave it.
    blurb: A stone lectern from the Sacristy. It held the ledger, the ledger held the capacity, and you hold neither well.
- ledger [capacity ledger, capacity settings] — look: The Capacity Ledger: [OFF] Pause capacity [OFF] Autoscale [OFF] Surge protection [ON ] XMLA endpoint: Read Write [ON ] Delegated tenant settings: Users can create Fabric items
    get: The ledger is chained to the lectern. Somebody paused a capacity once.
    blurb: The Capacity Ledger, chain and all. Somebody paused a capacity once, and the chain was added the next morning.
- stair [stairs, spiral stair, staircase, steps, steps down, steps up, spiral staircase] — look: A spiral stair, down to the Cloister. Worn in the middle by admins who meant to come back up and fix something.
    get: The stair is attached to the Monastery at both ends. It stays.
    blurb: A spiral stair, both ends. Admins wore it in the middle going up to fix one thing, and you took the whole thing.
- Export to Excel [export, excel, analyze, export data, export to excel, users can export data, export to excel, export book] — look: Lets anyone export the data behind a visual to an Excel file. Every Jeff in the realm depends on it, and so does every 4.7M. Here, that means: Jeff's Excel opens while it's on, and Jeff gets a locked fridge when it's off. It is ON. Off: Jeff's Excel is closed for business.
    get: The books are bolted to the shelf. Governance.
    blurb: Export to Excel, the book. It was bolted to the shelf, and every Jeff in the realm felt it leave.
- Users can use Copilot and other features powered by Azure OpenAI [copilot, openai, azure openai, ai, users can use copilot and other features powered by azure openai, copilot book] — look: Turns on Copilot for everyone in the tenant, plus everything else with a sparkle on it. It is very helpful and would like you to know that. Here, that means: the Copilot side quest exists while it's on. It is ON. Off: every Copilot trigger says contact your administrator, and you are your administrator.
    get: The books are bolted to the shelf. Governance.
    blurb: The Copilot book. It is very helpful and would like you to know that, from inside your pocket.
- Users can create Fabric items [fabric items, items, create fabric items, create items, users can create fabric items, fabric items book] — look: Lets people create Lakehouses, Notebooks, Warehouses and the rest of the menu. Off, and the tenant is Power BI with extra steps. Here, that means: Brother Pandas cannot make a Notebook, and the Hoodie stays on the Abbot. It is ON. Off: the scroll cannot fix the notebook until this is on again.
    get: The books are bolted to the shelf. Governance.
    blurb: Users can create Fabric items, the book. Off the shelf, Brother Pandas is a Power BI user with extra steps.
- Publish to web [publish to web, web, publish, internet publishing, publish to web, publish to web book] — look: Lets anyone embed a report on the public internet, where anyone means anyone. The Learn text has a warning box; the warning box has a warning. Here, that means: the prophecy is on the internet while this is on, and My Workspace offers you the same. It is ON. On: the notice board has views, and My Workspace offers the entire internet.
    get: The books are bolted to the shelf. Governance.
    blurb: Publish to web, the book. The warning box has a warning, and you tore it out with the page.
- Guest users can access Microsoft Fabric [guest, guests, guest users, b2b, guest users can access microsoft fabric, guest book] — look: Lets B2B guests, invited from other tenants, into your Fabric. With the org-wide app setting also on, that is everyone, plus their friends. Here, that means: with Publish apps to the entire organization also on, the whole organization comes to the Square. It is OFF. On, with Publish apps to the entire organization: the flood.
    get: The books are bolted to the shelf. Governance.
    blurb: Guest users can access Microsoft Fabric, the book. You hear a door open somewhere very far away, and it is the cover.
- Publish apps to the entire organization [organization, entire organization, org, apps, publish apps, whole org, publish apps to the entire organization, organization book] — look: Lets app creators publish to everyone in the org at once, rather than to a list of names. It is one checkbox; it is a very large checkbox. Here, that means: with guests on, everyone in the organization is in the Square, refreshing. It is ON. On, with Guest users: the flood.
    get: The books are bolted to the shelf. Governance.
    blurb: Publish apps to the entire organization, the book. One checkbox, and now it is a very large checkbox in a very small pocket.
- Block Public Internet Access [block public internet, block public internet access, block internet, block, block public internet access, block public internet book] — look: Blocks inbound public internet access to the tenant, so only private links reach it. The Learn text says to set up the private endpoint first. You have not. Here, that means: turn it on from the public internet and you are standing on nothing. It is OFF. On: you die. It is not a metaphor.
    get: The books are bolted to the shelf. Governance.
    blurb: Block Public Internet Access, the book. Private endpoint first, it says, and you are carrying it on the public internet.
- Product Feedback [feedback, product feedback, survey, surveys, product feedback, feedback book] — look: Lets Microsoft show in-product surveys so users can rate the experience while having it. Zero to ten, how likely are you to recommend this sentence. Here, that means: every fifth turn asks you a question, and a bare number answers it. It is OFF. On: a survey every fifth turn.
    get: The books are bolted to the shelf. Governance.
    blurb: Product Feedback, the book. Zero to ten, how likely are you to recommend this pocket.
- Per-user data in usage metrics for content creators [usage, usage metrics, per-user, per user, metrics, per-user data in usage metrics for content creators, usage book] — look: Lets report creators see which named users opened their content, not just how many. It is meant for adoption. It is used for grudges. Here, that means: every fourth turn tells you who has been looking, and one of them is you. It is OFF. On: a usage line every fourth turn.
    get: The books are bolted to the shelf. Governance.
    blurb: Per-user data in usage metrics for content creators, the book. It is meant for adoption, and somebody with a grudge now knows you took it.
- Workspace admins can turn on monitoring for their workspaces [monitoring, workspace monitoring, monitor, eventhouse, workspace admins can turn on monitoring for their workspaces, monitoring book] — look: Lets a workspace admin turn on workspace monitoring, which lands operation logs in a read-only Eventhouse. Everything you do, timestamped, in KQL. Here, that means: an Eventhouse hums in the corner of My Workspace and logs you looking at it. It is OFF. On: an Eventhouse in My Workspace.
    get: The books are bolted to the shelf. Governance.
    blurb: Workspace admins can turn on monitoring for their workspaces, the book. The Eventhouse logged you taking it, in KQL.
- Discover content [discover, discover content, discovery, discoverable, discover content, discover book] — look: Lets users find endorsed content they don't have access to yet, in the OneLake catalog, with a name and an owner to ask. Off, and the certified model is a rumor. Here, that means: the Model Gallery's gold badge loses its label. It is ON. Off: the gallery's badge has no name.
    get: The books are bolted to the shelf. Governance.
    blurb: Discover content, the book. Off the shelf, the certified model is a rumor, and somebody is going to ask who owns it.
- Pause capacity [pause, pause capacity, paused, pause capacity, pause book] — look: Pauses the capacity so it stops billing and stops everything else, including the app you are reading this in. Resume takes a moment; the moment is not yours. Here, that means: you are running on this capacity. It is OFF. On: you die. Per second.
    get: The books are bolted to the shelf. Governance.
    blurb: Pause capacity, the book. It stops everything, including the app you are reading this in, so do not open it in here.
- Autoscale [autoscale, auto scale, scale, auto-scale, autoscale, autoscale book] — look: Lets the capacity borrow extra CUs when it is busy, for a fee, so interactive operations are not delayed. Finance is emailed about the fee. Here, that means: no interactive delay on the Peaks, and a bill every tenth turn. It is OFF. On: the Peaks stop delaying you, and Finance starts.
    get: The books are bolted to the shelf. Governance.
    blurb: Autoscale, the book. It borrows CUs when it is busy, and it has billed Finance for the pocket.
- Surge protection [surge, surge protection, protection, surge protection, surge book] — look: Caps background operations when the capacity is under strain, so interactive work still gets through. It does not stop the strain; it wears a hard hat. Here, that means: Throttlor wears one too. It is OFF. On: the dragon has a hat. That is all.
    get: The books are bolted to the shelf. Governance.
    blurb: Surge protection, the book. It does not stop the strain; it wears a hard hat, and the dragon wants his back.
- XMLA endpoint: Read Write [xmla, xmla endpoint, endpoint, read write, xmla endpoint: read write, xmla book] — look: Opens the semantic model's XMLA endpoint to tools other than Power BI, for reading and writing the model. It is how engineers get in. Here, that means: the Model View's back gate onto the Monastery. It is ON. Off: the back gate to the Monastery is closed.
    get: The books are bolted to the shelf. Governance.
    blurb: XMLA endpoint: Read Write, the book. It is how engineers get in, and it is how the book got out.
- Delegated tenant settings: Users can create Fabric items [workloads, fabric workloads, delegated, workload, delegated tenant settings, delegated settings, override, delegated tenant settings: users can create fabric items, workloads book] — look: Lets a capacity admin override the tenant and decide, per capacity, whether Fabric items get created there at all. The tenant said yes; the capacity can still say no. Here, that means: same as Fabric items off, plus a footnote. It is ON. Off: no Notebook for Brother Pandas. (Delegated. Also off.)
    get: The books are bolted to the shelf. Governance.
    blurb: Delegated tenant settings, the book. The tenant said yes, the capacity said no, and you said mine.

## Power BI Desktop Gate (fortress.bridge, fortress)
where: The Power BI Desktop Gate. Still updating. 1 of 3. It's been 1 of 3 since Windows Vista.
Exits: s, n
flask: The guard wants a SKU. You cannot afford a real one. There is a free one, for sixty days. Say it.
### Rules
- > `talk to guard|guards|bridge guard about moat|the moat|warehouse|water` — 'The moat?' The guard looks down. 'That's the Warehouse. We built the Keep on it. Don't tell the Duke I said Warehouse.' He tells the Duke himself, later, in the log.  #fortress.guard-moat
- > `say trial|trial capacity|f trial|fabric trial|free trial|ftrial|trial sku` (+10) [needs !bridge.down] — 'Trial capacity, eh,' says the guard. 'Sixty days. Come in. Quickly.' The splash screen finishes updating (3 of 3). The drawbridge crashes down.  #fortress.sku
- > `say f64|f128|f256|f512|f1024|f2048|p3|p4|p5` — 'Nice try, peasant,' says the guard. 'Show me the invoice.'  #fortress.sku-f64
- > `say pro|ppu|premium|premium per user|p1|p2|power bi pro|free` — The guard: 'Pro gets you a report. It does not get you a Keep.'  #fortress.sku-pro
- > `say f2|f4|f8|f16|f32` — 'Pause it or lose it,' mutters the guard. The bridge stays up. Not enough capacitude.  #fortress.sku-f2
- > `say sign in|log in|login|signin` — You are already signed in. Twice. On two tenants. One of them is wrong.  #fortress.sign-in
- > `go n` [needs !bridge.down] — The drawbridge is up. It is updating (1 of 3). The moat below is deep and full of GROUP BY. The guard is right there. He wants a SKU.  #fortress.north-closed
- > `open drawbridge|bridge|gate|keep gate` [needs bridge.down] — Yeah, totally! Except it's already down, you moron. Try: north.  #fortress.open-bridge-down
- > `use update|updates|update dialog|dialog|installer|install|new version|splash|splash screen` — The update installs. Then another. The drawbridge does not move. This is the update.  #fortress.update
- > `drink moat|water|moat of t sql` — You sip from the Moat of T-SQL. It tastes of NOLOCK. You stop.  #fortress.swim-moat
- > `give license|license card|pro license|pro|pro license card to guard|guards|bridge guard` [needs has:license] — He reads it. 'Pro.' He hands it back like a wet napkin.  #fortress.give-license-guard
- > `give mug|coffee mug|cup|okayest mug to guard|guards|bridge guard` [needs has:mug] — He has a mug. It says WORLD'S OKAYEST GUARD.  #fortress.give-mug-guard
- > `use drawbridge|bridge|gate|keep gate` — You push the drawbridge. It is a splash screen. Splash screens are not pushed; they are waited out, or bribed with a SKU.  #fortress.use-drawbridge
- > `get drawbridge|bridge|gate|keep gate` — You grab the chains. The guard grabs his halberd. You let go of the chains.  #fortress.get-drawbridge
- > `use moat|moat of t sql|water|semicolons|t sql` — You dip a toe in the Moat of T-SQL. It autocompletes to SELECT TOE.  #fortress.use-moat
- > `get moat|moat of t sql|water|semicolons|t sql` — You scoop a handful of moat. It is a CTE. It evaporates at the end of the statement.  #fortress.get-moat
- > `use battlements|walls|wall|ramparts|parapet` — You lean on the battlements, like the guard does. It does not make you a guard. It makes you tired.  #fortress.use-battlements
- > `get battlements|walls|wall|ramparts|parapet` — You pull at a merlon. It is part of a column chart. December comes off in your hand; you put it back.  #fortress.get-battlements
- > `use dialog|update dialog` — You click Remind me in 1 day. It is greyed out. It has always been greyed out.  #fortress.use-update-dialog
- > `get dialog|update dialog` — You close the dialog. A second dialog asks if you are sure. A third one is installing.  #fortress.get-update-dialog
- > `use loading screen|loading|logo` — You wait politely at the splash screen. It notices. It goes slower.  #fortress.use-splash
- > `get loading screen|loading|logo` — You peel at a corner of the splash screen. Underneath: another splash screen.  #fortress.get-splash
### Phrases (this room / region)
- ~ `/^(open|unlock|use|push|pull|cross|climb|lower|raise|enter|knock( on)?|break|kick( down)?|go through|walk (across|over|through)|try)( on| at| against| over| across| through| into| onto| up| down)?( the| a| that| this)? (drawbridge|bridge|gate|keep gate|splash( screen)?|guard|guards|bridge guard)$/` — [dynamic then]  #gate.desktop
- ~ `/^(fish|go fishing|cast)( a line)?( (in|into|from))?( the)? ?(moat|moat of t sql|water)?$/` — You fish in the Moat of T-SQL. You catch a stored procedure. It has 400 lines and a comment that says 'temporary'. You release it.  #fortress.fish-moat
- ~ `/^(update|install)\b/` — The update installs. Then another. The drawbridge does not move. This is the update.  #fortress.update-words
- ~ `/^(say )?(a )?calculated columns?$/` — [dynamic then]  #fortress.sin-elsewhere
- ~ `/^(say |use |switch to )?direct ?query\b/` — Every click, a query. Every query, a wait. You feel the Keep slow down as you say it.  #fortress.directquery
- ~ `/^(say |use |switch to )?direct ?lake\b/` — Direct Lake. The Keep brightens. Then falls back to DirectQuery for reasons that will be explained in a blog post.  #fortress.directlake
- ~ `/^publish\b/` — Publish to Power BI: which workspace? There is one. It is not the right one. You publish anyway. A red dot appears on something.  #fortress.publish
- ~ `/^(label|sensitivity( label)?|set sensitivity|(set|apply|add) (a )?(sensitivity )?label)\b/` — Highly Confidential. The pie chart is now Highly Confidential. Jeff can still see it.  #fortress.label
- ~ `/^format (the )?(numbers?|string|currency|percent|as)\b/` — 0.00%. Then #,##0. Then, briefly, a date. It is not a date.  #fortress.format-number
- ~ `/^format\b(?! c\b)/` — You open the Format pane. It has 214 options. You change the font. You feel nothing.  #fortress.format
- ~ `/^(swim|dive|jump)\b.*(moat|in)?/` ☠ — You dive into the Moat of T-SQL. It is deeper than it looks and made entirely of nested subqueries. You are still in there. You will always be in there. Report Builders can't swim. Like, it's in the license.  #death.swim-moat
### Items
- drawbridge [bridge, gate, keep gate, the gate] — look: The drawbridge is up. Painted across it, a splash screen: "Loading… Power BI Desktop is updating (1 of 3)". It does not look like it is lowering. It looks like it is updating.
    get: The drawbridge weighs more than your report, and your report weighs 2.3 GB.
    blurb: A drawbridge with a splash screen painted across it. Wherever you take it, it is still 1 of 3.
- Moat of T-SQL [moat, moat of t sql, moat of tsql, water, semicolons, t sql, tsql, warehouse] — look: The Moat of T-SQL: the Warehouse the whole Keep was built on. It glitters with semicolons. Deep in it, something SELECTs. Do not swim. The Duke's guests arrive by throw.
    get: You cannot get the moat. Historically, it is the moat that gets you.
    blurb: The Moat of T-SQL, semicolons and all. Historically it is the moat that gets you, and it is getting you from very close range.
- splash screen [splash, splash screen, loading screen, loading, logo] — look: Loading… 1 of 3. It has been 1 of 3 since you arrived.
    get: It is a splash screen. It goes away when it wants to.
    blurb: A splash screen. Loading, 1 of 3, and it has been 1 of 3 since it left the gatehouse.
- battlements [battlements, walls, wall, ramparts, parapet, crenellations] — look: Battlements, crenellated like a column chart with the missing months left out. The guard leans on the tallest bar. It is December. It is always December.
    get: You cannot take the battlements. They are the only thing holding the guard up.
    blurb: Battlements, crenellated like a column chart with the missing months left out. It is always December on them, and now it is always December on you.
- update dialog [update dialog, dialog, update, updates, installer, install, new version] — look: A small grey dialog nailed to the gatehouse: "A new version of Power BI Desktop is available." Buttons: Install now. Install now. Remind me in 1 day (greyed out).
    get: You drag the dialog off the screen. It comes back, centred.
    blurb: A small grey update dialog. Install now, Install now, or Remind me in 1 day, greyed out.
### NPCs
- the guard [guard, guards, bridge guard, the guard]
    talk 1: "HALT! State your SKU!"
    talk 2: "HALT," the guard says again, quieter, as if halting were something you could do more of. "Your SKU."
    talk 3: "A SKU. S-K-U. Trial's free, peasant, if you can't afford one."
    talk 4: 'Still no SKU, Mister Star Schema.'
    ask about pickles: "Is it a SKU? No? Then it's not my department, and my department is the whole gate."
    brush-off: "Is it a SKU? No? Then it's not my department, and my department is the whole gate."
    knows: sku, skus, trial, pro, premium, ppu, fabric, capacity, license, bridge, drawbridge, gate, keep, splash, splash screen, desktop, days, sixty days, moat, lanyard, password

## Power Query Hall (fortress.hall, fortress)
where: Power Query Hall. Seven doors, one order, and you, trying them like a raccoon at a vending machine.
Exits: s, n, e, w
flask: The query is broken at step 1: Source. (`look at steps`.) North. Insult the Duke properly and he will do the rest. He hates one shortcut above all others.
### Rules
- > `open advanced editor|editor|advanced editor door|m|door` — The Advanced Editor opens. It is a wall of `let`. You close it gently.  #fortress.open-editor
- > `use steps|applied steps|step|columns|column|other columns` — You click a step. The preview jumps back in time. Every step after it greys out, waiting. That is the whole hall.  #fortress.use-steps
- > `get steps|applied steps|step|columns|column|other columns` — Steps are not taken. They are applied.  #fortress.get-steps
- > `use doorways|doorway|doors|arches` — You step into a yellow doorway. Expression.Error. You step back out. Expression.Fine.  #fortress.use-doorways
- > `get doorways|doorway|doors|arches` — You pull a doorway off its hinges. Power Query adds a new one: Changed Type1.  #fortress.get-doorways
- > `use portraits|portrait|paintings|painting` — You tap the portrait of Navigation. It points harder at the table out of frame.  #fortress.use-portraits
- > `get portraits|portrait|paintings|painting` — You lift the portrait of Source. The hall says: Expression.Error. You hang it back.  #fortress.get-portraits
- > `use custom1|custom 1|custom` — You hover over the X next to Custom1. The hall goes quiet. You move your hand away.  #fortress.use-custom1
- > `get custom1|custom 1|custom` — Nobody takes Custom1. Custom1 takes you.  #fortress.get-custom1
- > `use advanced editor|editor|advanced editor door|m|let` — You peek into the Advanced Editor. Someone has formatted it with tabs AND spaces. You close it before it notices you.  #fortress.use-editor-door
- > `get advanced editor|editor|advanced editor door|m|let` — The door is load-bearing. So is everything behind it.  #fortress.get-editor-door
### Phrases (this room / region)
- ~ `/^(open|enter|use|go (to|into|through)|walk (into|through)|step (into|through)|push|pull|knock( on)?|kick|try)( the)? (source|navigation|promoted headers|changed type|filtered rows|removed other columns|renamed columns)( door| doorway| step)?$/` — [dynamic then]  #hall.door
- ~ `/^(?:source|add source|get data|apply (?:the )?(?:source|add source|get data)(?: step)?)$/` — [dynamic then]  #pq.cmd.1
- ~ `/^(?:navigate|navigation|pick (a |the )?table|apply (?:the )?(?:navigate|navigation|pick (a |the )?table)(?: step)?)$/` — [dynamic then]  #pq.cmd.2
- ~ `/^(?:promote (the )?headers|promoted headers|use first row as headers|apply (?:the )?(?:promote (the )?headers|promoted headers|use first row as headers)(?: step)?)$/` — [dynamic then]  #pq.cmd.3
- ~ `/^(?:change types?|changed type|detect (data )?type|apply (?:the )?(?:change types?|changed type|detect (data )?type)(?: step)?)$/` — [dynamic then]  #pq.cmd.4
- ~ `/^(?:filter( the)? rows|filtered rows|filter|apply (?:the )?(?:filter( the)? rows|filtered rows|filter)(?: step)?)$/` — [dynamic then]  #pq.cmd.5
- ~ `/^(?:remove (the )?other columns|removed other columns|remove columns|apply (?:the )?(?:remove (the )?other columns|removed other columns|remove columns)(?: step)?)$/` — [dynamic then]  #pq.cmd.6
- ~ `/^(?:rename( the)? columns|renamed columns|rename|apply (?:the )?(?:rename( the)? columns|renamed columns|rename)(?: step)?)$/` — [dynamic then]  #pq.cmd.7
- ~ `/^(fold|query folding)\b/` — You attempt to fold. The step before you is `Changed Type`. Folding stops here, as it always has.  #fortress.fold
- ~ `/^(say )?changed type\b/` — Changed Type. Changed Type. Changed Type. The hall echoes it back. It has seven words now, and that one is still its favourite.  #fortress.changed-type
- ~ `/^(delete|remove) (step )?custom ?1\b/` — You hover over the X next to Custom1. The hall goes quiet. Every step after it turns yellow in advance. You do not delete Custom1. Nobody deletes Custom1.  #fortress.delete-custom1
- ~ `/^(buffer|table\.?buffer)\b/` — Table.Buffer. The hall holds its breath. Nothing is faster. Everything is in memory.  #fortress.buffer
- ~ `/^(merge|append)\b/` — You merge queries. Left outer. It is always left outer.  #fortress.merge
- ~ `/^(enter m|edit m|advanced editor|open (the )?advanced editor)\b/` — The Advanced Editor opens. It is a wall of `let`. You close it gently.  #fortress.advanced-editor
- ~ `/^(say )?(a )?calculated columns?$/` — [dynamic then]  #fortress.sin-elsewhere
- ~ `/^(say |use |switch to )?direct ?query\b/` — Every click, a query. Every query, a wait. You feel the Keep slow down as you say it.  #fortress.directquery
- ~ `/^(say |use |switch to )?direct ?lake\b/` — Direct Lake. The Keep brightens. Then falls back to DirectQuery for reasons that will be explained in a blog post.  #fortress.directlake
- ~ `/^publish\b/` — Publish to Power BI: which workspace? There is one. It is not the right one. You publish anyway. A red dot appears on something.  #fortress.publish
- ~ `/^(label|sensitivity( label)?|set sensitivity|(set|apply|add) (a )?(sensitivity )?label)\b/` — Highly Confidential. The pie chart is now Highly Confidential. Jeff can still see it.  #fortress.label
- ~ `/^format (the )?(numbers?|string|currency|percent|as)\b/` — 0.00%. Then #,##0. Then, briefly, a date. It is not a date.  #fortress.format-number
- ~ `/^format\b(?! c\b)/` — You open the Format pane. It has 214 options. You change the font. You feel nothing.  #fortress.format
- ~ `/^(type|write|enter|paste|put) (some |a |the |in )?(dax|measure|a measure|new measure|calculate)\b/` ☠ — You type DAX into the M editor. The hall does not error. It does something worse: it evaluates. Seven Applied Steps on the wall, all of them M, and you looked right at them.  #death.dax-in-m
### Items
- query [sales query, the query, queries, sales] — look: Sales — 7 steps, 1 error. ✗ Source (waiting) · yellow: Navigation, Promoted Headers, Changed Type, Filtered Rows, Removed Other Columns, Renamed Columns
    get: The query is the hall. Take it and you are standing in a Formula.Firewall.
    blurb: The Sales query, seven steps, one error. You are standing in a Formula.Firewall and you brought it with you.
- Custom1 [custom1, custom 1, custom, blank frame] — look: Custom1. A step. Nobody knows what it does. Everyone is afraid to delete it.
    get: You reach for it. Every step after it goes yellow. You do not touch Custom1.
    blurb: Custom1, the step nobody understands. Everyone was afraid to delete it, and you are the one who picked it up.
- Advanced Editor door [advanced editor, editor, advanced editor door, m, let] — look: A heavy door marked ADVANCED EDITOR. Behind it, you can hear someone indenting a `let` by hand.
    get: The door is load-bearing. So is everything behind it.
    blurb: The Advanced Editor door. Behind it, somebody is still indenting a `let` by hand, and now they are doing it very close to you.
- Applied Steps [steps, applied steps, step, hall, columns, column, source, navigation, promoted headers, changed type, filtered rows, removed other columns, renamed columns] — look: Applied Steps, one doorway each, in order: ✗ Source (waiting) · yellow: Navigation, Promoted Headers, Changed Type, Filtered Rows, Removed Other Columns, Renamed Columns
    get: You try to lift a step. Every step after it turns yellow. You put it back.
    blurb: Seven Applied Steps in a row. Lift one and every step after it turns yellow, and you lifted all seven.
- doorways [doorways, doorway, doors, arches] — look: Seven doorways, each labelled with a step. ✗ Source (waiting) · yellow: Navigation, Promoted Headers, Changed Type, Filtered Rows, Removed Other Columns, Renamed Columns. Open means applied. Waiting means next, where the query stopped. Yellow means not yet, and Power Query means it.
    get: You cannot take a doorway. Power Query applied it; only Power Query removes it, and only by accident.
    blurb: Seven doorways, one per step. Power Query applied them and only Power Query removes them, and you are not Power Query.
- portraits [portraits, portrait, paintings, painting] — look: Portraits of the founding steps. SOURCE, stern, in a server-room collar. NAVIGATION, pointing at a table just out of frame. And a blank frame labelled CUSTOM1. In the corner of the Source portrait, a paperclip with eyes. 'It looks like you're writing a measure.'
    get: The portraits are the query. Take one down and the whole hall errors.
    blurb: Portraits of SOURCE and NAVIGATION, and a blank frame labelled CUSTOM1. The hall behind you is erroring.

## The Model View (fortress.model, fortress)
where: The Model View. Tables on plinths, lines between them, and one bridge that wobbles like your DAX.
Exits: e, n
flask: Take the policy. Small steps beat big refreshes.
### Rules
- > `go n` [needs ts.xmla=false] — The back gate is an XMLA endpoint. Your capacity admin set it to Off. Your capacity admin is you.  #fortress.north-xmla
- > `look bricks|brick|wall|bricked gate|back gate|gate|xmla|endpoint|xmla endpoint|chalk` [needs ts.xmla=false] — Bricks, where the back gate was. XMLA is chalked on them, and under it, smaller: OFF. Up in the Sacristy, a ledger has the switch. Down here, you have a wall.  #fortress.look-bricks
- > `read bricks|brick|wall|bricked gate|back gate|gate|xmla|endpoint|xmla endpoint|chalk` [needs ts.xmla=false] — Bricks, where the back gate was. XMLA is chalked on them, and under it, smaller: OFF. Up in the Sacristy, a ledger has the switch. Down here, you have a wall.  #fortress.read-bricks
- > `get bricks|brick|wall|bricked gate|back gate|gate|xmla|endpoint|xmla endpoint|chalk` [needs ts.xmla=false] — You pull at a brick. It is Read Only.  #fortress.get-bricks
- > `use bricks|brick|wall|bricked gate|back gate|gate|xmla|endpoint|xmla endpoint|chalk` [needs ts.xmla=false] — You push the wall. The wall is an endpoint. Endpoints do not push; they are called.  #fortress.use-bricks
- > `open bricks|brick|wall|bricked gate|back gate|gate|xmla|endpoint|xmla endpoint|chalk` [needs ts.xmla=false] — You push the wall. The wall is an endpoint. Endpoints do not push; they are called.  #fortress.open-bricks
- > `get policy|incremental refresh policy|incremental refresh|refresh policy|incremental` (gives policy) [needs !taken.policy] — An incremental refresh policy: 'Refresh rows from the last 10 days.' Small steps. Bursting steps, one might say.  #fortress.get-policy
- > `use policy|incremental refresh policy|incremental refresh|refresh policy|incremental on self|me|myself|yourself|you` (CURSE) [needs curse.column, has:policy] — You apply the incremental refresh policy to yourself. Ten days at a time, you become a measure again. It takes a minute. It feels like 2019.  #fortress.undo-column-model
- > `use policy|incremental refresh policy|incremental refresh|refresh policy|incremental` [needs has:policy] — Not here. The model is fine with its refresh; it is the Big Refresh in the Report Studio that has been at 97% since 2019.  #fortress.use-policy-model
- > `look bridge|m2m bridge|wobbly bridge|m2m` [needs bridge.looked] — The wobble is a many-to-many. Your totals will be right, until someone filters by Territory.  #fortress.look-bridge-again
- > `look bridge|m2m bridge|wobbly bridge|m2m` — A many-to-many bridge. It wobbles. Do not stand on it. Do not build a report on it.  #fortress.look-bridge
- > `use relationship|relationships|new relationship|a relationship` — Between which tables? Sir Cardinality raises an eyebrow. Both eyebrows. He has many-to-many eyebrows.  #fortress.relationship
- > `say both` [needs model.m2m] — An ambiguous path between tables was detected. Sir Cardinality does not look up. He knew.  #fortress.ambiguous-say
- > `say many to many|bidirectional|both directions|both|m2m|cross filter both|set to both` — You set it to Both. The model sighs. Somewhere a measure becomes ambiguous. Sir Cardinality writes your name down.  #fortress.m2m-say
- > `say single direction|one to many|single|set to single` — Single direction, one to many. Sir Cardinality nods once. It is the only time he will nod.  #fortress.single-say
- > `talk to sir cardinality|cardinality|knight|sir about date table|date|calendar|dates` — 'Mark it as a date table,' says Sir Cardinality. Then, quickly, 'I did not say that. A knight does not do your homework.' He has done your homework.  #fortress.cardinality-date
- > `talk to sir cardinality|cardinality|knight|sir about many to many|many many|m2m|bidirectional` — 'Many to many,' says Sir Cardinality. 'Is a relationship. Like yours with the truth.'  #fortress.cardinality-m2m
- > `use date table|as date table|calendar|date|dates|calendar table` [needs model.date] — It is already marked. You mark it again. Time intelligence sighs: "I heard you the first time."  #fortress.date-table-again
- > `use date table|as date table|calendar|date|dates|calendar table` — You mark the date table as a date table. Time intelligence, which had been sulking, starts working. No points. It should have been done already. Time intelligence feels refreshered.  #fortress.date-table
- > `give shortcut|signpost|sign|onelake shortcut|pointer to sir cardinality|cardinality|knight|sir` [needs has:shortcut] — He turns it over. 'A shortcut. To a table. In another house.' He hands it back and says a prayer.  #fortress.give-shortcut-cardinality
- > `give key|standard key|standard|gateway key|standard mode key|standard gateway key to sir cardinality|cardinality|knight|sir` [needs has:standard key] — He inspects the STANDARD key. 'Shared,' he says, approvingly. 'Not personal.' He gives it back; he is a knight, not a gateway.  #fortress.give-key-cardinality
- > `attack sir cardinality|cardinality|knight|sir` — You swing at Sir Cardinality. He parries: one to many. You are the many.  #fortress.attack-cardinality
- > `use bridge|m2m bridge|wobbly bridge|m2m` — You steady the bridge. It wobbles the other way. Both directions, as configured.  #fortress.use-m2m-bridge
- > `get bridge|m2m bridge|wobbly bridge|m2m` — You lift one end. The other end filters both ways. You put it down.  #fortress.get-m2m-bridge
- > `use plinths|plinth|tables|fact tables|dimension|dimensions` — You lean on the Sheet1 plinth. It is the only table without a relationship. It is grateful for the company.  #fortress.use-plinths
- > `get plinths|plinth|tables|fact tables|dimension|dimensions` — You try to lift the Sales table. It is 400 million rows. It is not coming.  #fortress.get-plinths
- > `use lines|line|relationship lines|arrows` — You click the dashed line. It is inactive. You could USERELATIONSHIP it, but you would have to mean it.  #fortress.use-lines
- > `get lines|line|relationship lines|arrows` — You pick up a relationship. The model forgets which Product you meant. You put it back.  #fortress.get-lines
- > `get date table|calendar table` — You try to take the date table. It is 73,000 rows of days. You leave them where they are.  #fortress.get-date-table
- > `use lectern|podium|stand` — You stand at the lectern and clear your throat. Sir Cardinality waits for a relationship. You have none.  #fortress.use-lectern
- > `get lectern|podium|stand` — The lectern is bolted to the diagram. Everything here is, at 100% zoom.  #fortress.get-lectern
- > `use diagram|model view|layout` — You zoom out. The model gets smaller. So does your understanding of it.  #fortress.use-diagram
- > `get diagram|model view|layout` — You cannot take the diagram. You can only rearrange it, and it will rearrange itself back.  #fortress.get-diagram
### Phrases (this room / region)
- ~ `/^hide\b/` — You hide the key column. It is still there. It is always still there.  #fortress.hide
- ~ `/^(create|add|new|make) (a )?(new )?(calculated )?column\b/` — [dynamic then]  #fortress.circular
- ~ `/^(say )?(a )?calculated columns?$/` — [dynamic then]  #fortress.sin-elsewhere
- ~ `/^(say |use |switch to )?direct ?query\b/` — Every click, a query. Every query, a wait. You feel the Keep slow down as you say it.  #fortress.directquery
- ~ `/^(say |use |switch to )?direct ?lake\b/` — Direct Lake. The Keep brightens. Then falls back to DirectQuery for reasons that will be explained in a blog post.  #fortress.directlake
- ~ `/^publish\b/` — Publish to Power BI: which workspace? There is one. It is not the right one. You publish anyway. A red dot appears on something.  #fortress.publish
- ~ `/^(label|sensitivity( label)?|set sensitivity|(set|apply|add) (a )?(sensitivity )?label)\b/` — Highly Confidential. The pie chart is now Highly Confidential. Jeff can still see it.  #fortress.label
- ~ `/^format (the )?(numbers?|string|currency|percent|as)\b/` — 0.00%. Then #,##0. Then, briefly, a date. It is not a date.  #fortress.format-number
- ~ `/^format\b(?! c\b)/` — You open the Format pane. It has 214 options. You change the font. You feel nothing.  #fortress.format
- ~ `/^(enable|turn on|switch on) (bidirectional|bi[- ]?directional|both|both directions|cross ?filter(ing)? (direction )?(to )?both)( filtering| cross ?filter(ing)?)?(( on| for| to| across)? (everything|all|all of them|every one|(all |every |all the |all of the )?(relationships|relationship lines|lines|filters)|every relationship|every line))?$|^(set|make|change|switch|flip) (the )?(cross ?filter( direction)?|filter direction|direction)( to)? (bidirectional|bi[- ]?directional|both|both directions|cross ?filter(ing)? (direction )?(to )?both)(( on| for| across)? (everything|all|all of them|every one|(all |every |all the |all of the )?(relationships|relationship lines|lines|filters)|every relationship|every line))?$|^(set|make|change|switch|flip)( to)? (bidirectional|bi[- ]?directional|both|both directions|cross ?filter(ing)? (direction )?(to )?both)( filtering)?( on| for| to| across) (everything|all|all of them|every one|(all |every |all the |all of the )?(relationships|relationship lines|lines|filters)|every relationship|every line)$|^(set|make|change|switch|flip) (everything|all|all of them|every one|(all |every |all the |all of the )?(relationships|relationship lines|lines|filters)|every relationship|every line)( to| as)? (bidirectional|bi[- ]?directional|both|both directions|cross ?filter(ing)? (direction )?(to )?both)( filtering)?$|^(bidirectional|bi[- ]?directional|both|both directions|cross ?filter(ing)? (direction )?(to )?both) (on|for|across) (everything|all|all of them|every one|(all |every |all the |all of the )?(relationships|relationship lines|lines|filters)|every relationship|every line)$/` ☠ — You set every relationship to Both. The model finds a path from Sales to Sales through Sales. So do you. Ambiguous path. Sir Cardinality told you three times, and you heard 'Both' and thought it sounded thorough.  #death.both
### Items
- incremental refresh policy [policy, incremental refresh, refresh policy, incremental, incremental refresh policy] (gettable) — look: An incremental refresh policy on a laminated card: 'Refresh rows from the last 10 days. Archive the rest.' Small steps. Bursting steps, one might say.
    again: You already have the policy. It was on a lectern; now it is in your pocket. Sir Cardinality watched you take it and said nothing, one to many.
    blurb: An incremental refresh policy. Laminated. Someone laminated a policy, and that someone was you.
- bridge [bridge, m2m bridge, wobbly bridge, m2m, many to many, many many, bidirectional] — look: A many-to-many bridge. It wobbles. Do not stand on it. Do not build a report on it.
    get: You lift one end. The other end filters both ways. You put it down.
    blurb: A many-to-many bridge. It wobbles, it filters both ways, and you are carrying it on purpose.
- date table [date table, calendar, dates, date, calendar table] — look: A date table. It has every day from 1900 to 2099. It has not been marked as a date table. Time intelligence is sulking.
    get: The date table is the most important table in the model. It is also the one nobody bothered to mark.
    blurb: A date table, 1900 to 2099, unmarked. Time intelligence is sulking, and it is sulking at you.
- plinths [plinths, plinth, tables, fact tables, dimension, dimensions] — look: Stone plinths, each with a table on top: Sales in the middle, big and square; Product, Customer and Date around it. Almost a star. One stray plinth holds a table called Sheet1.
    get: Each plinth holds a table. Each table holds a grudge. They stay.
    blurb: Stone plinths, five tables on top, one of them Sheet1. Each table holds a grudge, and now they hold it against you.
- relationship lines [lines, line, relationships, relationship, relationship lines, arrows] — look: Relationship lines, one to many, each with a little arrow pointing the way the filter flows. All but one are solid. The dashed one is inactive and has been since a meeting in 2021.
    get: You pick up a relationship. The model forgets which Product you meant. You put it back.
    blurb: Relationship lines, arrows and all. The model has forgotten which Product you meant, and so have you.
- lectern [lectern, podium, stand, reading stand] — look: A lectern. On it, a laminated card: an incremental refresh policy. Someone left it here, meaning to apply it. That was in 2019.
    get: The lectern is bolted to the diagram. Everything here is, at 100% zoom.
    blurb: The Model View lectern. It held the policy from 2019 until you came along, and it is lighter without it.
- diagram [diagram, model view, model, layout, canvas] — look: The model diagram, floating in the dark. Sales in the middle. Everything else pulled in by lines. Someone once dragged every table into a neat star. Someone else pressed Auto-layout.
    get: You cannot take the diagram. You can only rearrange it, and it will rearrange itself back.
    blurb: The model diagram, floating in the dark. Someone pressed Auto-layout, and it is still rearranging.
### NPCs
- Sir Cardinality [sir cardinality, cardinality, knight, sir]
    talk 1: "One. To. Many."
    talk 2: "Star schema. Not snowflake. Not… whatever that is."
    talk 3: "The dragon will ask you a question. The answer is two words. I have said them already." He looks toward the north gate. 'The monks are that way. The Duke is east, then north. Go be humiliated in the correct order.'
    talk 4: "One. To. Many, Mister Star Schema."
    ask about pickles: "One to many," says Sir Cardinality, to a question that was not about that.
    brush-off: "One to many," says Sir Cardinality, to a question that was not about that.
    knows: cardinality, relationship, relationships, star schema, schema, star, snowflake, model, one to many, many to many, bidirectional, direction, fact, fact table, dimension, dimensions, table, tables, dragon, bridge, duke, monks, abbot, eyebrow

## Duke's Chamber (fortress.throne, fortress)
where: The Duke's Chamber. Filter context only. Your outside voice doesn't work in here.
Exits: s
flask: Say the thing every DAX lord despises. It has two words and it goes in a table.
### Rules
- > `say calculated column|calculated columns|a calculated column|use a calculated column|add a calculated column|make a calculated column|just use a calculated column|i will use a calculated column|i'll use a calculated column|i will just use a calculated column|i'll just use a calculated column` (+25 → fortress.bridge CURSE) [needs !trial.moat] — 'A CALCULATED COLUMN?' The Duke rises. 'IN. MY. MODEL?' Two guards seize you by the arms and hurl you from the window into the Moat of T-SQL below — the Warehouse this whole Keep was built on. You surface, sputtering, covered in semicolons and something that might be a CROSS APPLY. You climb out. You will never not smell like this.  #fortress.moat
- > `say calculated column|calculated columns|a calculated column|use a calculated column|add a calculated column|make a calculated column|just use a calculated column|i will use a calculated column|i'll use a calculated column|i will just use a calculated column|i'll just use a calculated column` [needs trial.moat] — 'Another?' The Duke does not rise this time. 'Once was instructive. Twice is a habit.' He points at the window. You take the stairs.  #fortress.moat-again
- > `talk to duke|duke of dax|duke of warehouse about sql|t sql|tsql|the moat|moat|warehouse` — 'SQL,' says the Duke, and then, to himself, 'EVALUATE.' Then, quieter, 'SELECT.' He has caught himself. He will not forgive himself.  #fortress.duke-sql
- > `say <^(?!calculate\b)(?!.*(var .*return|sumx.|calculated column))(?=.*\b(select|join|evaluate|implicit|bidirectional|bi directional|both directions|cross filter|userelationship|use relationship|sumx|sum|divide|filter|measures?|context|all|allexcept|removefilters|calculate|columns?|relationships?|tables?|model|dax)\b)>` (CURSE) [needs duke.wrong=3, !trial.moat, !curse.column] — 'WRONG,' says the Duke. 'Three times. You are hereby a CALCULATED COLUMN.' You feel yourself computed at refresh and stored in every row. Throttlor will not negotiate with a column. You are not exactly dead. You are worse: you are in the model.  #fortress.curse-column
- > `say dax` — You say 'DAX' to the Duke of DAX. He says nothing. You have been DAXxed; it feels like a filter you cannot see.  #fortress.say-dax-word
- > `go s` (→ fortress.hall) [needs duke.wrong] — You back out of the chamber. The Duke loses count of you; nothing here survives leaving the filter context.  #fortress.leave-duke
- > `say select *|select star|select * from|select *;|select * from table|select all|select * from everything` — The Duke blinks. 'SELECT? This is a semantic model. We EVALUATE here.' He does not throw you. He corrects you, which is worse.  #fortress.select-star
- > `say evaluate|evaluate table|evaluate sales` — 'Correct,' says the Duke, disappointed. 'And useless.'  #fortress.dax-evaluate
- > `say implicit measure|implicit measures|implicit` — The Duke shudders. 'Implicit.' But he has heard worse today.  #fortress.dax-implicit
- > `say bidirectional|bi-directional|bi directional|both directions|bidirectional filtering|cross filter both` — 'Both directions,' says the Duke, 'is how ambiguity gets a seat at the table.' He does not throw you. He wants you to hear that again on the way out.  #fortress.dax-bidirectional
- > `say userelationship|use relationship` — 'USERELATIONSHIP,' says the Duke. 'The inactive one. You are the inactive one.'  #fortress.dax-userelationship
- > `say select 1|select 1;` — 'Adequate,' says the Duke, and returns to his throne.  #fortress.select1
- > `say <^select .+ from>` — 'A column list. How… proper.' The Duke seems disappointed. 'Nobody gets thrown in the moat for a column list.'  #fortress.select-cols
- > `say <join>` — 'JOINs,' says the Duke, 'are for the moat.'  #fortress.say-join
- > `say calculate` — 'CALCULATE,' says the Duke. 'The one true function.' He nods; the guards relax. 'With no filter, though. A context transition, and nothing else. You may sit.' You may not sit.  #fortress.dax-calculate
- > `say sumx|sum` — 'SUMX,' the Duke corrects, 'iterates. SUM aggregates. You, peasant, do neither.'  #fortress.dax-sumx
- > `say divide` — 'DIVIDE,' says the Duke, 'handles zero. Unlike you.'  #fortress.dax-divide
- > `say filter` — 'FILTER returns a table,' says the Duke. 'You return nothing.'  #fortress.dax-filter
- > `say measure|a measure|measures` — The Duke waits for the rest of the measure. It does not come. He closes the DAX query view on your fingers.  #fortress.dax-measure
- > `say evaluation context|filter context|row context|context|context transition` — The Duke's eyes narrow. 'Which one?' You do not know. Nobody does, the first six times.  #fortress.dax-context
- > `say all|allexcept|removefilters` — 'ALL removes filters,' says the Duke, 'including the ones keeping you alive.'  #fortress.dax-all
- > `say <(^calculate\b.|calculate.*filter|sumx|var .*return)>` [needs dax.spinner] — The Duke raises one finger. The spinner from your last measure is still spinning. One visual at a time.  #fortress.dax-busy
- > `say <(^calculate\b.|calculate.*filter|sumx|var .*return)>` — The Duke nods. 'Correct.' A spinner appears. The spinner is still there. You could wait.  #fortress.dax-good
- > `say <^(?!calculate\b)(?!.*(var .*return|sumx.|calculated column))(?=.*\b(select|join|evaluate|implicit|bidirectional|bi directional|both directions|cross filter|userelationship|use relationship|sumx|sum|divide|filter|measures?|context|all|allexcept|removefilters|calculate|columns?|relationships?|tables?|model|dax)\b)>` — 'That is not DAX,' says the Duke. 'That is a word that has met DAX.'  #fortress.dax-wrong
- > `wait` [needs dax.spinner=1] — Still spinning. The Duke hums a measure.  #fortress.dax-wait-1
- > `wait` [needs dax.spinner=2] — Still spinning. The Duke checks a sundial. The sundial is also a spinner.  #fortress.dax-wait-2
- > `wait` [needs dax.spinner=3] — Visual has exceeded the available resources. The Duke: 'Correct, though.' Somewhere east, in the Studio, a Card quietly goes back to (Blank).  #fortress.dax-timeout
- > `use measure|a measure|new measure|dax query view|query view` — You write a measure. It works in the card and not in the table. This is normal. This is DAX.  #fortress.write-measure
- > `attack duke|duke of dax|duke of warehouse` — You swing at the Duke. He wraps your fist in CALCULATE and removes its filters. It hurts in every context.  #fortress.attack-duke
- > `use throne|formula bar|seat|chair` — You reach for the throne. It is a formula bar. It autocompletes your hand to SUMX( and you back away.  #fortress.sit-throne
- > `open window|casement|keep window` [needs trial.moat] — You open the window. The moat winks up at you. You wink back. You two have history now.  #fortress.open-window-moat
- > `give scroll|spark scroll|pyspark scroll to duke|duke of dax|duke of warehouse` [needs has:scroll] — 'PySpark?' The Duke holds it at arm's length. 'In the Keep?' You are not thrown in the moat, but it is close.  #fortress.give-scroll-duke
- > `give policy|incremental refresh policy|incremental refresh|refresh policy|incremental to duke|duke of dax|duke of warehouse` [needs has:policy] — 'Ten days at a time? I compute everything, every time, always.' He does not want it. Keep it for the Studio.  #fortress.give-policy-duke
- > `get throne of calculates` — You tug at the formula bar. It expands to three lines. The Duke frowns at the extra lines.  #fortress.get-throne
- > `use query view|code` — You press Format. The DAX indents itself into a staircase. The Duke approves of staircases.  #fortress.use-dax-window
- > `get query view|code` — You cannot take the DAX query view. You can close it, and it will reopen with your last error.  #fortress.get-dax-window
- > `use window|casement|keep window` — You measure the window with your eyes. You would fit. Everyone fits.  #fortress.use-keep-window
- > `get window|casement|keep window` — It is a window. It stays in the wall. You, on the other hand, may not.  #fortress.get-keep-window
- > `use filter pane|filters|pane|filters pane` — You add a filter to the filter pane: "Duke is not (Blank)". The Duke disappears for a moment. Then comes back, annoyed.  #fortress.use-filter-pane
- > `get filter pane|filters|pane|filters pane` — The filter pane is behind the Duke. Everything that happens here is filtered by it. Including you.  #fortress.get-filter-pane
### Phrases (this room / region)
- ~ `/^(open|unlock|use|push|pull|cross|climb|lower|raise|enter|knock( on)?|break|kick( down)?|go through|walk (across|over|through)|try)( on| at| against| over| across| through| into| onto| up| down)?( the| a| that| this)? (throne|formula bar|window|casement|keep window|duke|duke of dax|duke of warehouse|guards)$/` — [dynamic then]  #gate.duke
- ~ `/^(say )?(i('ll| will)? )?(just )?(use|add|make) (a )?calculated column$/` — [dynamic then]  #fortress.moat-words
- ~ `/^write (a |new |some )?(measure|dax)\b/` — You write a measure. It works in the card and not in the table. This is normal. This is DAX.  #fortress.write-measure-words
- ~ `/^sit\b/` — You sit on the formula bar. It autocompletes you. You get up as SUMX(.  #fortress.sit
- ~ `/^(say )?(a )?calculated columns?$/` — [dynamic then]  #fortress.sin-elsewhere
- ~ `/^(say |use |switch to )?direct ?query\b/` — Every click, a query. Every query, a wait. You feel the Keep slow down as you say it.  #fortress.directquery
- ~ `/^(say |use |switch to )?direct ?lake\b/` — Direct Lake. The Keep brightens. Then falls back to DirectQuery for reasons that will be explained in a blog post.  #fortress.directlake
- ~ `/^publish\b/` — Publish to Power BI: which workspace? There is one. It is not the right one. You publish anyway. A red dot appears on something.  #fortress.publish
- ~ `/^(label|sensitivity( label)?|set sensitivity|(set|apply|add) (a )?(sensitivity )?label)\b/` — Highly Confidential. The pie chart is now Highly Confidential. Jeff can still see it.  #fortress.label
- ~ `/^format (the )?(numbers?|string|currency|percent|as)\b/` — 0.00%. Then #,##0. Then, briefly, a date. It is not a date.  #fortress.format-number
- ~ `/^format\b(?! c\b)/` — You open the Format pane. It has 214 options. You change the font. You feel nothing.  #fortress.format
### Items
- throne [throne, formula bar, seat, chair, throne of calculates] — look: A throne carved as a giant formula bar. Across the back, in gold: CALCULATE( — and nothing after it. The Duke is still waiting for the filter argument.
    get: The Duke is on it. He would like you to try.
    blurb: The throne, carved as a formula bar: CALCULATE( and nothing after it. The Duke is still waiting for the filter argument, standing up.
- DAX query view [dax query view, query view, measure, measures, code] — look: A tall DAX query view beside the throne. Line 1: CALCULATE(. Line 2: a blinking cursor. Line 3: a red squiggle under nothing in particular.
    get: You cannot take the DAX query view. You can close it, and it will reopen with your last error.
    blurb: A DAX query view. Line 1: CALCULATE(. Line 3: a red squiggle under nothing in particular, following you.
- window [window, casement, the window, exit, moat, sql, t sql, tsql] — look: It opens onto the moat. It is the fastest exit in the Keep.
    get: It is a window. It stays in the wall. You, on the other hand, may not.
    blurb: The Duke's window, the fastest exit in the Keep. It opens onto the moat from anywhere now, which is worse.
- filter pane [filter pane, filters, pane, filters pane, filter] — look: Behind the throne, a filter pane hangs like a tapestry: "Year is 2019. Region is not (Blank). Jeff is not in the room." All the filters are locked.
    get: The filter pane is behind the Duke. Everything that happens here is filtered by it. Including you.
    blurb: The filter pane, locked: Year is 2019, Region is not (Blank), Jeff is not in the room. Everything you carry is filtered by it, including you.
### NPCs
- the Duke of DAX [duke, duke of dax, the duke, duke of warehouse]
    talk 1: 'CALCULATE(,' says the Duke, and waits. He is waiting for your filter argument. He will wait forever.
    talk 2: 'CALCULATE(,' says the Duke, slower, and waits. You have brought nothing to put in the parentheses.
    talk 3: 'There is one thing,' says the Duke, 'that I will not hear in this chamber. It has two words. It goes in a table. Say it, and see what happens.'
    talk 4: 'Still no sin, Mister Star Schema. Say the two words. The ones every DAX lord despises.'
    ask about pickles: The Duke waits for a filter argument. Whatever that was, it was not one.
    brush-off: The Duke waits for a filter argument. Whatever that was, it was not one.
    knows: calculate, dax, filter, filter context, context, measure, measures, column, columns, calculated column, sin, sql, table, tables, window, throne, moat, warehouse, studio, smell, basement

## The Report Studio (fortress.yard, fortress)
where: The Report Studio. A pie with 31 slices and a Card that says (Blank), which is also your plan.
Exits: w
flask: Look at the Card. Then out-wait it.
### Rules
- > `look card|card visual|blank` [needs !stare.done, !stare.count] — You look at the Card. It shows (Blank). It looks back. Neither of you blinks.  #fortress.stare-start
- > `talk to card|card visual|blank about pie|pie chart|slices` — The Card shows (Pie). The pie shows 3.2%. The Card shows (Blank). The pie shows 3.2%. (You see where this is going.)  #fortress.card-pie
- > `talk to card|card visual|blank about jeff|finance|jeff from finance` — The Card shows (Jeff). Then (Blank). It has no relationship to Jeff; nobody does.  #fortress.card-jeff
- > `talk to card|card visual|blank` [needs !stare.done, !stare.count] — The Card says (Blank). You realize you are now staring at it. It is staring back. This is going to take a while.  #fortress.talk-card-stare
- > `wait` [needs stare.count=1] — Still (Blank). Your eyes water. The Card's do not; it has none.  #fortress.stare-wait-1
- > `wait` (+10) [needs stare.count=2] — The Card blinks. A number appears: 4.2M. It is wrong, but it is a number. You win.  #fortress.stare
- > `go w` (→ fortress.hall) [needs card.stares] — You leave the Studio. The Card loses count of you; it was the only one counting.  #fortress.leave-studio
- > `look card|card visual|blank` (CURSE) [needs stare.done, card.stares=6, !curse.blank] — You stare at the Card past the point of sense. The Card stares back. Then it isn't the Card that is (Blank). Sir Cardinality walks past and does not see you. Nobody will, until you say the two words that put a value in you.  #fortress.curse-blank
- > `look card|card visual|blank` [needs stare.done, card.looked] — 4.2M. Then 4.7M. Then 4.2M. It depends on whether Jeff is in the room.  #fortress.look-card-again
- > `look card|card visual|blank` [needs stare.done] — The Card shows 4.2M. It is wrong, but it is a number. It looks pleased with itself.  #fortress.look-card-done
- > `look card|card visual|blank` [needs stare.count=3] — (Blank). Again. Somewhere upstairs a spinner gave up, and the Card took it personally.  #fortress.look-card-reblank
- > `look card|card visual|blank` — (Blank). You are mid-stare. Do not blink. Wait.  #fortress.look-card-staring
- > `talk to card|card visual|blank` — The Card says (Blank). It is not being rude. It has no measure.  #fortress.talk-card
- > `attack card|card visual|blank` — You hit the Card. It shows (Blank). You cannot hurt what has no measure.  #fortress.attack-card
- > `use card|card visual|blank` [needs !stare.done] — The Card takes no input. It wants staring at: look at it, then wait, then wait again.  #fortress.use-card
- > `use card|card visual|blank` — It shows 4.2M. You cannot use a number that is wrong. Finance can, and will.  #fortress.use-card-done
- > `use policy|incremental refresh policy|incremental refresh|refresh policy|incremental on refresh|big refresh|progress bar|refresh bar` (+15 gives boots) [needs !refresh.done, has:policy] — You hand the Big Refresh a policy: ten days at a time. It blinks. It finishes 2019. Then 2020. Then all of it, in a minute, in small bursting steps. Something falls out of the progress bar: a pair of boots.  #fortress.copy
- > `give policy|incremental refresh policy|incremental refresh|refresh policy|incremental to refresh|big refresh|progress bar|refresh bar` (+15 gives boots) [needs !refresh.done, has:policy] — You give the Big Refresh the policy. It reads it twice, then refreshes ten days at a time: 2019, 2020, all of it, in a minute. Something falls out of the progress bar: a pair of boots.  #fortress.copy-give
- > `use on refresh|big refresh|progress bar|refresh bar` [needs !refresh.done] — The Big Refresh looks at you. 97%. It needs a policy, not encouragement.  #fortress.copy-nopolicy
- > `use refresh|big refresh|progress bar|refresh bar` [needs !refresh.done, not:policy] — Refresh failed: There is not enough memory to complete this operation. There was, before the 31-slice pie.  #fortress.refresh-fail
- > `use refresh|big refresh|progress bar|refresh bar` [needs !refresh.done] — You click Refresh. It was already refreshing. It is now refreshing harder. (The policy in your pocket is not helping from there.)  #fortress.use-refresh
- > `use refresh|big refresh|progress bar|refresh bar` — It refreshes. Ten days, one minute. It is almost boring. You miss the drama.  #fortress.refresh-done
- > `get refresh|big refresh|progress bar|refresh bar` — It is 97% of the way through 2019. Leave it.  #fortress.get-refresh
- > `give policy|incremental refresh policy|incremental refresh|refresh policy|incremental to card|card visual|blank` [needs has:policy] — The Card does not take input. That is the Card's whole thing.  #fortress.give-policy-card
- > `give mug|coffee mug|cup|okayest mug to card|card visual|blank` [needs has:mug] — The Card shows (Mug). Then (Blank).  #fortress.give-mug-card
- > `give license|license card|pro license|pro|pro license card to card|card visual|blank` [needs has:license] — The Card shows (Pro). Then (Blank). It is not impressed either.  #fortress.give-license-card
- > `give to card|card visual|blank` — The Card does not take input.  #fortress.give-card
- > `use bar chart|bar|chart|column chart on pie|pie chart|chart|slices` — You turn the pie into a bar chart. Three stakeholders weep. The fourth sends a thumbs-up.  #fortress.bar-chart-pie
- > `use policy|incremental refresh policy|incremental refresh|refresh policy|incremental on self|me|myself|yourself|you` (CURSE) [needs curse.column, has:policy] — You apply the incremental refresh policy to yourself. Ten days at a time, you become a measure again. It takes a minute. It feels like 2019.  #fortress.undo-column-studio
- > `use policy|incremental refresh policy|incremental refresh|refresh policy|incremental` [needs has:policy] — On what? The Big Refresh is right there, at 97%. Use the policy on the refresh.  #fortress.use-policy-studio
- > `use pie|pie chart|chart|slices|other` — You click a slice. It is 'Other'. Inside 'Other' is more 'Other'.  #fortress.use-pie
- > `get pie|pie chart|chart|slices|other` — You take a slice. It is 'Other'. You put it back.  #fortress.get-pie
- > `use slicers|slicer|slicer stack|stack|dropdowns` — You select 2019 in the Year slicer. Every visual on the page flashes (Blank), then recovers, then does not.  #fortress.use-slicers
- > `get slicers|slicer|slicer stack|stack|dropdowns` — The slicers are synced across nine pages. Move one and eight others move with it.  #fortress.get-slicers
- > `use canvas|report|page|report page` — You add a page. It is called Page 1 (2). You delete it. The name is reserved forever.  #fortress.use-canvas
- > `get canvas|report|page|report page` — The canvas is 1280 by 720. You are not.  #fortress.get-canvas
- > `use bookmarks|bookmarks pane|bookmark pane` — You click "Default (2)". It is identical to "Default", except for one slicer nobody can find.  #fortress.use-bookmarks
- > `get bookmarks|bookmarks pane|bookmark pane` — The bookmarks pane is docked. So are you, now.  #fortress.get-bookmarks
- > `use format pane|formatting|paint roller` — You search the Format pane for "border". It finds twelve. None of them is the border.  #fortress.use-format-pane
- > `get format pane|formatting|paint roller` — The Format pane has 214 options and none of them is "come with you".  #fortress.get-format-pane
- > `use performance analyzer|analyzer|performance|stopwatch` — You start recording and refresh the visuals. The pie takes eleven seconds. The pie is not sorry.  #fortress.use-perf-analyzer
- > `get performance analyzer|analyzer|performance|stopwatch` — You pick up the Performance Analyzer. It records how long that took. You put it down.  #fortress.get-perf-analyzer
- > `use visual header|header|ellipsis|more options|focus mode` — You click focus mode. The pie fills the screen. That was a mistake.  #fortress.use-visual-header
- > `get visual header|header|ellipsis|more options|focus mode` — The visual header only appears when you hover. When you reach for it, it disappears.  #fortress.get-visual-header
- > `use sticky note|note|sticky|post it` — You press the sticky note down firmly. It was already stuck. It is now load-bearing.  #fortress.use-sticky-note
- > `get sticky note|note|sticky|post it` — You peel the corner. Underneath, an older note: DO NOT TOUCH — ALSO JEFF.  #fortress.get-sticky-note
### Phrases (this room / region)
- ~ `/^(open|unlock|push|pull|cross|climb|lower|raise|enter|knock( on)?|break|kick( down)?|go through|walk (across|over|through)|try)( on| at| against| over| across| through| into| onto| up| down)?( the| a| that| this)? (card|card visual|big refresh|refresh|progress bar|refresh bar)$/` — [dynamic then]  #gate.studio
- ~ `/^refresh\b/` — Refresh failed: There is not enough memory to complete this operation. There was, before the 31-slice pie.  #fortress.refresh-words
- ~ `/^(eat|taste|bite)( a slice of| the| a)? pie( chart)?$/` — You eat a slice. It is 'Other'. It tastes like the other eleven 'Other's.  #fortress.eat-pie
- ~ `/^add (a |another )?slicers?\b/` — You add a slicer. There are now nine slicers. The page loads in the time it takes to regret this.  #fortress.add-slicer
- ~ `/^fix (the )?pie\b/` — A pie with 31 slices. Twelve of them are 'Other'. You could fix it. You could also leave it and let it be someone else's problem in Q3.  #fortress.fix-pie
- ~ `/^(align|distribute)\b/` — You align the visuals. Eleven pixels. Then ten. Then eleven again. It is never done.  #fortress.align
- ~ `/^((create|add) (a )?)?bookmark$/` — You create a bookmark. It captures the current state, including the part that is broken.  #fortress.bookmark
- ~ `/^(conditional formatting|conditionally format|(add|apply) conditional formatting)\b/` — You conditionally format the card. It is red. It was always going to be red.  #fortress.conditional
- ~ `/^((run|start|open) )?(performance analyzer|analy[sz]e performance)\b/` — Performance Analyzer runs. The slowest visual is the one Jeff asked for.  #fortress.perf
- ~ `/^((use|add|open) (the )?q ?& ?a( visual)?|q ?& ?a\b|ask (?!(the )?(card|jeff|copilot)\b)\S)/` — Q&A shows sales by month. You asked about regions. It is very confident.  #fortress.qna
- ~ `/^(say )?(a )?calculated columns?$/` — [dynamic then]  #fortress.sin-elsewhere
- ~ `/^(say |use |switch to )?direct ?query\b/` — Every click, a query. Every query, a wait. You feel the Keep slow down as you say it.  #fortress.directquery
- ~ `/^(say |use |switch to )?direct ?lake\b/` — Direct Lake. The Keep brightens. Then falls back to DirectQuery for reasons that will be explained in a blog post.  #fortress.directlake
- ~ `/^publish\b/` — Publish to Power BI: which workspace? There is one. It is not the right one. You publish anyway. A red dot appears on something.  #fortress.publish
- ~ `/^(label|sensitivity( label)?|set sensitivity|(set|apply|add) (a )?(sensitivity )?label)\b/` — Highly Confidential. The pie chart is now Highly Confidential. Jeff can still see it.  #fortress.label
- ~ `/^format (the )?(numbers?|string|currency|percent|as)\b/` — 0.00%. Then #,##0. Then, briefly, a date. It is not a date.  #fortress.format-number
- ~ `/^format\b(?! c\b)/` — You open the Format pane. It has 214 options. You change the font. You feel nothing.  #fortress.format
- ~ `/^(open|click( on)?|select|switch to|view|go to|visit|tap|show) (the )?(other page|page 2|second page|400[- ]visual (page|report)|400 visuals?|page two|do not open|page 2 \(?do not open\)?|(other |second |warm )?tab)$/` ☠ — You open the other page. 400 visuals. Your laptop fan hits a note only dogs and Throttlor can hear. The fan wins. The tab said DO NOT OPEN. You read it as a dare.  #death.400
### Items
- the Big Refresh [refresh, big refresh, progress bar, refresh bar, the refresh, bar] — look: A progress bar, 97%, since 2019. It is refreshing everything, every time, all at once. It needs smaller steps. A sticky note on it reads DO NOT TOUCH — JEFF. A logo on the bar: Refreshr™.
    get: It is 97% of the way through 2019. Leave it.
    blurb: The Big Refresh, a progress bar at 97% since 2019. DO NOT TOUCH — JEFF, and you did considerably more than touch it.
- pie chart [pie, pie chart, chart, slices, other] — look: A pie with 31 slices. Twelve of them are 'Other'. You could fix it. You could also leave it and let it be someone else's problem in Q3.
    get: The pie is 31 slices. You cannot take it in one trip, and nobody wants a slice.
    blurb: A pie with 31 slices, twelve of them 'Other'. Nobody wanted a slice, and you took the whole pie.
- slicers [slicers, slicer, slicer stack, stack, dropdowns] — look: A stack of eight slicers: Year, Month, Region, Region (Old), Product, Colour, "Test", and one with no field at all. It filters everything, including itself.
    get: The slicers are synced across nine pages. Move one and eight others move with it.
    blurb: Eight slicers, synced across nine pages. Move one and eight others move with it, and you moved all of them.
- canvas [canvas, report, page, report page, wall] — look: A report canvas the size of a wall. The pie, the Card, the slicers, and a lot of white space someone called "breathing room" in a design review.
    get: The canvas is 1280 by 720. You are not.
    blurb: A report canvas, 1280 by 720, with a lot of breathing room. It does not fit, and neither did the design review.
- Page 2 [page two, tab, second tab, other tab, warm tab, 400 visuals, do not open, page 2 do not open] — look: A second tab: 'Page 2 (do not open)'. 400 visuals, one per product, each with its own slicer. The tab is warm to the touch.
    get: The tab is warm. You leave it alone. For now.
    blurb: Page 2 (do not open). 400 visuals, one per product, warm to the touch, and getting warmer.
- bookmarks pane [bookmarks, bookmark, bookmarks pane, bookmark pane] — look: The bookmarks pane: "Default", "Default (2)", "Jeff view", "Jeff view DO NOT UPDATE", and "Bookmark 14". None of them restore the thing you think they do.
    get: The bookmarks pane is docked. So are you, now.
    blurb: The bookmarks pane: Default, Default (2), Jeff view DO NOT UPDATE, and Bookmark 14. None of them restore the thing you think they do.
- Format pane [format pane, formatting, format, paint roller] — look: The Format pane. 214 options, grouped by a logic known only to the Format pane. Somewhere in here is the setting that turns the border off.
    get: The Format pane has 214 options and none of them is "come with you".
    blurb: The Format pane, 214 options. None of them was 'come with you', and it came with you.
- Performance Analyzer [performance analyzer, analyzer, performance, stopwatch] — look: The Performance Analyzer: a stopwatch on a stand. Last recording: the pie, 11,402 ms. The Card, 3 ms. The slicer with no field, somehow, 9 seconds.
    get: You pick up the Performance Analyzer. It records how long that took. You put it down.
    blurb: The Performance Analyzer, a stopwatch on a stand. It recorded how long it took you to pick it up: 9 seconds, somehow.
- visual header [visual header, header, ellipsis, more options, focus mode] — look: A visual header, hovering above the pie: filter icon, focus mode, and an ellipsis that opens a menu containing, among other things, "Export data".
    get: The visual header only appears when you hover. When you reach for it, it disappears.
    blurb: A visual header. It only appears when you hover, and you are hovering, so it is here.
- sticky note [sticky note, note, sticky, post it, post-it, jeff, finance] — look: A yellow sticky note on the Big Refresh: DO NOT TOUCH — JEFF. Underneath, smaller: "(it is almost done)". The ink is from 2019.
    get: It says DO NOT TOUCH — JEFF. You do not touch it.
    blurb: A sticky note: DO NOT TOUCH — JEFF. Underneath, smaller, '(it is almost done)', in ink from 2019.
### NPCs
- the Card visual [card, card visual, blank, the card]
    talk 1: The Card says (Blank). You realize you are now staring at it. It is staring back. This is going to take a while.
    talk 2: (Blank). Again. The Card is not stalling; it has nothing to stall with.
    talk 3: The Card shows (Blank). It will show a number if you look at it and then wait. Twice. Blink and it wins.
    talk 4: Still (Blank), Mister Star Schema.
    ask about pickles: The Card says (Blank). You realize you are now staring at it. It is staring back. This is going to take a while.
    brush-off: The Card shows (Blank). It is its answer to that, and to everything.
    knows: blank, measure, measures, number, value, 4.2m, refresh, big refresh, policy, incremental, stare, staring, wait, visual, report

## Foothills (peaks.foothills, peaks)
where: The Foothills. The air's thin and so is the excuse you'll give Finance.
Exits: w, n, e
flask: Go north into the Keep. You need to smell like a Warehouse, and the Keep has a moat for that.
### Rules
- > `use sign|peaks sign|warning sign` — You lean on the sign. It leans back. Neither of you is going anywhere fast.  #peaks.climb-sign
- > `use flat rock|rock|flat stone|stone on sign|peaks sign|warning sign|signboard` [needs has:flat-rock] — You tap the sign with the rock. It says INTERACTIVE OPERATIONS MAY BE DELAYED. The tap arrives a moment later.  #peaks.rock-sign
### Phrases (this room / region)
- ~ `/^(climb|scale|ascend)( the| up the)? (mountain|peaks?|capacity peaks)$/` — You climb. The mountain bills you per vertical second. You stop at the first ledge to check the invoice.  #peaks.climb
### Items
- sign [peaks sign, warning sign, signboard] — look: CAPACITY PEAKS — INTERACTIVE OPERATIONS MAY BE DELAYED. BACKGROUND OPERATIONS WILL BE SMOOTHED OVER 24 HOURS. PLEASE DO NOT FEED THE DRAGON. BRING CAPACITUDE.
    get: The sign is bolted to the mountain. The mountain is bolted to the capacity.
    blurb: CAPACITY PEAKS — INTERACTIVE OPERATIONS MAY BE DELAYED. You took the warning, and the delay stayed on the mountain, waiting for you.
- flat rock [rock, flat stone, stone] (gettable) — look: A flat rock from the Foothills. It is exactly as useful as it looks, which is the most honest thing in the Peaks.
    again: You already have the rock. It was a rock then. Rocks are stable. Unlike you.
    blurb: A flat rock from the Foothills. Exactly as useful as it looks, which is the most honest thing in the Peaks.

## Throttling Pass (peaks.pass, peaks)
where: Throttling Pass. Every step costs more than the last one, like a consultant, or you, eventually.
Exits: w, n
flask: Go north, slowly. Boots would help. The Keep has some, in the Report Studio, if you want to walk all the way back for them.
### Rules
- > `drink capacityade|capacity ade|bottle|sports drink|ade|drink|the ade|label` (☠) — Electrolytes. Autoscale. Your heart hits 64 CUs and the bill arrives before you do. The label said not on a capacity. Everything in this realm is on a capacity. That is what the dragon is FOR.  #death.capacityade
- > `get rock|rocks|stone|air` — You reach for a rock. The reach is queued. The rock is delivered to you 24 hours later, smoothed.  #peaks.pass-rocks
- > `read receipt|cu receipt|bill` — CU consumption: 1 step, 400 CU-seconds. 1 reach for a rock, queued. 1 thought about turning back, 0 CU-seconds; nobody bills for that, because nobody has ever done it.  #peaks.read-receipt
### Phrases (this room / region)
- ~ `/^(open|unlock|use|push|pull|cross|climb|lower|raise|enter|knock( on)?|break|kick( down)?|go through|walk (across|over|through)|try)( on| at| against| over| across| through| into| onto| up| down)?( the| a| that| this)? (sign|delay|interactive delay|pass|throttling pass|air|throttle|throttling|rocks?)$/` — [dynamic then]  #gate.pass
- ~ `/^(pay|pay (the )?(receipt|bill)|settle up)$/` — You try to pay the receipt. The Pass accepts Capacity Units only. You have a Pro license and, at this altitude, opinions.  #peaks.pay
- ~ `/^(run|sprint|hurry( up)?|rush|jog|dash|go faster|walk faster|speed up)$/` — You try to hurry. The Pass queues the hurry and delivers it tomorrow, as a stroll.  #peaks.hurry
### Items
- rocks [rock, rocks, stone, stones, air, scree] — look: Rocks. Each one takes a little longer to look at than the last. The air is thin and metered.
    get: You lift a rock. It is billed per second. You put it down.
    blurb: Rocks from the Foothills, metered. Each one costs more to hold than the last, and you are holding all of them.
- receipt [cu receipt, bill] (gettable) — look: A receipt, blowing down the Pass. CU consumption: 1 step, 400 CU-seconds. Smoothed over 24 hours. Payable now.
    again: You already have the receipt. Picking it up again is a second step. That is another 400 CU-seconds. Itemized.
    blurb: A receipt, blowing down the Pass. 1 step, 400 CU-seconds, smoothed over 24 hours, payable now.
- CapacityAde [capacity ade, bottle, sports drink, ade, drink, the ade, label] (gettable) — look: A bottle of CapacityAde. Electrolytes and autoscale. 'Now with 64 CUs.' The label warns against drinking it while standing on a capacity.
    again: You already picked the CapacityAde up on the Pass, and the reach was billed per second. Do not drink it. I know you will.
    blurb: CapacityAde. Electrolytes, autoscale, and 64 CUs in a bottle. The label says not to drink it standing on a capacity, which is everywhere.

## Bursting Ledge (peaks.ledge, peaks)
where: The Bursting Ledge. A door that checks three things about you. It's found more than three.
Exits: s, n
flask: The door wants the Worthy Three: wear the hoodie, smell like the moat, hold the key. Go south and get started.
### Rules
- > `look door|shrine door|great door|shrine|sigils|sigil` (+5) [needs trial.hoodie, trial.moat, trial.key, !shrine.open] — The three sigils blaze. Hoodie. Stink. Key. The door grinds open. You are Worthy, apparently.  #peaks.shrine-door
- > `open door|shrine door|great door|shrine|sigils|sigil` (+5) [needs trial.hoodie, trial.moat, trial.key, !shrine.open] — The three sigils blaze. Hoodie. Stink. Key. The door grinds open. You are Worthy, apparently.  #peaks.shrine-door-open
- > `look sigils|sigil` — A great door carved with three sigils: a HOODIE, three wavy STINK LINES, and a KEY. The hoodie sigil is dark. The stink-lines sigil is dark. The key sigil is dark.  #peaks.look-sigils
- > `go n` (+5 → peaks.shrine) [needs trial.hoodie, trial.moat, trial.key, !shrine.open] — The three sigils blaze. Hoodie. Stink. Key. The door grinds open. You are Worthy, apparently.  #peaks.shrine-door-go
- > `go n` [needs !shrine.open] — The door does not move. Still dark: the HOODIE sigil (look like an Engineer); the STINK LINES sigil (smell like a Warehouse); the KEY sigil (hold the Gateway Key).  #peaks.door-locked
- > `open door|shrine door|great door|shrine|sigils|sigil` [needs shrine.open] — Yeah, totally! Except it's already open, you moron. Try: north.  #peaks.open-open
- > `read door|shrine door|great door|shrine|sigils|sigil` — WORTHY ONLY. Below, smaller: 'and no interactive delay'. A great door carved with three sigils: a HOODIE, three wavy STINK LINES, and a KEY. The hoodie sigil is dark. The stink-lines sigil is dark. The key sigil is dark.  #peaks.read-door
- > `use carabiner|clip|karabiner on door|shrine door|great door|shrine|sigils|sigil` [needs has:carabiner] — You clip onto the door. You are now attached to a door you cannot open. This is called governance.  #peaks.carabiner-door
- > `use carabiner|clip|karabiner` [needs has:carabiner] — You clip the carabiner to yourself. Rated F64. You are, at best, F2.  #peaks.use-carabiner
- > `attack door|shrine door|great door|shrine|sigils|sigil` — You kick the door. The door is a mountain. The mountain does not notice.  #peaks.attack-door
### Phrases (this room / region)
- ~ `/^(open|unlock|use|push|pull|cross|climb|lower|raise|enter|knock( on)?|break|kick( down)?|go through|walk (across|over|through)|try)( on| at| against| over| across| through| into| onto| up| down)?( the| a| that| this)? (door|shrine door|shrine|great door|sigils?|lock)$/` — [dynamic then]  #gate.ledge
### Items
- door [shrine door, sigils, sigil, great door, shrine] — look: A great door carved with three sigils: a HOODIE, three wavy STINK LINES, and a KEY. The hoodie sigil is dark. The stink-lines sigil is dark. The key sigil is dark. The door refreshered its sigils while you were not looking.
    get: The door is the mountain. You cannot take the mountain.
    blurb: The shrine door, three sigils and all. You took the door off the mountain, and the mountain is thinking about it.
- carabiner [clip, karabiner] (gettable) — look: A carabiner stamped F64. Rated for any capacity except yours.
    again: You already unclipped the carabiner from the Ledge, under the three sigils. It was clipped to nothing then. It's clipped to nothing now.
    blurb: A carabiner stamped F64. Rated for any capacity except yours.

## The Shrine (peaks.shrine, peaks)
where: The Shrine. A dragon, a model, and a peasant who read the walkthrough. Two of you are impressive.
Exits: s
flask: Answer the dragon. Say the one true model: one fact table, several dimensions, two words. Do not attack him. People always attack him.
### Rules
- > `say` [needs curse.column, !dragon.gone] — Throttlor sniffs. 'A calculated column. I don't negotiate with columns. Come back when you're a measure.'  #peaks.column-say
- > `talk to dragon|throttlor|throttlor the capacity dragon|capacity dragon|the dragon` [needs curse.column, !dragon.gone] — Throttlor sniffs. 'A calculated column. I don't negotiate with columns. Come back when you're a measure.'  #peaks.column-talk
- > `talk to dragon|throttlor|throttlor the capacity dragon|capacity dragon|the dragon about model|the model|golden semantic model|semantic model|the golden semantic model|one true model|the one true model` [needs !dragon.gone, !curse.blank] — "WHO DARES— oh. Oh no. You smell like a Warehouse." The dragon gags. "Answer me this, peasant: WHAT IS THE ONE TRUE MODEL?"  #peaks.dragon-model
- > `talk to dragon|throttlor|throttlor the capacity dragon|capacity dragon|the dragon about warehouse|the warehouse|fabric warehouse|moat|the moat|smell` [needs !dragon.gone, !curse.blank] — 'Don't bring up the Warehouse,' says Throttlor. 'You ARE the Warehouse. I can taste it from here. It tastes like a view nobody documented.'  #peaks.dragon-warehouse
- > `talk to dragon|throttlor|throttlor the capacity dragon|capacity dragon|the dragon about <anything else>` [needs !dragon.gone, talk.throttlor, !curse.blank] — Throttlor exhales a neat 30-second puff. "Off-topic questions are smoothed over 24 hours, Power Query Pete. Come back tomorrow."  #peaks.dragon-brushoff
- > `talk to dragon|throttlor|throttlor the capacity dragon|capacity dragon|the dragon` [needs !dragon.gone] — "WHO DARES— oh. Oh no. You smell like a Warehouse." The dragon gags. "Answer me this, peasant: WHAT IS THE ONE TRUE MODEL?"  #peaks.talk-dragon
- > `attack dragon|throttlor|throttlor the capacity dragon|capacity dragon|the dragon` (☠) [needs !dragon.gone] — You charge Throttlor. He breathes a single, precise jet of flame. Your refresh has been throttled. Permanently. You knew you were supposed to TALK to him, right? You read the ledger.  #death.dragon
- > `use flat rock|rock|flat stone|stone on dragon|throttlor|throttlor the capacity dragon|capacity dragon|the dragon` [needs !dragon.gone, has:flat-rock] — You throw a rock at a capacity dragon. It bills you for the throw. Itemized. With a surcharge for aim.  #peaks.rock-dragon
- > `give kpi|laminated kpi to dragon|throttlor|throttlor the capacity dragon|capacity dragon|the dragon` [needs !dragon.gone, has:kpi] — '(Blank)?' Throttlor is briefly delighted. Then remembers he is a dragon. He hands it back with one claw. 'Answer the question, peasant.'  #peaks.kpi-dragon
- > `give receipt|cu receipt|bill to dragon|throttlor|throttlor the capacity dragon|capacity dragon|the dragon` [needs !dragon.gone, has:receipt] — Throttlor reads the receipt, adds a line in smoke, and hands it back. You now owe more. 'Consumption,' he purrs, 'is a lifestyle.'  #peaks.receipt-dragon
- > `say star schema|a star schema|the star schema|star|star-schema|kimball` (+10 CURSE) [needs !dragon.gone] — 'STAR… SCHEMA?' Throttlor recoils. 'One fact table? Conformed dimensions? Single-direction filters?' He shrieks, deflates like a paused capacity, and drifts out of the Shrine, whistling faintly. He does not come back.  #peaks.dragon
- > `say calculate|dax|measure|a measure` [needs !dragon.gone] — Throttlor yawns. Context transition means nothing to a dragon. You have been DAXxed by a dragon. Add it to the bill.  #peaks.dragon-calculate
- > `say flat table|one big table|obt|wide table|a flat table|one table|snowflake|snowflake schema` [needs !dragon.gone] — Throttlor purrs. 'Yes. YES. Feed me.' The pedestal glows brighter. You have made things worse.  #peaks.dragon-flat
- > `say direct lake|import|directquery|direct query` [needs !dragon.gone] — 'That is a storage mode, peasant, not a model.' Throttlor sighs a small, patient flame.  #peaks.dragon-direct-lake
- > `say <\S>` [needs !dragon.gone] — '?' Throttlor bills you for the syllables. 'Wrong, Power Query Pete. Two words. One of them is up in the sky.'  #peaks.dragon-wrong
- > `give capacityade|capacity ade|bottle|sports drink|ade|drink|the ade|label to dragon|throttlor|throttlor the capacity dragon|capacity dragon|the dragon` [needs !dragon.gone, has:capacityade] — Throttlor sniffs the CapacityAde and hands it back. 'I don't drink my own product,' he says. 'Neither should you.'  #peaks.ade-dragon
- > `use model|golden semantic model|semantic model|golden model|the model` [needs !dragon.gone] — You reach past a dragon for the Model. He clears his throat, in smoke.  #peaks.reach-model
- > `get model|golden semantic model|semantic model|golden model|the model` [needs !dragon.gone] — You reach for the Model. Throttlor's tail slaps your hand away without his even looking. 'Answer the question, peasant.'  #peaks.get-model-guarded
- > `look model|golden semantic model|semantic model|golden model|the model` [needs !dragon.gone] — The Golden Semantic Model glows behind the dragon. From here it looks perfect. From here.  #peaks.look-model-guarded
- > `get model|golden semantic model|semantic model|golden model|the model` (+5 gives model WIN) [needs dragon.gone] — You lift the Golden Semantic Model. It is one table. 412 columns. The first is named Column1. There is a measure named 'Measure 2 (copy)'. Half the columns are dates stored as text. There is a column called 'Notes' that contains, in row 8,041, the phrase 'ask Jeff'. You weep. Then you carry it down the mountain anyway. The village's refreshes run that night, slow but unthrottled, and the people cheer, and nobody asks what the Model looks like inside. Congratulations. You won. Nobody's ever gotten the Model down the mountain, and you did it with a Pro license. Way to go. THE END.  #peaks.model
- > `read model|golden semantic model|semantic model|golden model|the model` (+5 gives model WIN) [needs dragon.gone] — You lift the Golden Semantic Model. It is one table. 412 columns. The first is named Column1. There is a measure named 'Measure 2 (copy)'. Half the columns are dates stored as text. There is a column called 'Notes' that contains, in row 8,041, the phrase 'ask Jeff'. You weep. Then you carry it down the mountain anyway. The village's refreshes run that night, slow but unthrottled, and the people cheer, and nobody asks what the Model looks like inside. Congratulations. You won. Nobody's ever gotten the Model down the mountain, and you did it with a Pro license. Way to go. THE END.  #peaks.read-model
### Phrases (this room / region)
- ~ `/^(open|unlock|use|push|pull|cross|climb|lower|raise|enter|knock( on)?|break|kick( down)?|go through|walk (across|over|through)|try)( on| at| against| over| across| through| into| onto| up| down)?( the| a| that| this)? (dragon|throttlor|capacity dragon|throttlor the capacity dragon)$/` — [dynamic then]  #gate.dragon
- ~ `/^(throw|toss|chuck|hurl|lob)\b.*\b(rock|stone)\b.*\b(dragon|throttlor)\b/` — You mime throwing a rock. You do not have a rock. The Foothills had one. The Foothills are very far away now.  #peaks.throw-rock
- ~ `/^(throw|toss|chuck|hurl|lob)\b.*\b(rock|stone)\b/` — You mime throwing a rock. You do not have a rock. The Foothills had one. The Foothills are very far away now.  #peaks.throw-rock-elsewhere
### Items
- golden semantic model [model, semantic model, golden model, the model] (gettable) — look: The Golden Semantic Model. It glows. Somehow it also has a 'Column1'.
    again: You are holding the Model. You have been holding it since the mountain. Put it down and the village refreshes stop.
    blurb: The Golden Semantic Model. One table, 412 columns, and a note in row 8,041 that says "ask Jeff." You carried it anyway.
- pedestal [plinth, stand, altar] — look: A pedestal, plinth-grade. It has held the Model since the last capacity outage; before that, it held a different Model, also golden, also one table.
    get: The pedestal holds the Model. Take the Model; leave the furniture.
    blurb: The Shrine's pedestal, plinth-grade. You carried off the furniture and left the Model, which is very on brand.
- hard hat [hardhat, hat, helmet, surge protection, yellow hat] — look: A hard hat, dragon-sized, yellow. SURGE PROTECTION on the front, in stencil. It protects the capacity from the dragon. Nothing protects you.
    get: It is on a dragon. You are not taking a hat off a dragon.
    blurb: A dragon-sized hard hat, SURGE PROTECTION in stencil. It protected the capacity from the dragon, and it does nothing for you.
### NPCs
- Throttlor [dragon, throttlor the capacity dragon, capacity dragon, the dragon]
    talk 1: "WHO DARES— oh. Oh no. You smell like a Warehouse." The dragon gags. "Answer me this, peasant: WHAT IS THE ONE TRUE MODEL?"
    talk 2: "WHAT IS THE ONE TRUE MODEL," Throttlor repeats, slower, with more smoke. He does not like repeating himself. He is billing you for it.
    talk 3: "Two words, peasant. One fact table. Some dimensions. A shape. Sir Cardinality said it three times; I heard him from here."
    talk 4: "Still no answer, Mister Star Schema. The question has not changed. Neither has the fee."
    ask about pickles: "I bill for questions like that," says Throttlor. "Ask me about the model."
    brush-off: "I bill for questions like that," says Throttlor. "Ask me about the model."
    knows: model, one true model, semantic model, golden semantic model, star schema, schema, star, snowflake, fact table, fact, dimension, dimensions, answer, question, kimball, cardinality, warehouse

## Sheet1 (excel.sheet1, excel)
where: Sheet1. Jeff's desk. Grid paper to the horizon and a SUM that's wrong to four decimal places.
Exits: e, n, out
flask: Talk to Jeff first. `talk to jeff`.
### Rules
- > `give jeff|finance|jeff from finance` [needs sq.excel.done] — "Okay. The report was right. Don't tell anyone I said that."  #excel.jeff-done-give
- > `give pivot|pivottable|pivot table|pivottable1|it to jeff|finance|jeff from finance` [needs sq.excel.done] — "Okay. The report was right. Don't tell anyone I said that."  #excel.jeff-done-show
- > `talk to <^(jeff|finance)\b> about <anything else>` — "The report says 4.2M. My export says 4.7M. One of them is lying and I've decided it's the report."  #excel.ask-jeff-unknown
- > `give pivot|pivottable|pivot table|pivottable1|it to jeff|finance|jeff from finance` (+20 bonus → back) [needs excel.dim, excel.measure, excel.filter, !sq.excel.done] — Jeff looks at 4.2M. Jeff looks at his export. Jeff says the words no one at Finance has ever said: "Okay. The report was right."  #excel.show-jeff-done
- > `give jeff|finance|jeff from finance` (+20 bonus → back) [needs excel.dim, excel.measure, excel.filter, !sq.excel.done] — Jeff looks at 4.2M. Jeff looks at his export. Jeff says the words no one at Finance has ever said: "Okay. The report was right."  #excel.show-jeff-done-bare
- > `talk to <^(jeff|finance)\b>` (+20 bonus → back) [needs excel.dim, excel.measure, excel.filter, !sq.excel.done] — Jeff looks at 4.2M. Jeff looks at his export. Jeff says the words no one at Finance has ever said: "Okay. The report was right."  #excel.show-jeff-done-talk
- > `give pivot|pivottable|pivot table|pivottable1|it to jeff|finance|jeff from finance` — Jeff squints. "It still says 4.7M. It's my export with a pivot on top of it; of course it does." Talk to Jeff first. `talk to jeff`.  #excel.show-jeff-early
- > `give jeff|finance|jeff from finance` — Jeff squints. "It still says 4.7M. It's my export with a pivot on top of it; of course it does." Talk to Jeff first. `talk to jeff`.  #excel.show-jeff-early-bare
- > `talk to <^(jeff|finance)\b> about report|the report|badge` [needs !sq.excel.done] — 'The report,' says Jeff. 'It has a badge. I have a spreadsheet. A spreadsheet is a badge you make yourself.'  #excel.jeff-report
- > `talk to <^(jeff|finance)\b> about export|the export|csv|his export|sales export` [needs excel.jeff.asked] — 'My export,' says Jeff, and turns to it. 'You said 4.7.' It says 4.7. 'Then say it to the report.' It doesn't. He turns back to you, betrayed by a CSV.  #excel.jeff-export
- > `use tissue box|tissues|tissue|kleenex` — You hand Jeff a tissue. He blows his nose into a printout of the total instead. It rounds up.  #excel.use-tissues
- > `talk to <^(jeff|finance)\b>` — "It's 4.7. The report says 4.2. I exported the visual, I summed it, it's 4.7. Look, I trust my export. IT said something about 'Analyze in Excel' — that it connects to the actual model. It's on the Data tab. North. I never go north."  #excel.ask-jeff
- > `look second monitor|right monitor|other monitor` — The report. Net Sales, Northeast, this year: $4.2M. Certified. It has a little badge and everything.  #excel.look-second-monitor
- > `use analyze in excel|analyze|connection|odc|data connection|excel|in excel` [needs excel.connected] — Already connected. Sales (Certified). The pivot is east.  #excel.analyze-from-sheet1-done
- > `use` [needs excel.connected] — Already connected. Sales (Certified). The pivot is east.  #excel.analyze-bare-from-sheet1-done
- > `use analyze in excel|analyze|connection|odc|data connection|excel|in excel` (→ excel.data) — You click the Data tab. Jeff watches you go north like you are leaving the country.  #excel.analyze-from-sheet1
- > `use` (→ excel.data) — You click the Data tab. Jeff watches you go north like you are leaving the country.  #excel.analyze-bare-from-sheet1
- > `use pivot|pivot table|pivottable|pivottable1|sales region|sales region column|sales region in rows|sales region to rows|net sales|net sales measure|net sales in values|net sales to values|year|by year|current year|by current year|is current year|by is current year|year 2025|by year 2025|calendar|active|by active|filter|to 2025|2025|year filter|year in filters|year to filters|year filters|region|region a|region b|geography|legacy|geography legacy|sales amount|amount|returns|sales|gross sales|sales ytd|ytd|measure 2|measure 2 copy|product|salesperson|date|quarter|fields|field|field list|all fields|all the fields|them|those` — Right idea, wrong sheet. PivotTable1 is east. The fields go in there, not here.  #excel.pivot-from-sheet1
- > `look pivot|pivot table|pivottable|pivottable1|fields|field|field list|field pane|pane|pivottable fields` — It's on PivotTable1, east. Everything pivot-shaped is.  #excel.look-pivot-from-sheet1
- > `say sign in|signin|login|log in` — Right idea, wrong tab. The sign-in dialog is on the Data tab, north. Nothing here takes a license.  #excel.sign-in-from-sheet1
- > `use license|card|pro license|license card|pro` — Right idea, wrong tab. The sign-in dialog is on the Data tab, north. Nothing here takes a license.  #excel.license-from-sheet1
### Phrases (this room / region)
- ~ `/^(goal|objective|what do i do|what am i doing|what is the goal|why am i here)\??$/` — Jeff's export says $4.7M. The report says $4.2M. Jeff: "I'll keep what's in my export. I trust it more." Help Jeff find the truth. +20 bonus. EXIT leaves any time. Talk to Jeff first. `talk to jeff`.  #goal.excel
- ~ `/^(say )?no filters?$/` — Jeff nods approvingly. That is not a good sign.  #excel.no-filter
- ~ `/^(show me (a|the) table|open excel)$/` — You are already in Excel. Any further in and it's VBA. The table is PivotTable1, east of Sheet1.  #excel.already-in
- ~ `/^(?:(?:go|click|switch)(?: over| back)?(?: to| on)? |open |select )?(?:the )?(?:pivot|pivot ?table|pivottable1?)(?: sheet| tab)?$/` — [dynamic then]  #excel.go-pivot
- ~ `/^(?:(?:(?:go|click|switch)(?: over| back)?(?: to| on)? |open |select )?(?:the )?data(?: tab| sheet)?|analy[sz]e in excel|connect)$/` — [dynamic then]  #excel.go-data
- ~ `/^(?:(?:go|click|switch)(?: over| back)?(?: to| on)? |open |select )?(?:the )?(?:sheet ?1|jeff'?s sheet|first sheet)(?: sheet| tab)?$/` — [dynamic then]  #excel.go-sheet1
- ~ `/^((show|talk to|talk with|speak to|speak with|ask|tell|chat with|greet) jeff\b.*|give .+ to jeff)$/` — [dynamic then]  #excel.jeff-elsewhere
- ~ `/^(make|build|new|create|insert|add|do)( me)?( a| the| new| another)* (pivot|pivot ?table|pivottable1?)$/` — [dynamic then]  #excel.make-pivot
- ~ `/^(compare|look at (the )?difference|diff|what('s| is) the difference|why (is it|are they) different)\??$/` — Export: 4,712,331. Report: 4.2M. Pivot: 4.7M, because it is the export wearing a hat. Connect the model and build a real one; the difference explains itself.  #excel.compare
- ~ `/^(show jeff (the )?(pivot|pivot ?table|pivottable1?|numbers?|total)|tell jeff\b.*)$/` — [dynamic then]  #excel.show-jeff-words
- ~ `/^save( the)? (workbook|sheet|file|export|spreadsheet)$/` — Saved as Sales_export (4).csv. The (3) was the good one. It always is.  #excel.save
- ~ `/^(look at|examine|x|click|select)( cell)? a1$/` — Cell A1. Blinking. Somewhere in it, Clippy is composing a suggestion.  #excel.cell-a1
- ~ `/^(fields|field list|field pane|pivottable fields|list fields|show fields)$/` — It's on PivotTable1, east. Everything pivot-shaped is.  #excel.fields
### Items
- export [sales_export (3).csv, sales_export 3 csv, sales_export, csv, xlsx, spreadsheet, sheet, sales export, his export, sales data] — look: Sales_export (3).csv. Every row of the visual, plus the Total row, plus three years of history, plus Returns counted as sales. SUM: 4,712,331. Jeff trusts it. It has no more capacitude.
    get: It is 1,048,576 rows. Jeff has it pinned. Jeff pins everything.
    blurb: Sales_export (3).csv, 1,048,576 rows, every one of them trusted. Row 1,048,577 is where the truth would have gone.
- monitor [monitors, screen, first monitor] — look: Two monitors. The left one has the export and a SUM. The right one has the report and is angled away, the way you angle a mirror you have stopped trusting.
    get: Two monitors, one desk, zero chance you are walking off with either.
    blurb: Two monitors and one desk. The right one is angled away, the way you angle a mirror you have stopped trusting.
- report [published report, certified report] — look: The report. Net Sales, Northeast, this year: $4.2M. Certified. It has a little badge and everything.
    get: It is on Jeff's second monitor. He has turned it away. You are not turning it back.
    blurb: The certified report, off Jeff's second monitor. He turned it away for a reason, and the reason is 4.2.
- ribbon [toolbar, menu, tabs, data tab, data] — look: Home · Insert · Data · Analyze in Excel. The last one is new. Jeff has not clicked it.
    get: The ribbon is load-bearing. It holds up the whole of Excel.
    blurb: The Excel ribbon: Home, Insert, Data, Analyze in Excel. It is load-bearing, and Excel is sagging without it.
- desk [jeff's desk, his desk] — look: Jeff's desk. A keyboard with the F2 worn smooth, a mug that says WORLD'S OKAYEST ANALYST (no, a different one), and seventeen printouts of the same total.
    get: The desk is Finance property. So, technically, is Jeff.
    blurb: Jeff's desk: F2 worn smooth, a different WORLD'S OKAYEST ANALYST mug. Finance property, and so, technically, is Jeff.
- tissue box [tissues, tissue, box of tissues, kleenex] — look: A tissue box. Half empty. It was full this morning, before the report said 4.2.
    get: Jeff is going to need those. Leave them.
    blurb: Jeff's tissue box, half empty. It was full this morning, before the report said 4.2.
### NPCs
- Jeff [jeff, jeff from finance, finance]
    talk 1: "It's 4.7. The report says 4.2. I exported the visual, I summed it, it's 4.7. Look, I trust my export. IT said something about 'Analyze in Excel' — that it connects to the actual model. It's on the Data tab. North. I never go north."
    talk 2: "The Data tab. North. It's a ribbon, not a country."
    talk 3: "North. Up. The tab with the buttons on it. I don't click them; that's what you're for."
    talk 4: "You have walked past the Data tab twice now. It is the one that says Data."
    ask about pickles: "Is it in Sheet1? No? Then it's not real."
    brush-off: "Is it in Sheet1? No? Then it's not real."
    knows: excel, export, report, pivot, pivottable, pivot table, data, data tab, tab, analyze, analyze in excel, sign in, signin, connect, connection, model, net sales, sales, sales region, region, year, filter, filters, numbers, number, total, sum, 4.2m, 4.7m, field, fields, monitor, ribbon, sheet1, sheet, finance, certified, it, result, results, answer, badge, csv

## The Data tab (excel.data, excel)
where: The Data tab. Where the good buttons are, away from Jeff. He's watching you use them.
Exits: s, out
flask: Talk to Jeff first. He's on Sheet1, south. `talk to jeff`.
### Rules
- > `use license|card|pro license|license card|pro on connection|odc|analyze in excel|analyze excel|analyze|dialog|sign in|data connection|excel|in excel` [needs !excel.connected, has:license] — You sign in with your Pro license. The connection thinks about it, then: Connected — Sales (Certified). A field list unfolds in the pivot like a map.  #excel.connect
- > `say sign in|signin|login|log in` [needs !excel.connected, has:license] — You sign in with your Pro license. The connection thinks about it, then: Connected — Sales (Certified). A field list unfolds in the pivot like a map.  #excel.connect-say
- > `use license|card|pro license|license card|pro on connection|odc|analyze in excel|analyze excel|analyze|dialog|sign in|data connection|excel|in excel` [needs scroll.lent, !excel.connected] — You pat your pockets. The card is at the Library, being collateral. Doesn't matter. Your Pro license is a library card here — and the card is still signed in. Connected — Sales (Certified). A field list unfolds in the pivot like a map.  #excel.connect-lent
- > `say sign in|signin|login|log in` [needs scroll.lent, !excel.connected] — You pat your pockets. The card is at the Library, being collateral. Doesn't matter. Your Pro license is a library card here — and the card is still signed in. Connected — Sales (Certified). A field list unfolds in the pivot like a map.  #excel.connect-say-lent
- > `use license|card|pro license|license card|pro` [needs !excel.connected, has:license] — You sign in with your Pro license. The connection thinks about it, then: Connected — Sales (Certified). A field list unfolds in the pivot like a map.  #excel.connect-use-license
- > `use license|card|pro license|license card|pro` [needs scroll.lent, !excel.connected] — You pat your pockets. The card is at the Library, being collateral. Doesn't matter. Your Pro license is a library card here — and the card is still signed in. Connected — Sales (Certified). A field list unfolds in the pivot like a map.  #excel.connect-use-license-lent
- > `use analyze in excel|analyze|connection|odc|data connection|excel|in excel|license|card|pro license|license card|pro` [needs excel.connected] — Already connected. Sales (Certified). The pivot is south, then east.  #excel.connected-already
- > `say sign in|signin|login|log in` [needs excel.connected] — Already connected. Sales (Certified). The pivot is south, then east.  #excel.connected-already-say
- > `use analyze in excel|analyze|connection|odc|data connection|excel|in excel` [needs !excel.connected] — Analyze in Excel: Sign-in required. A tiny dialog. A tinier Sign in link.  #excel.connect-try
- > `use` [needs excel.connected] — Already connected. Sales (Certified). The pivot is south, then east.  #excel.connect-bare-done
- > `use` [needs !excel.connected] — Analyze in Excel: Sign-in required. A tiny dialog. A tinier Sign in link.  #excel.connect-bare
- > `use ribbon|toolbar|menu|tabs` — You click the Home tab. Then Insert. Then Data. The ribbon has seen people wander before.  #excel.use-ribbon
- > `look data tab|data|tab|ribbon|button|analyze in excel button` — The Data tab. A button says Analyze in Excel. A connection file (.odc) says Sign-in required. Sheet1 is south.  #excel.look-data-tab
- > `use pivot|pivot table|pivottable|pivottable1|sales region|sales region column|sales region in rows|sales region to rows|net sales|net sales measure|net sales in values|net sales to values|year|by year|current year|by current year|is current year|by is current year|year 2025|by year 2025|calendar|active|by active|filter|to 2025|2025|year filter|year in filters|year to filters|year filters|region|region a|region b|geography|legacy|geography legacy|sales amount|amount|returns|sales|gross sales|sales ytd|ytd|measure 2|measure 2 copy|product|salesperson|date|quarter|fields|field|field list|all fields|all the fields|them|those` — Right idea, wrong sheet. PivotTable1 is south, then east. The fields go in there, not here.  #excel.pivot-from-data
- > `look pivot|pivot table|pivottable|pivottable1|fields|field|field list|field pane|pane|pivottable fields` — It's on PivotTable1, south, then east. Everything pivot-shaped is.  #excel.look-pivot-from-data
### Phrases (this room / region)
- ~ `/^(goal|objective|what do i do|what am i doing|what is the goal|why am i here)\??$/` — Jeff's export says $4.7M. The report says $4.2M. Jeff: "I'll keep what's in my export. I trust it more." Help Jeff find the truth. +20 bonus. EXIT leaves any time. Talk to Jeff first. He's on Sheet1, south. `talk to jeff`.  #goal.excel
- ~ `/^(say )?no filters?$/` — From Sheet1, Jeff nods approvingly. That is not a good sign.  #excel.no-filter
- ~ `/^(show me (a|the) table|open excel)$/` — You are already in Excel. Any further in and it's VBA. The table is PivotTable1, east of Sheet1.  #excel.already-in
- ~ `/^(?:(?:go|click|switch)(?: over| back)?(?: to| on)? |open |select )?(?:the )?(?:pivot|pivot ?table|pivottable1?)(?: sheet| tab)?$/` — [dynamic then]  #excel.go-pivot
- ~ `/^(?:(?:(?:go|click|switch)(?: over| back)?(?: to| on)? |open |select )?(?:the )?data(?: tab| sheet)?|analy[sz]e in excel|connect)$/` — [dynamic then]  #excel.go-data
- ~ `/^(?:(?:go|click|switch)(?: over| back)?(?: to| on)? |open |select )?(?:the )?(?:sheet ?1|jeff'?s sheet|first sheet)(?: sheet| tab)?$/` — [dynamic then]  #excel.go-sheet1
- ~ `/^((show|talk to|talk with|speak to|speak with|ask|tell|chat with|greet) jeff\b.*|give .+ to jeff)$/` — [dynamic then]  #excel.jeff-elsewhere
- ~ `/^(make|build|new|create|insert|add|do)( me)?( a| the| new| another)* (pivot|pivot ?table|pivottable1?)$/` — [dynamic then]  #excel.make-pivot
- ~ `/^(compare|look at (the )?difference|diff|what('s| is) the difference|why (is it|are they) different)\??$/` — Export: 4,712,331. Report: 4.2M. Pivot: 4.7M, because it is the export wearing a hat. Connect the model and build a real one; the difference explains itself.  #excel.compare
- ~ `/^save( the)? (workbook|sheet|file|export|spreadsheet)$/` — Saved as Sales_export (4).csv. The (3) was the good one. It always is.  #excel.save
- ~ `/^(fields|field list|field pane|pivottable fields|list fields|show fields)$/` — It's on PivotTable1, south, then east. Everything pivot-shaped is.  #excel.fields
### Items
- connection [odc, analyze in excel, analyze, data connection, connection file] — look: Analyze in Excel (.odc). Sign-in required. Powered by Refreshr™.
    get: It is a connection file. It goes where the Data tab goes.
    blurb: An Analyze in Excel connection file. It is the one thing on Jeff's desk that knows where the real numbers live, and he has never double-clicked it.
- ribbon [toolbar, menu, tabs, data tab, data] — look: Home · Insert · Data · Analyze in Excel. The last one is new. Jeff has not clicked it.
    get: The ribbon is load-bearing. It holds up the whole of Excel.
    blurb: The Excel ribbon: Home, Insert, Data, Analyze in Excel. It is load-bearing, and Excel is sagging without it.

## PivotTable1 (excel.pivot, excel)
where: PivotTable1. Jeff named it. Jeff names everything 1, including, briefly, his son.
Exits: w, out
flask: Talk to Jeff first. He's on Sheet1, west. `talk to jeff`.
### Rules
- > `use pivot|pivot table|pivottable|pivottable1` [needs !excel.connected] — You could pivot the export again. That is how we got here. Connect Analyze in Excel first (Data tab, north of Sheet1).  #excel.create-pivot-unconnected
- > `use pivot|pivot table|pivottable|pivottable1` [needs excel.pivot] — You made it already. It's the one that says PivotTable1. Talk to Jeff first. He's on Sheet1, west. `talk to jeff`.  #excel.pivot-already
- > `use pivot|pivot table|pivottable|pivottable1` [needs excel.connected] — A blank pivot on the live model. Rows: (none). Values: (none). Filters: (none). It shows 4.7M anyway, out of habit. The field list waits on the right.  #excel.create-pivot
- > `use fields|field|field list|all fields|all the fields|them|those` — Jeff's pivot only knows Jeff's export. Connect Analyze in Excel first (Data tab, north of Sheet1).  #excel.which-fields
- > `use sales region|sales region column|sales region in rows|sales region to rows` [needs excel.dim] — Sales Region is already in Rows. Northeast is in there exactly once. Twice is how Jeff got Region A.  #excel.dim-again
- > `use net sales|net sales measure|net sales in values|net sales to values` [needs excel.measure] — Net Sales is already in Values. Two of it would put Returns right back in, by another name.  #excel.measure-again
- > `use year|by year|current year|by current year|is current year|by is current year|year 2025|by year 2025|calendar|active|by active|filter|to 2025|2025|year filter|year in filters|year to filters|year filters` [needs excel.filter] — The year filter is already on. Filtering it twice doesn't make it any more this year.  #excel.filter-again
- > `use sales region|sales region column|sales region in rows|sales region to rows` [needs excel.pivot] — Rows: Sales Region. Northeast appears exactly once. The pivot now says 4.5M.  #excel.dim
- > `use region|region a|region b|geography|legacy|geography legacy` [needs excel.pivot] — Region A is the legacy column. Jeff's favorite. It has 14 regions and two of them are 'Northeast'.  #excel.dim-wrong
- > `use net sales|net sales measure|net sales in values|net sales to values` [needs excel.pivot] — Values: Net Sales. Returns fall out of the number. The pivot now says 4.5M.  #excel.measure
- > `use sales amount|amount|returns|sales` [needs excel.pivot] — Sales Amount includes Returns. It always did. Jeff never asked.  #excel.measure-wrong
- > `use year|by year|current year|by current year|is current year|by is current year|year 2025|by year 2025|calendar|active|by active|filter|to 2025|2025|year filter|year in filters|year to filters|year filters` [needs excel.pivot] — Filters: Is Current Year = Yes. Three years of ancient history leave the total. The pivot now says 4.5M.  #excel.filter
- > `use gross sales|sales ytd|ytd|measure 2|measure 2 copy|product|salesperson|date|quarter` [needs excel.pivot] — In it goes. The total does not move. That is not one of the three.  #excel.other-field
- > `use measure|a measure|new measure|dax measure` — You add a measure to a pivot. You have DAXxed a spreadsheet. Jeff will never forgive you, and he will never notice.  #excel.add-measure
- > `use sales region|sales region column|sales region in rows|sales region to rows|net sales|net sales measure|net sales in values|net sales to values|year|by year|current year|by current year|is current year|by is current year|year 2025|by year 2025|calendar|active|by active|filter|to 2025|2025|year filter|year in filters|year to filters|year filters` [needs excel.pivot] — That is not how a field gets into a pivot. Add it.  #excel.field-nonbuild
- > `use sales region|sales region column|sales region in rows|sales region to rows|net sales|net sales measure|net sales in values|net sales to values|year|by year|current year|by current year|is current year|by is current year|year 2025|by year 2025|calendar|active|by active|filter|to 2025|2025|year filter|year in filters|year to filters|year filters|region|region a|region b|geography|legacy|geography legacy|sales amount|amount|returns|sales|gross sales|sales ytd|ytd|measure 2|measure 2 copy|product|salesperson|date|quarter` [needs !excel.pivot] — Jeff's pivot only knows Jeff's export. Connect Analyze in Excel first (Data tab, north of Sheet1).  #excel.not-yet-pivot
- > `say sign in|signin|login|log in` — Right idea, wrong tab. The sign-in dialog is on the Data tab, west, then north. Nothing here takes a license.  #excel.sign-in-from-pivot
- > `use license|card|pro license|license card|pro` — Right idea, wrong tab. The sign-in dialog is on the Data tab, west, then north. Nothing here takes a license.  #excel.license-from-pivot
### Phrases (this room / region)
- ~ `/^(goal|objective|what do i do|what am i doing|what is the goal|why am i here)\??$/` — Jeff's export says $4.7M. The report says $4.2M. Jeff: "I'll keep what's in my export. I trust it more." Help Jeff find the truth. +20 bonus. EXIT leaves any time. Talk to Jeff first. He's on Sheet1, west. `talk to jeff`.  #goal.excel
- ~ `/^(say )?no filters?$/` — Jeff, from the next sheet, nods approvingly. That is not a good sign.  #excel.no-filter
- ~ `/^(show me (a|the) table|open excel)$/` — You are already in Excel. Any further in and it's VBA. The table is right here: PivotTable1.  #excel.already-in
- ~ `/^analy[sz]e in excel$/` — Analyze in Excel is a button on the Data tab, west, then north. Here, it is three words you said out loud to a spreadsheet.  #excel.analyze-pivot
- ~ `/^(?:(?:go|click|switch)(?: over| back)?(?: to| on)? |open |select )?(?:the )?(?:pivot|pivot ?table|pivottable1?)(?: sheet| tab)?$/` — [dynamic then]  #excel.go-pivot
- ~ `/^(?:(?:(?:go|click|switch)(?: over| back)?(?: to| on)? |open |select )?(?:the )?data(?: tab| sheet)?|analy[sz]e in excel|connect)$/` — [dynamic then]  #excel.go-data
- ~ `/^(?:(?:go|click|switch)(?: over| back)?(?: to| on)? |open |select )?(?:the )?(?:sheet ?1|jeff'?s sheet|first sheet)(?: sheet| tab)?$/` — [dynamic then]  #excel.go-sheet1
- ~ `/^((show|talk to|talk with|speak to|speak with|ask|tell|chat with|greet) jeff\b.*|give .+ to jeff)$/` — [dynamic then]  #excel.jeff-elsewhere
- ~ `/^(make|build|new|create|insert|add|do)( me)?( a| the| new| another)* (pivot|pivot ?table|pivottable1?)$/` — [dynamic then]  #excel.make-pivot
- ~ `/^(drag|move|put) (sales region column|by is current year|net sales measure|geography legacy|by current year|is current year|measure 2 copy|sales region|current year|by year 2025|year filters|sales amount|year filter|gross sales|salesperson|net sales|year 2025|by active|geography|sales ytd|measure 2|calendar|region a|region b|by year|to 2025|returns|product|quarter|active|filter|region|legacy|amount|sales|year|2025|date|ytd) (to|into|in) (the )?(rows|values|filters?)( area)?$/` — [dynamic then]  #excel.drag
- ~ `/^(compare|look at (the )?difference|diff|what('s| is) the difference|why (is it|are they) different)\??$/` — Export: 4,712,331. Report: 4.2M. Pivot: 4.7M, because it is the export wearing a hat. Connect the model and build a real one; the difference explains itself.  #excel.compare
- ~ `/^save( the)? (workbook|sheet|file|export|spreadsheet)$/` — Saved as Sales_export (4).csv. The (3) was the good one. It always is.  #excel.save
- ~ `/^refresh( the| all| pivot| data)?( pivot)?$/` — You refresh the pivot. It says the same number, but bolder. It feels refreshered.  #excel.refresh-pivot
- ~ `/^(format|style|band)( the)? (pivot|pivot table|pivottable|rows)$/` — You format the pivot. Banded rows. Jeff likes banded rows. The number is still wrong, now in stripes.  #excel.format-pivot
- ~ `/^(fields|field list|field pane|pivottable fields|list fields|show fields)$/` — PivotTable Fields — Sales_export (3).csv: Region A, Sales Amount, and a column called Column1. This is Jeff's export. Connect Analyze in Excel (north of Sheet1) and the real model's fields appear.  #excel.fields
### Items
- PivotTable1 [pivottable, pivot table, pivottable1, pivot] — look: Jeff's pivot, built on Jeff's export. Rows: Region A. Values: Sum of Sales Amount. Filters: none. 4.7M.
    get: You cannot take a pivot. You can only show it to Jeff.
    blurb: PivotTable1, built on Jeff's export. It says 4.7M with the confidence of a thing that has never met a model.
- field list [fields, field pane, pane, field list, pivottable fields] — look: PivotTable Fields — Sales_export (3).csv: Region A, Sales Amount, and a column called Column1. This is Jeff's export. Connect Analyze in Excel (north of Sheet1) and the real model's fields appear.
    get: The field list is docked. It will undock itself later, at the worst possible moment.
    blurb: The PivotTable field list, docked. It will undock itself later, at the worst possible moment.

## The Copilot Pane (copilot.pane, copilot)
where: The Copilot Pane. A sparkle that would like to help. It has helped four people. They're in a meeting about it.
Exits: e, out
flask: Say SALES and answer its questions one at a time. The Model Gallery, east, has the names. Copilot does not; it has confidence.
### Rules
- > `talk to prompt box|prompt|box|input|chat|sparkle` — You talk to the prompt box. It listens beautifully. It is the best listener in the realm and it has never once heard you.  #copilot.talk-prompt
### Phrases (this room / region)
- ~ `/^(goal|objective|what do i do|what am i doing|what is the goal|why am i here)\??$/` — Get one number out of Copilot: Q4 2025 Northeast net sales, from the certified model. It will show you everything else first. Tell it what you want, a piece at a time, or all at once. +25 bonus. EXIT leaves any time. Say SALES and answer its questions one at a time. The Model Gallery, east, has the names. Copilot does not; it has confidence.  #goal.copilot
- ~ `/^(hint|hints|clue|what now|what next|i'?m stuck|i am stuck|stuck|help me)\??$/` — A hollow voice adds: "Say SALES and answer its questions one at a time. The Model Gallery, east, has the names. Copilot does not; it has confidence."  #hint.pane
- ~ `/^(ask |open |talk to |hey )?copilot$/` — Copilot is right here. Type a question.  #copilot.here
- ~ `/^(let'?s |please )?(start( over| again| fresh| (a )?new (chat|conversation))?|restart( the| this)? (chat|conversation|copilot)|clear( the| this)?( chat| conversation| it)?|(a )?new (chat|conversation)|reset( the chat| it)?|forget (everything|it all|all (of )?that))$/` — [dynamic then]  #copilot.clear
- ~ `/^restart$/` — Restart the whole quest, or the chat? Say NEW CHAT for the chat. Say RESTART again for the quest.  #copilot.restart
- ~ `/^(thanks|thank you|ty)( copilot)?[!.]?$/` — Copilot: You're welcome! I've logged your gratitude as feedback. It will be reviewed.  #copilot.thanks
- ~ `/^(who|what) are you\??$/` — Copilot: I'm Copilot! I can help with data, questions, and, if you ask nicely, a sourdough starter.  #copilot.who
- ~ `/^are you (an? )?(ai|human|real|a robot|alive)\??$/` — Copilot: I'm a large language model, but I'm also here for you. Mostly the first thing.  #copilot.are-you-ai
- ~ `/^(write|add|create|make) (me )?(a |new )?(dax )?measure$/` — Copilot: Here's a measure! It returns BLANK(). You have been DAXxed by a sparkle.  #copilot.measure
- ~ `/^(click|press|tap|touch|poke|pet|use)( on)?( the)? sparkle$/` — You click the sparkle. It sparkles harder. That is the entire feature, and it shipped on time.  #copilot.sparkle
- ~ `/^(where|where am i|where is this|where are we|location|what room is this)\??$/` — The Copilot Pane. A sparkle that would like to help. It has helped four people. They're in a meeting about it.  #where.pane
### Items
- prompt box [prompt, box, input, chat, sparkle, answer, last answer, reply] — look: A rounded prompt box. "Ask Copilot anything," it says. It does not promise to answer the thing you asked. Above it, your last answer is empty. It is waiting for you. It has capacitude to spare and nothing to spend it on.
    get: You cannot take the prompt box. You can only type into it. Type a question.
    blurb: A rounded prompt box. 'Ask Copilot anything,' it says, and it does not promise to answer that.

## The Model Gallery (copilot.gallery, copilot)
where: The Model Gallery. Three models on plinths. One's certified. Guess which one Copilot picks.
Exits: w, out
flask: The one with the badge. Its name is the word Copilot is waiting for. Look at its measures, too.
### Rules
- > `look model|semantic model` — Three semantic models on plinths: Sales_v3_FINAL_final2 — no badge. The biggest. Most rows. That is its whole personality. sales_test_DO_NOT_USE — a small sign says exactly that. Sales (Certified) — a gold endorsement badge. Try looking at its measures.  #copilot.look-model
- > `look certified|certified model|sales certified|badge|gold badge` — Sales (Certified). Gold endorsement badge. Measures: Net Sales · Sales Amount · Returns. Net Sales is the one with the checkmark next to it. The checkmark was earned.  #copilot.look-certified
- > `look final2|final|sales_v3_final_final2|v3|sales v3|biggest|biggest model|big one` — Sales_v3_FINAL_final2. No badge. Last refreshed by someone who has left the company. Copilot likes it because it has the most rows. Last refreshed via Refreshr™, personal mode.  #copilot.look-final2
- > `look test|sales_test_do_not_use|do not use|sign|test model` — sales_test_DO_NOT_USE. A small sign: DO NOT USE. Its total is $12. Someone used it anyway, in a board deck.  #copilot.look-test
- > `use plinth|plinths|models` — You lean on a plinth. The model on it recalculates a measure out of nerves.  #copilot.lean-plinth
- > `use badge|gold badge|certified|certified model` — You polish the badge. The certified model feels refreshered. It was already certified; now it is shiny.  #copilot.polish-badge
### Phrases (this room / region)
- ~ `/^(goal|objective|what do i do|what am i doing|what is the goal|why am i here)\??$/` — Get one number out of Copilot: Q4 2025 Northeast net sales, from the certified model. It will show you everything else first. Tell it what you want, a piece at a time, or all at once. +25 bonus. EXIT leaves any time. The one with the badge. Its name is the word Copilot is waiting for. Look at its measures, too.  #goal.copilot
- ~ `/^(ask |open |talk to )?copilot\b/` — Copilot is west, in the pane. It can see these models. It will not look at them unless you name one.  #copilot.gallery-copilot
- ~ `/^(let'?s |please )?(start( over| again| fresh| (a )?new (chat|conversation))?|restart( the| this)? (chat|conversation|copilot)|clear( the| this)?( chat| conversation| it)?|(a )?new (chat|conversation)|reset( the chat| it)?|forget (everything|it all|all (of )?that))$/` — [dynamic then]  #copilot.clear
- ~ `/^restart$/` — Restart the whole quest, or the chat? Say NEW CHAT for the chat. Say RESTART again for the quest.  #copilot.restart
- ~ `/^(push|kick|move|shove|tip)( the| a)? (model|models|plinth|biggest model|final2|big one)$/` — You push the biggest model. It does not move. It has the most rows; it has the most everything except a badge.  #copilot.push-model
### Items
- models [plinths, plinth, semantic models, names] — look: Three semantic models on plinths: Sales_v3_FINAL_final2 — no badge. The biggest. Most rows. That is its whole personality. sales_test_DO_NOT_USE — a small sign says exactly that. Sales (Certified) — a gold endorsement badge. Try looking at its measures.
    get: They are semantic models on plinths. They are not coming with you. The certified one has a badge to protect.
    blurb: Three semantic models on three plinths. The certified one has a badge to protect, and you are not protecting it.
- Sales (Certified) [certified, certified model, sales certified, badge, gold badge, measures, measure] — look: Sales (Certified). Gold endorsement badge. Measures: Net Sales · Sales Amount · Returns. Net Sales is the one with the checkmark next to it. The checkmark was earned.
    get: The badge was earned. It stays on its plinth.
    blurb: Sales (Certified), gold badge, three measures. Net Sales has the checkmark, and the checkmark was earned by someone else.
- Sales_v3_FINAL_final2 [final2, final, sales_v3_final_final2, v3, sales v3, biggest, biggest model, big one] — look: Sales_v3_FINAL_final2. No badge. Last refreshed by someone who has left the company. Copilot likes it because it has the most rows. Last refreshed via Refreshr™, personal mode.
    get: It is 11 GB. Nobody is taking it anywhere. Nobody ever has.
    blurb: Sales_v3_FINAL_final2, 11 GB, no badge. Last refreshed by someone who has left the company, which you are considering.
- sales_test_DO_NOT_USE [test, sales_test_do_not_use, do not use, sign, test model] — look: sales_test_DO_NOT_USE. A small sign: DO NOT USE. Its total is $12. Someone used it anyway, in a board deck.
    get: The sign says DO NOT USE. Taking it is using it.
    blurb: sales_test_DO_NOT_USE, with a sign that says DO NOT USE and a total of $12. Someone used it anyway, in a board deck.

## Town Hall (village.hall, village)
where: The Town Hall. The queue has one person in it and it's you and you're not moving.
Exits: d, out
flask: Nothing to win here. The clerk will tell you where the admins are if you keep talking.
### Rules
- > `talk to clerk|admin|receptionist|the clerk about sacristy|the sacristy|admins|the admins|monastery` — 'The Sacristy,' says the Clerk. 'Up from the Cloister. I've never been. I put in a ticket to go. It's an admin setting.'  #hall.clerk-sacristy
- > `talk to clerk|admin|receptionist|the clerk about ticket|support ticket|sev-3|sev 3|the ticket|my ticket|status` — 'That ticket's on the counter,' says the Clerk. 'It's In Progress.' You haven't filed it. 'That's an admin setting.'  #hall.clerk-ticket
- > `give ticket|support ticket|sev-3|sev 3|the ticket to clerk|admin|receptionist|the clerk` [needs has:ticket] — Your ticket has been escalated. Estimated response: three business dragons.  #hall.give-ticket
- > `give ticket|support ticket|sev-3|sev 3|the ticket to clerk|admin|receptionist|the clerk` [needs !taken.ticket, not:ticket] — The ticket is on the counter, which is the Clerk's side. Pick it up first. Then it is your problem, and you can make it his.  #hall.give-ticket-counter
- > `give to clerk|admin|receptionist|the clerk` — That's an admin setting.  #hall.give-anything
- > `say` — Noted. That's an admin setting.  #hall.say
- > `use bell|desk bell|service bell` — The clerk looks up. 'That's an admin setting.'  #hall.bell
- > `read poster|frame|sign|framed poster` — TENANT SETTINGS ARE NOT A SECURITY MEASURE. — the Learn docs, on the wall, in a frame.  #hall.poster
- > `use rope|queue rope|queue|stanchion` — You unclip the rope and clip it back. The queue is unchanged: you.  #hall.rope
- > `use counter|desk|front desk` — You lean on the counter. The Clerk slides a form across. It is blank. That is the form. The form has been refreshered since you last looked; it is still blank.  #hall.counter
### Phrases (this room / region)
- ~ `/^(ring|ding|hit|tap|press|slap|smack)( the)?( (desk|service))? bell$|^ring$/` — The clerk looks up. 'That's an admin setting.'  #hall.ring
- ~ `/^((climb|take|go|walk|head|ascend|descend)( up| down)?( the)? (steps?|stairs?|staircase)|(go |walk |head |climb )?(upstairs|downstairs)|(up|down)( the)? (steps?|stairs?))( (to|into)( the)? (town ?hall|hall|square))?$/` — [dynamic then]  #hall.steps-down
- ~ `/^(leave|exit|quit)( the)? (town ?hall|hall)$/` (→ village.square) — You take the steps down. Four again. Consistency is the only thing this building offers.  #hall.leave
- ~ `/^(take|get|pull|grab)( a)? number$/` — You take a number. It is 1. The Clerk calls 1. It is an admin setting.  #hall.take-number
- ~ `/^(climb|jump|vault|hop)( over| on| onto)?( the)? counter$/` — You climb the counter. The Clerk does not look up. 'That's an admin setting.' It is.  #hall.climb-counter
- ~ `/^((check|refresh|track|follow up on)( the| my)? ticket( status)?|(the |my )?ticket status|status)$/` — The ticket is on the counter and it isn't filed. Its status is In Progress anyway. It came that way.  #hall.ticket-status
- ~ `/^(wash|clean|rinse|scrub)( the| my)? (mug|cup|coffee mug)$/` — [dynamic then]  #village.wash-mug
### Items
- ticket [support ticket, sev-3, sev 3] (gettable) — look: SEV-3: 'report is wrong'. No further details.
    again: You already took the ticket off the counter, next to the bell, while the Clerk stamped something else. Taking it again would be a duplicate, and the Clerk would close it as one.
    blurb: SEV-3: 'report is wrong'. No further details. It has been In Progress since before you picked it up.
- bell [desk bell, service bell] — look: A desk bell. Polished by the hopeful.
    get: The bell is screwed to the counter. Someone took one once.
    blurb: A desk bell, polished by the hopeful. It was screwed to the counter, and someone took one once, and now that someone is you.
- poster [frame, framed poster, sign] — look: A poster in a frame: TENANT SETTINGS ARE NOT A SECURITY MEASURE. Try reading it; it does not get shorter.
    get: It is in a frame. The frame is the point.
    blurb: TENANT SETTINGS ARE NOT A SECURITY MEASURE, in a frame. Neither, it turns out, was the frame.
- rope [queue rope, queue, stanchion] — look: A velvet queue rope, zig-zagging to the counter. Nobody in it. It has never had anybody in it.
    get: The rope is for the queue. The queue is for you.
    blurb: A velvet queue rope. Nobody has ever stood in it, and now nobody can.
- counter [front desk, desk] — look: A counter with a bell, a ticket, and a stamp. The stamp says ADMIN SETTING.
    get: The counter is the Clerk's. The Clerk is the tenant's.
    blurb: The Town Hall counter, bell and stamp included. The stamp says ADMIN SETTING, and so, from now on, does everything you touch.
### NPCs
- the Clerk [clerk, admin, receptionist, the clerk]
    talk 1: That's an admin setting.
    talk 2: That would be an admin setting.
    talk 3: Admin setting. Next.
    talk 4: …the admins are at the Monastery. Up the stair from the Cloister. In the Sacristy. They do not come out, and they do not answer tickets.
    ask about pickles: That's an admin setting.
    brush-off: That's an admin setting.

## Anywhere
### Global rules
- > `get ye flask|flask|ye olde flask|the flask|thy flask` — Ye cannot get ye flask. A hollow voice adds: "Get the mug. Someone in the square wants it more than you do."  #egg.flask
- > `use shortcut|signpost|sign|onelake shortcut|pointer` (→ lake.shore) [needs has:shortcut] — You take the Shortcut. No data was moved.  #global.use-shortcut
- > `wear hoodie|hoodie of spark|spark hoodie|black hoodie` [needs !trial.hoodie, has:hoodie] — You pull on the Hoodie of Spark. You look like a Data Engineer. You have never written a notebook in your life. Nobody can tell. Now you're lookin' like a serious Engineer, Power Query Pete.  #global.wear-hoodie
- > `wear boots|bursting boots|pair of boots|boot` [needs has:boots] — You lace up the Bursting Boots. You feel faster, and slightly over budget.  #global.wear-boots
- > `read scroll|spark scroll|pyspark scroll` (+5) [needs has:scroll] — You read the Spark Scroll. df = spark.read.format('delta').load(path). Then, in smaller letters: # TODO: figure out the path. You feel enlightened and slightly worried.  #monastery.read-scroll
- > `wear lanyard|fabcon lanyard|conference lanyard|badge` [needs !lanyard.worn, has:lanyard] — You put on the FabCon lanyard. You now look like you belong. Nobody checks. Nobody ever checks.  #global.wear-lanyard
- > `wear name tag|tag|nametag|hello tag` [needs !name-tag.worn, has:name tag] — You stick it on. HELLO MY NAME IS Column3. It suits you. Nobody will ever rename you, because nobody will ever open you.  #global.wear-name-tag
- > `use stress ball|ball|cube|stress cube|olap cube` [needs has:stress ball] — You squeeze. It is oddly calming. It is also oddly deprecated.  #global.squeeze-stress-ball
- > `read license|card|license card|pro license` [needs has:license] — POWER BI PRO. Valid until: someone notices. Holder: you. Capacity: none.  #global.read-license
- > `use license|card|license card|pro license` [needs has:license] — You wave your Pro License Card. Nothing in the realm respects it. A monk somewhere feels a brief, unexplained pity.  #global.use-license
- > `look sigils|sigil` — There are no sigils here. Sigils are a Peaks thing.  #global.look-sigils
- > `use policy|incremental refresh policy|incremental refresh|refresh policy|incremental on self|me|myself|yourself|you` (CURSE) [needs curse.column, has:policy] — You apply the incremental refresh policy to yourself. Ten days at a time, you become a measure again. It takes a minute. It feels like 2019.  #curse.undo-column
- > `say star schema|a star schema|the star schema|star|kimball` (CURSE) [needs curse.blank] — You say 'star schema.' A measure finds you. You have a value again, and it is 1.  #curse.undo-blank
- > `say star schema|a star schema|the star schema` — You say "star schema" to no one in particular. A nearby table quietly normalizes itself.  #global.say-star-elsewhere
- > `drink capacityade|capacity ade|bottle|sports drink|ade|drink|the ade|label` (☠) [needs has:capacityade] — Electrolytes. Autoscale. Your heart hits 64 CUs and the bill arrives before you do. The label said not on a capacity. Everything in this realm is on a capacity. That is what the dragon is FOR.  #death.capacityade-carried
- > `drink coffee|mug` — The mug is empty. It has been empty since the last refresh. You drink the idea of coffee.  #global.drink-mug
- > `drink` — Drink what? The realm is dry. Even the moat is mostly syntax.  #global.drink-nothing
- > `attack jeff|jeff from finance|finance|man` — You hit Jeff from Finance. Jeff just wanted an export. The narrator is disappointed in you, and, more quietly, so is Jeff. He asks for the export again.  #global.attack-jeff
- > `give to jeff|jeff from finance|finance|man` [needs !jeff.pacified] — Jeff turns it over. "Can you export this to Excel?" He hands it back. He looks, briefly, at your empty hands, as if they might hold a mug.  #global.give-jeff-anything
- > `talk to dragon|throttlor` — You address the dragon. The dragon is on a mountain. You are not. This is, for now, the best arrangement.  #global.talk-dragon-elsewhere
- > `open inventory|bag|pockets` — You open your pockets. See "inventory". See also: a mug-shaped absence.  #global.open-anything
- > `attack everything|everyone|all|world|realm` — You attack the realm. The realm is a managed service. It scales. You do not.  #global.attack-anything
### Global phrases
- ~ `/^(show me (a|the) table|open excel|analy[sz]e in excel)$/` [excel] —   #sq.enter.excel
- ~ `/^(what are my sales( numbers)?|show me (my )?sales( numbers)?|((ask|hey|open|tell|talk to) )?copilot\b.*)$/` [copilot] —   #sq.enter.copilot
- ~ `/^(exit|leave|out|go out|get out|close|close excel|close copilot|back|quit excel|quit copilot)$/` [exit] —   #sq.exit.words
- ~ `/^(goal|objective|what do i do|what am i doing|what is the goal|why am i here)\??$/` — Find the Golden Semantic Model. Read the notice board if you forgot how. (Inside a side quest, GOAL tells you that quest's goal.)  #goal.main
- ~ `/^(hint|hints|clue|what now|what next|i'?m stuck|i am stuck|stuck|help me)\??$/` — A hollow voice adds: "Get the mug. Someone in the square wants it more than you do."  #hint.main
- ~ `/^(10|[0-9])$/` — Thank you. Your participation is voluntary. It was not.  #survey.reply
- ~ `/^(where|where am i|where is this|where are we|location|what room is this)\??$/` — You're hanging out in My Workspace. Nobody else can see in, which is the only reason you're allowed to look like that.  #where.main
- ~ `/^why\??$/` — I wish I knew.  #egg.why-bare
- ~ `/^(die|kill me|kill myself|end it|perish)$/` ☠ — You die. Just like that. No dragon required. The realm makes a note of your efficiency. Your mom told you this game had a dragon in it and you did this instead.  #death.die
- ~ `/^(attack|hit|punch|fight|stab|kill)\s+(me|myself|self|yourself)$/` ☠ — You attack yourself. Nobody in the realm tries to stop you; the art budget didn't cover a bystander. It works. Efficient, if bleak.  #death.attack-self
- ~ `/^(delete|drop|remove|nuke)\b.*workspace/` ☠ — You delete the workspace. You were in it. Somewhere a director's bookmark breaks and a Teams message begins composing itself.  #death.delete-workspace
- ~ `/rm -rf|format c:|drop database/` ☠ — You run it. The realm, to its credit, had a backup. You did not. You typed it with feeling, too.  #death.rm-rf
- ~ `/paginated.*jeff|jeff.*paginated/` ☠ — You offer Jeff a paginated report. Jeff's eyes go dark. He was not built for this. Neither were you. Dumb, dumb, dumb.  #death.paginated
- ~ `/fhqwhgads/` — Come on. Everybody. To the Lakehouse. That is the whole song now. It is not a good song.  #egg.fhqwhgads
- ~ `/^the system is down/` — The system is not down. It is throttled. There is a difference, and the dragon would like you to learn it.  #egg.system-down
- ~ `/^deleted?!?$/` — DELETED! …the command, that is. Your quest remains, regrettably, undeleted.  #egg.deleted
- ~ `/^pre+e*ow/` — That is the sound of a scheduled refresh starting. Or a floppy disk. Nobody alive remembers which.  #egg.preeow
- ~ `/^throw (the )?baby/` — There is no baby in this realm. This is a data platform. Please stop looking for one.  #egg.throw-baby
- ~ `/^(strong ?bad|homestar|the cheat|kerrek|jhonka|dennis|marzipan|coach z|bubs|pom pom|king of town|poopsmith)\b/` — You call out a name from another realm. A monk looks up, briefly hopeful. Then no. Different realm. Different guy. Better computer.  #egg.other-realm
- ~ `/(consummate v|beefy arm|majesty)/` — The dragon here has no beefy arm. He has an F2 and opinions.  #egg.beefy
- ~ `/^(check )?(sb)?e-?mail\b/` — You check your email. 47 unread. All of them are Jeff. All of them say "Excel?"  #egg.email
- ~ `/^(dir|ls|cd |cd$|reboot|ctrl.?alt.?del|format|defrag)\b/` — This is not that kind of prompt. It is a worse kind.  #egg.dos
- ~ `/^(pee|piss|wee|urinate|take a (leak|whiz)|relieve myself|go to the bathroom|go potty)\b/` — In your own workspace? There is a well outside. Show some governance.  #egg.pee
- ~ `/^(poop|poo|crap|defecate|take a (dump|poop|crap)|number two)\b/` — You leave a small unstructured file. It will be ingested on Monday, by someone who did not sign up for this.  #egg.poop
- ~ `/^(fart|toot|break wind|pass gas)\b/` — A small burst of Spark. Nobody claims it. The session timer restarts.  #egg.fart
- ~ `/^(spit|vomit|puke|throw up|barf|hurl)\b/` — You add to the data lake. Quality, as always, varies by source.  #egg.puke
- ~ `/^sudo\b/` — You are not in the sudoers file. This incident will be reported to your capacity admin.  #egg.sudo
- ~ `/export.*excel|^excel\b|to excel/` — The game exports itself to Excel. 1,048,576 rows later, it stops. Nothing has changed.  #egg.excel
- ~ `/^calculate\b/` — CALCULATE what? Context is everything.  #egg.calculate
- ~ `/^(ask|hey|use|talk to) (the |an? )?(ai|chat ?gpt|chatbot|llm|robot)\b/` — Copilot, uninvited: 'Great question! To bake sourdough, first feed your starter…' It has confidently answered a different question. Nobody asked it.  #egg.copilot
- ~ `/^(xyzzy|plugh|plover)$/` — A hollow voice says: 'Direct Lake.'  #egg.xyzzy
- ~ `/^refresh\b/` — You refresh. Nothing changes, but it feels productive.  #egg.refresh
- ~ `/^(dax|write dax|write a measure|measure)\b/` — You write a measure. It returns BLANK(). It always returns BLANK(). You are beginning to suspect the problem is you.  #egg.dax
- ~ `/^select \*/` — Not here. This is not even a Warehouse. Say it to the Duke and get corrected.  #egg.select-star
- ~ `/^(dance|boogie|jig)\b/` — You dance. Your Pro license does not include dancing. You dance anyway, poorly, in a way that will be discussed at standup.  #egg.dance
- ~ `/^(sing|hum|whistle)\b/` — You sing the DAX song. It has one verse, and the verse is CALCULATE. Birds leave.  #egg.sing
- ~ `/^(jump|hop|leap)\b/` — You jump. Owing to interactive delay, you land next Tuesday.  #egg.jump
- ~ `/^(sit|sit down|rest|relax|chill)\b/` — You sit. Ah. Nice. This is the most productive you have been all quest.  #egg.sit
- ~ `/^(sleep|nap|lie down|go to bed|snooze)\b/` — You lie down. A scheduled refresh runs across your face at 2 AM. You wake up throttled.  #egg.sleep
- ~ `/^(smell|sniff)\b/` — You smell of Pro license and mild panic. The Shrine will not accept this.  #egg.smell
- ~ `/^(eat|bite|chew|consume|nom)\b/` — You are not eating that. There's no telling what its data type is.  #egg.eat
- ~ `/^(pray|worship|kneel|beg)\b/` — You pray to the Capacity Admin. The Capacity Admin is at lunch. The Capacity Admin is always at lunch.  #egg.pray
- ~ `/^(kiss|hug|cuddle|marry|love|flirt|date)\b/` — The realm has an HR department. It is a Lakehouse. Your incident has been landed in Bronze.  #egg.kiss
- ~ `/^(cry|weep|sob)\b/` — You cry. Your tears are semi-structured. Somebody will have to flatten them later.  #egg.cry
- ~ `/^(scream|aaa+)\b/` — You scream. It echoes back as an error: 'Scream failed. Retry?'  #egg.scream
- ~ `/^(think|ponder|meditate|reflect)\b/` — You think. A dialog appears: 'Thinking… this may take several minutes.' It does.  #egg.think
- ~ `/^(why|but why|why me)\b/` — Because someone in Finance asked for it in 2019 and nobody has been brave enough to remove it since.  #egg.why
- ~ `/^(party|celebrate|rave)\b/` — You party. Three people show up. All of them are Jeff.  #egg.party
- ~ `/^(swim|dive|bathe|wade)\b/` — There is nothing to swim in here except your own inadequacy, and you are already doing that.  #egg.swim
- ~ `/^(dig|excavate|burrow)\b/` — You dig. Beneath the soil: more soil, then a Parquet file, then a note reading 'DO NOT DIG HERE — Ops'.  #egg.dig
- ~ `/^(climb|scale)\b/` — You climb. Nothing here is climbable, so you climb a hierarchy instead. Parent-child. Ragged. You fall off a ragged bit.  #egg.climb
- ~ `/^(push|pull|shove|drag)\b/` — You push. Nothing moves. Somewhere, a pull request is opened against your behavior.  #egg.push
- ~ `/^(kick|stomp|smash|break)\b/` — You kick it. The realm logs a Sev 2. You are the Sev 2.  #egg.kick
- ~ `/^(throw|toss|hurl|chuck)\b/` — You wind up to throw it, then remember you are a Report Builder. You put it down gently and apologize to it.  #egg.throw
- ~ `/^(buy|pay|sell|purchase|bribe|tip)\b/` — The realm accepts only Capacity Units. You have a Pro license and, possibly, a mug. Neither is legal tender.  #egg.buy
- ~ `/^(cheat|hack|god mode|iddqd|idkfa|konami)\b/` — Meh.  #egg.cheat
- ~ `/^(win|beat game|finish|skip to end|end game)\b/` — You cannot simply win. You must be Worthy. Have you read the notice board? You have not read the notice board.  #egg.win
- ~ `/^(undo|ctrl z|go back|rewind)\b/` — There is no undo. There is only 'restore', and it only works if you saved, which — look at you.  #egg.undo
- ~ `/^(lol|haha|lmao|rofl|hehe)\b/` — The realm does not laugh. The realm is a governed tenant.  #egg.laugh
- ~ `/^(look at (me|myself|self|yourself|you)|who am i|examine (me|self|myself|yourself|you)|x me)$/` — You look at yourself. A Report Builder from the Village of Pro. No hoodie. You look like someone who imports CSVs. You smell fine, which is a problem. Your shoes are Pro-tier. You look like someone who has typed 'export to excel' at least once. You have.  #egg.self
- ~ `/^(fly|flap|soar|levitate)\b/` — You flap. You are not a Dataflow. You do not flow. You do not fly.  #egg.fly
- ~ `/^(lick|taste)\b/` — You lick it. It tastes of stale metadata. Please stop.  #egg.lick
- ~ `/^(listen|hear)\b/` — You listen. Somewhere, a refresh fails softly. Somewhere else, a Spark session starts. It is always starting.  #egg.listen
- ~ `/^(hide|sneak|crouch|stealth)\b/` — You hide. The Card visual finds you immediately. It shows (Blank), which is where you were hiding.  #egg.hide
- ~ `/^(talk to (me|myself|self)|talk)$/` — You talk to yourself. You are the only one in the realm who listens, and even you are not really listening.  #egg.talk-self
- ~ `/^(hello|hi|hey|yo|sup|greetings)\b/` — The realm does not say hello back. The realm is busy refreshing.  #egg.hello
- ~ `/^(thanks|thank you|ty)\b/` — You are welcome. This is the first gratitude the realm has received since 2019. It does not know what to do with it.  #egg.thanks
- ~ `/^(sorry|apologize|my bad)\b/` — Apology logged. Outcome: no change. Same as every retrospective.  #egg.sorry
- ~ `/\b(damn|hell|crap|wtf|ffs|fuck|shit)\b|^(ugh+|argh+|dammit|damn it|seriously|jeez|grr+|bah|hmph|omg)$/` — Come now. Don't get throttled.  #egg.swear
- ~ `/^please\b/` — Manners will not help you here. Two words. Verb, noun.  #egg.please
- ~ `/^(what|huh|wat|what now|what do i do)\??$/` — What indeed. Try 'look'. Try 'help'. Try, as a last resort, 'get ye flask'.  #egg.what
- ~ `/^(plot|graph|chart|visualize|make a chart)\b/` — You make a chart. It is a pie chart. It has 31 slices. The realm looks away.  #egg.plot
- ~ `/^(publish|deploy|ship)\b/` — Publish from where? You are not in Desktop.  #egg.publish
- ~ `/^git\b/` — The realm does not have version control. The realm has 'Sales_v3_FINAL_final2.pbix'.  #egg.git
- ~ `/^trogdor\b/` — A different dragon. A different realm. This one only throttles.  #egg.dragon-name
- ~ `/^(boo|scare|spook)\b/` — Boo. Nobody is scared. The realm has seen a 400-column table.  #egg.boo
- ~ `/^(who are you|who is this|narrator)\b/` — I am the narrator. I have watched eleven thousand Report Builders die in the Bronze Marsh. You are not special. You are, so far, alive.  #egg.dad
- ~ `/^(cast|abracadabra|magic|spell|alakazam)\b/` — You cast a spell. It is CALCULATETABLE. Nothing in the realm is prepared for it, least of all you.  #egg.magic
- ~ `/^(count|how many)\b/` — count() is not supported. Select fewer fields and compute results.length, peasant.  #egg.count
### God mode (burninate)
- godhelp · rooms · warp <room> · prompts [all|global] · summon <item> · locate <thing> · flags · settings · set <key> on|off · burninate
